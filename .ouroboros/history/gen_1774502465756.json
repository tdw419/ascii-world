{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const w1 = Math.sin(x*0.018+t*0.3)*18 + Math.cos(y*0.013-t*0.22)*12;\n  const w2 = Math.cos(x*0.012-t*0.15)*20 + Math.sin(y*0.02+t*0.28)*14;\n  const sx = x + w1 + Math.sin(y*0.008+t*0.1)*8, sy = y + w2 + Math.cos(x*0.009-t*0.12)*8;\n  const src = [\n    [120+Math.sin(t*0.5)*90, 60+Math.cos(t*0.4)*40, 0.07, 2.1, 1.0],\n    [360+Math.cos(t*0.45)*80, 70+Math.sin(t*0.55)*35, 0.09, 1.8, 0.8],\n    [240+Math.sin(t*0.3+2)*130, 120+Math.cos(t*0.45+1)*80, 0.055, 2.4, 1.2],\n    [80+Math.cos(t*0.7+1)*70, 180+Math.sin(t*0.5+3)*55, 0.11, 1.6, 0.9],\n    [400+Math.sin(t*0.35+4)*85, 170+Math.cos(t*0.8+2)*65, 0.065, 2.7, 1.1],\n    [240+Math.cos(t*0.3+5)*110, 210+Math.sin(t*0.5)*70, 0.08, 2.0, 0.7],\n    [180+Math.sin(t*0.6+3)*60, 30+Math.cos(t*0.35+4)*50, 0.1, 1.9, 1.3],\n    [300+Math.cos(t*0.4+1)*75, 200+Math.sin(t*0.65+2)*60, 0.075, 2.2, 0.85]\n  ];\n  let aS = 0, iS = 0, cross = 0;\n  for (const [sX, sY, f, sp, w] of src) {\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    const a = Math.sin(d*f - t*sp), i = Math.sin(d*f*0.7 + t*sp*0.6 + 1.5);\n    aS += a * w; iS += i * w;\n    cross += Math.sin(d*f*1.3 - t*sp*0.4 + 2.0) * w * 0.5;\n  }\n  const act = aS/8, inh = iS/8, crs = cross/8;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const sp1 = Math.sin(dist*0.045-ang*3.5-t*1.3), sp2 = Math.sin(dist*0.035+ang*4.5-t*1.0);\n  const sp3 = Math.sin(dist*0.06-ang*2-t*1.8)*0.5;\n  const spiral = (sp1+sp2)*0.35 + sp3;\n  const bg = (Math.sin(sx*0.035+sy*0.022+t*0.7) + Math.sin(sx*0.028-sy*0.038+t*1.0))/2;\n  const bg2 = (Math.sin((sx+sy)*0.1+t*2.2) + Math.sin((sx-sy)*0.085-t*1.8))*0.2;\n  const micro = (Math.sin(sx*0.2+sy*0.15-t*1.5)*Math.sin(sx*0.15-sy*0.2+t*1.2))*0.15;\n  const field = act - inh*0.65 + crs*0.4 + (bg+bg2)*0.25 + spiral*0.35 + micro;\n  const react = Math.tanh(field*2.0);\n  const cell = Math.tanh((Math.sin(sx*0.14)*Math.sin(sy*0.14)+Math.sin((sx+sy)*0.09+t)*0.4)*1.4)*0.25;\n  const edge = Math.abs(react) > 0.65 ? 1.2 : 1.0;\n  const glow = Math.exp(-dist*0.008) * (react*0.5+0.5) * 0.15;\n  const blend = react*0.7 + cell*0.2 + glow, v = blend*0.5+0.5;\n  const pal = [\n    [5,10,45],[10,40,80],[12,75,105],[18,120,125],[35,155,130],\n    [120,175,90],[190,180,65],[210,140,55],[195,95,60],[160,55,55],\n    [110,30,50],[60,18,45],[25,12,40],[5,10,45]\n  ];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const lum = 0.85 + Math.sin(react*Math.PI)*0.15;\n  return [\n    Math.min(255, (pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*edge)|0,\n    Math.min(255, (pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*edge)|0,\n    Math.min(255, (pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*edge)|0\n  ];\n}",
  "score": {
    "coherence": 6,
    "harmony": 5,
    "complexity": 9,
    "density": 4,
    "total": 24,
    "reason": "The image displays high mathematical complexity with intricate, organic patterns, but lacks visual coherence due to chaotic color transitions and no clear structure. Color harmony is low as the palette is jarring and unbalanced. Density is low — it's mostly abstract noise with minimal informational content — making it unsuitable for a UI. It's visually interesting but functionally weak for an OS framebuffer."
  },
  "timestamp": 1774502465755,
  "filename": "gen336_1774502465755"
}