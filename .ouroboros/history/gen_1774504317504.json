{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const wx1 = Math.sin(x * 0.02 + t * 0.3) * 15 + Math.cos(y * 0.015 - t * 0.2) * 10;\n  const wy1 = Math.cos(y * 0.02 - t * 0.25) * 15 + Math.sin(x * 0.018 + t * 0.35) * 10;\n  const wx2 = Math.sin((x+wx1) * 0.035 + t * 0.5) * 8 + Math.cos((y+wy1) * 0.03 - t * 0.4) * 6;\n  const wy2 = Math.cos((y+wy1) * 0.03 - t * 0.45) * 8 + Math.sin((x+wx1) * 0.025 + t * 0.55) * 6;\n  const sx = x + wx1 + wx2, sy = y + wy1 + wy2;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0, 0.7],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7, -0.5],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3, 0.9],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5, -0.8],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6, 0.6],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9, -0.4],\n    [80+Math.sin(t*0.45+1)*110, 40+Math.cos(t*0.65+3)*60, 0.11, 2.1, 0.3],\n    [400+Math.cos(t*0.55+2)*70, 200+Math.sin(t*0.75)*50, 0.085, 1.8, -0.7]\n  ];\n  let aS = 0, iS = 0, cS = 0;\n  for (const [sX, sY, f, sp, ph] of src) {\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    aS += Math.sin(d*f - t*sp + ph);\n    iS += Math.sin(d*f*0.7 + t*sp*0.6 + 1.5 + ph);\n    cS += Math.sin(d*f*1.3 - t*sp*0.8 + ph*2) * 0.3;\n  }\n  const n = src.length, act = aS/n, inh = iS/n, cross = cS/n;\n  const bg1 = (Math.sin(sx*0.04+sy*0.025+t*0.8) + Math.sin(sx*0.03-sy*0.04+t*1.1))/2;\n  const bg2 = Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2;\n  const bg3 = Math.sin(sx*0.08+t*0.6)*Math.sin(sy*0.07-t*0.4)*0.15;\n  const bg = bg1 + bg2 + bg3;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2) + Math.sin(dist*0.07-ang*2+t*0.8)*0.3)*0.35;\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5 + Math.sin(sx*0.2-sy*0.08+t*0.7)*0.3)*1.2)*0.35;\n  const detail = Math.sin(sx*0.3+sy*0.25+t*3)*Math.sin(sx*0.2-sy*0.3-t*2.5)*0.1;\n  const reaction = Math.tanh((act - inh*0.65 + cross*0.3 + bg*0.25 + spiral + cell + detail)*2.0);\n  const reaction2 = Math.tanh((act*0.5 + inh*0.3 + cross*0.5 - bg*0.2 + spiral*0.5)*1.5);\n  const blend = reaction*0.65 + reaction2*0.2 + cell*0.15, v = blend*0.5+0.5;\n  const pal = [[5,12,52],[12,45,85],[18,95,115],[30,140,130],[120,180,80],[210,195,60],[230,145,55],[215,90,55],[180,50,65],[120,25,55],[55,15,60],[15,10,45],[5,12,52]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const sf = f*f*(3-2*f);\n  const lum = 0.85 + Math.sin(reaction*Math.PI)*0.15 + Math.abs(reaction2)*0.1;\n  const edge = Math.abs(reaction)>0.75 ? 1.12+Math.sin(t*3)*0.03 : 1.0;\n  const hl = cross > 0.3 ? 1.08 : 1.0, mod = lum*edge*hl;\n  return [\n    Math.min(255, (pal[i][0]+(pal[i+1][0]-pal[i][0])*sf)*mod)|0,\n    Math.min(255, (pal[i][1]+(pal[i+1][1]-pal[i][1])*sf)*mod)|0,\n    Math.min(255, (pal[i][2]+(pal[i+1][2]-pal[i][2])*sf)*mod)|0\n  ];\n}",
  "score": {
    "coherence": 7,
    "harmony": 6,
    "complexity": 9,
    "density": 4,
    "total": 26,
    "reason": "The image displays high complexity with intricate, swirling patterns suggesting mathematical or fractal generation. Coherence is good with no obvious artifacts, though the central point is slightly less defined. Harmony is moderate due to the vibrant but clashing color palette (reds, greens, purples) which may be visually jarring. Density is low — it's mostly abstract noise with no clear UI elements or information — making it unsuitable for practical use in an OS interface."
  },
  "timestamp": 1774504317504,
  "filename": "gen388_1774504317504"
}