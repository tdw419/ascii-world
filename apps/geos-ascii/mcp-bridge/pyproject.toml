[project]
name = "mcp-ascii-bridge"
version = "1.0.0"
description = "MCP Bridge for ASCII World"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.25.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
