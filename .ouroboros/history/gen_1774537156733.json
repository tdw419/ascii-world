{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const dist0 = Math.sqrt((x-cx)**2 + (y-cy)**2);\n  const grav = 600 / (dist0 + 80);\n  const wx = Math.sin(x*0.02+t*0.3)*15 + Math.cos(y*0.015-t*0.2)*10 - (x-cx)*grav*0.0015;\n  const wy = Math.cos(y*0.02-t*0.25)*15 + Math.sin(x*0.018+t*0.35)*10 - (y-cy)*grav*0.0015;\n  const sx = x+wx, sy = y+wy;\n  const src = [\n    [160+Math.sin(t*0.6)*100,80+Math.cos(t*0.4)*50,0.08,2.0],\n    [320+Math.cos(t*0.5)*100,160+Math.sin(t*0.7)*50,0.1,1.7],\n    [240+Math.sin(t*0.3+2)*140,120+Math.cos(t*0.5+1)*90,0.06,2.3],\n    [120+Math.cos(t*0.8+1)*80,180+Math.sin(t*0.6+3)*60,0.12,1.5],\n    [360+Math.sin(t*0.4+4)*90,60+Math.cos(t*0.9+2)*70,0.07,2.6],\n    [240+Math.cos(t*0.35+5)*120,200+Math.sin(t*0.55)*80,0.09,1.9],\n    [80+Math.sin(t*0.45)*60,40+Math.cos(t*0.65)*30,0.05,3.0],\n    [400+Math.cos(t*0.55)*50,200+Math.sin(t*0.75)*40,0.055,2.8]\n  ];\n  let aS=0,iS=0,energy=0;\n  for(const[sX,sY,f,sp]of src){\n    const d=Math.sqrt((sx-sX)**2+(sy-sY)**2);\n    const wave=Math.sin(d*f-t*sp);\n    aS+=wave;iS+=Math.sin(d*f*0.7+t*sp*0.6+1.5);\n    energy+=1/(d+15)*Math.abs(wave);\n  }\n  const act=aS/8,inh=iS/8;energy/=8;\n  const n1=Math.sin(sx*0.04+sy*0.025+t*0.8),n2=Math.sin(sx*0.03-sy*0.04+t*1.1);\n  const n3=Math.sin((sx+sy)*0.12+t*2.5)*0.2,n4=Math.sin((sx-sy)*0.1-t*2)*0.2;\n  const n5=Math.sin(sx*0.08+sy*0.06+t*1.8)*0.15;\n  const bg=(n1+n2)/2+n3+n4+n5;\n  const dist=Math.sqrt((sx-cx)**2+(sy-cy)**2),ang=Math.atan2(sy-cy,sx-cx);\n  const spiral=(Math.sin(dist*0.05-ang*3-t*1.5)+Math.sin(dist*0.04+ang*4-t*1.2))*0.4;\n  const reaction=Math.tanh((act-inh*0.7+bg*0.3+spiral)*2.2);\n  const cell=Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15)+Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  const blend=reaction*0.75+cell*0.25;\n  const edgeGlow=Math.exp(-Math.abs(reaction)*6)*energy*4;\n  const photoPulse=Math.sin(t*1.5+dist0*0.03)*0.5+0.5;\n  const photoZone=Math.exp(-dist0*0.006)*photoPulse*0.35;\n  const fm=6,fp=12;\n  const frameH=(x>fm&&x<fm+2)||(x>477-fm&&x<479-fm)||(y>fm&&y<fm+2)||(y>237-fm&&y<239-fm);\n  const titleBar=y>fp&&y<fp+12;\n  const titleDots=x>fp+4&&x<fp+8&&titleBar;\n  const by=222,bh=6;\n  const b1w=100+Math.sin(t*0.5)*40,b2w=70+Math.cos(t*0.7)*25,b3w=55+Math.sin(t*0.3+1)*20;\n  const inB1=y>by&&y<by+bh&&x>18&&x<18+b1w;\n  const inB2=y>by&&y<by+bh&&x>140&&x<140+b2w;\n  const inB3=y>by&&y<by+bh&&x>228&&x<228+b3w;\n  const bOutline1=y>=by&&y<=by+bh&&x>=17&&x<=19+b1w;\n  const bOutline2=y>=by&&y<=by+bh&&x>=139&&x<=141+b2w;\n  const bOutline3=y>=by&&y<=by+bh&&x>=227&&x<=229+b3w;\n  const sep1=Math.abs(y-100)<0.5&&x>20&&x<230;\n  const sep2=Math.abs(y-100)<0.5&&x>250&&x<460;\n  const sep3=Math.abs(x-240)<0.5&&y>30&&y;\n  return [0, 0, 0];\n}",
  "score": {
    "coherence": 10,
    "harmony": 10,
    "complexity": 1,
    "density": 1,
    "total": 22,
    "reason": "The image is a solid black framebuffer, which is perfectly coherent and harmonious with no visual artifacts. However, it contains no patterns or information, making its complexity and density extremely low. This is functionally appropriate for a minimal OS framebuffer, but visually uninteresting."
  },
  "timestamp": 1774537156733,
  "filename": "gen7_1774537156733"
}