{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const wx = Math.sin(x*0.02+t*0.3)*15 + Math.cos(y*0.015-t*0.2)*10;\n  const wy = Math.cos(y*0.02-t*0.25)*15 + Math.sin(x*0.018+t*0.35)*10;\n  const sx = x + wx, sy = y + wy;\n  const primes = [2,3,5,7,11,13,17,19];\n  const src = primes.map((p,i) => {\n    const ph = i*1.618;\n    return [240+Math.sin(t*0.3+ph)*p*18, 120+Math.cos(t*0.4+ph)*p*12, 0.025+p*0.013, 0.9+p*0.16];\n  });\n  let aS=0, iS=0;\n  for(const [sX,sY,f,sp] of src){\n    const d=Math.sqrt((sx-sX)**2+(sy-sY)**2);\n    aS+=Math.sin(d*f-t*sp);\n    iS+=Math.sin(d*f*0.7+t*sp*0.6+1.5);\n  }\n  const n=src.length, act=aS/n, inh=iS/n;\n  const bg=(Math.sin(sx*0.04+sy*0.025+t*0.8)+Math.sin(sx*0.03-sy*0.04+t*1.1))/2+Math.sin((sx+sy)*0.12+t*2.5)*0.2+Math.sin((sx-sy)*0.1-t*2)*0.2;\n  const dist=Math.sqrt((sx-cx)**2+(sy-cy)**2), ang=Math.atan2(sy-cy,sx-cx);\n  const spiral=(Math.sin(dist*0.05-ang*3-t*1.5)+Math.sin(dist*0.04+ang*4-t*1.2))*0.4;\n  const reaction=Math.tanh((act-inh*0.7+bg*0.3+spiral)*2.5);\n  const cell=Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15)+Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  const blend=reaction*0.75+cell*0.25, v=blend*0.5+0.5;\n  const pal=[[2,4,18],[0,15,50],[0,60,110],[0,160,200],[20,240,220],[120,255,160],[240,255,60],[255,180,40],[255,80,60],[255,40,140],[160,0,240],[60,0,160],[2,4,18]];\n  const idx=v*(pal.length-1), i=Math.min(idx|0,pal.length-2), f=idx-i;\n  const lum=0.85+Math.sin(reaction*Math.PI)*0.35, edge=Math.abs(reaction)>0.65?1.5:1.0;\n  const neonGlow=Math.max(0,1-Math.abs(blend))*0.6;\n  let r=(pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*edge+neonGlow*40;\n  let g=(pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*edge+neonGlow*80;\n  let b=(pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*edge+neonGlow*120;\n  let ui=0, uiR=80, uiG=200, uiB=255;\n  const bw=(p,x0,x1,y0,y1)=>x0<=x&&x<=x1&&y0<=y&&y<=y1;\n  const be=(x0,x1,y0,y1,t2)=>bw(x,x0,x0+t2,y0,y1)||bw(x,x1-t2,x1,y0,y1)||bw(x,x0,x1,y0,y0+t2)||bw(x,x0,x1,y1-t2,y1);\n  if(be(12,238,8,105,2)){ui=0.7;uiR=0;uiG=180;uiB=255;}\n  if(be(248,468,8,85,2)){ui=0.7;uiR=0;uiG=255;uiB=180;}\n  if(be(12,468,130,138,1)){ui=0.4;uiR=40;uiG=40;uiB=80;}\n  if(bw(x,14,14+Math.max(0,(Math.sin(t*0.5)*0.5+0.5)*450),131,137)){ui=0.9;uiR=0;uiG=255;uiB=200;}\n  for(let li=0;li<5;li++){\n    const ly=18+li*15, len=60+Math.sin(t*0.25+li*1.7)*50;\n    if(y>=ly&&y<=ly+2&&x>=22&&x<=22+len){ui=Math.max(ui,0.45);uiR=100;uiG=220;uiB=255;}\n  }\n  for(let li=0;li<3;li++){\n    const ly=16+li*18, len=40+Math.sin(t*0.3+li*2.1)*35;\n    if(y>=ly&&y<=ly+2&&x>=258&&x<=258+len){ui=Math.max(ui,0.4);uiR=80;uiG=255;uiB=180;}\n  }\n  for(let bi=0;bi<8;bi++){\n    const bx=255+bi*25, bh=15+Math.abs(Math.sin(t*0.5+bi*0.9))*30;\n    if(x>=bx&&x<=bx+17&&y>=82-bh&&y<=82){ui=Math.max(ui,0.35);uiR=0;uiG=255;uiB=150;}\n  }\n  for(let pi=0;pi<8;pi++){\n    const px=25+pi*56, py=148, p=primes[pi], pulse=Math.sin(t*2.5+p)*0.5+0.5;\n    const d2=Math.sqrt((x-px)**2+(y-py)**2);\n    if(d2<p*0.7+pulse*4){const st=Math.max(0,1-d2/(p*0.7+pulse*4));ui=Math.max(ui,st*0.5*pulse);uiR=255;uiG=100;uiB=200;}\n    if(d2<3){ui=Math.max(ui,0.8);uiR=255;uiG=200;uiB=255;}\n  }\n  for(let layer=0;layer<4;layer++){\n    const ly=165+layer*18;\n    if(y>=ly&&y<=ly+1&&x>=15&&x<=465){ui=Math.max(ui,0.2);uiR=60;uiG=80;uiB=140;}\n    for(let ni=0;ni<8;ni++){\n      const nx=30+ni*57, d3=Math.sqrt((x-nx)**2+(y-ly)**2);\n      if(d3<5){const act2=Math.sin(t*3+ni*0.7+layer*1.3)*0.5+0.5;ui=Math.max(ui,0.5+act2*0.5);uiR=act2>0.5?0:200;uiG=act2>0.5?255:100;uiB=255;}\n    }\n  }\n  const sparkX=[80,200,350,420], sparkY=[200,215,195,220];\n  for(let si=0;si<4;si++){\n    const fl=Math.sin(t*8+si*4)>0.85?1:0;\n    const d4=Math.sqrt((x-sparkX[si])**2+(y-sparkY[si])**2);\n    if(d4<3&&fl){ui=1;uiR=255;uiG=255;uiB=255;}\n    if(d4<8&&fl){ui=Math.max(ui,0.4);uiR=200;uiG=240;uiB=255;}\n  }\n  const scanline=Math.sin(y*1.5+t*4)*0.03+0.97;\n  if(ui>0){r=r*(1-ui)+uiR*ui;g=g*(1-ui)+uiG*ui;b=b*(1-ui)+uiB*ui;}\n  return [Math.min(255,r*scanline)|0,Math.min(255,g*scanline)|0,Math.min(255,b*scanline)|0];\n}",
  "score": {
    "coherence": 6,
    "harmony": 5,
    "complexity": 9,
    "density": 4,
    "total": 24,
    "reason": "The image has high visual complexity with fractal-like patterns and vibrant color gradients, but lacks coherence due to chaotic, non-uniform shapes. Color harmony is low because of clashing neon tones. Density is low — it's mostly abstract art with minimal functional UI elements — making it unsuitable for a practical OS interface."
  },
  "timestamp": 1774630819267,
  "filename": "gen2001_1774630819267"
}