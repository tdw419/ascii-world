{
  "code": "(x, y, t) => {\n  const s = [\n    [120 + Math.sin(t * 0.5) * 80, 60 + Math.cos(t * 0.35) * 40],\n    [360 + Math.cos(t * 0.45) * 80, 60 + Math.sin(t * 0.55) * 40],\n    [240 + Math.sin(t * 0.3 + 1) * 100, 120 + Math.cos(t * 0.4 + 2) * 60],\n    [100 + Math.cos(t * 0.6 + 3) * 70, 180 + Math.sin(t * 0.5 + 1) * 50],\n    [380 + Math.sin(t * 0.55 + 2) * 70, 180 + Math.cos(t * 0.45 + 3) * 50]\n  ];\n  const d = s.map(p => Math.sqrt((x - p[0]) ** 2 + (y - p[1]) ** 2));\n  const w = d.map((dist, i) => {\n    const f = [0.08, 0.1, 0.065, 0.09, 0.075][i];\n    const sp = [2.0, 1.7, 2.3, 1.9, 2.1][i];\n    return Math.sin(dist * f - t * sp) + Math.sin(dist * f * 2.1 - t * sp * 1.3) * 0.4;\n  });\n  const bg1 = Math.sin(x * 0.05 + y * 0.03 + t * 0.8);\n  const bg2 = Math.sin(x * 0.03 - y * 0.05 + t * 1.1);\n  const bg3 = Math.sin((x + y) * 0.15 + t * 3) * 0.15;\n  const bg4 = Math.sin((x - y) * 0.12 - t * 2.5) * 0.15;\n  const warpX = x + (w[0] * 8 + w[1] * 6 + bg1 * 4);\n  const warpY = y + (w[2] * 8 + w[3] * 6 + bg2 * 4);\n  const wbg1 = Math.sin(warpX * 0.04 + warpY * 0.02 + t * 1.2);\n  const wbg2 = Math.sin(warpX * 0.02 - warpY * 0.04 - t * 0.9);\n  const activator = (w[0] + w[1] + w[2] + w[3] + w[4]) / 5;\n  const inhibitor = (bg1 + bg2 + wbg1 + wbg2) / 4;\n  const reaction = activator - inhibitor * 0.7 + bg3 + bg4;\n  const reaction2 = Math.tanh(reaction * 2.5);\n  const fine = Math.sin(reaction2 * 12 + t * 0.5) * 0.15;\n  const cx = 240, cy = 120;\n  const dist = Math.sqrt((x - cx) ** 2 + (y - cy) ** 2);\n  const ang = Math.atan2(y - cy, x - cx);\n  const radial = Math.sin(dist * 0.04 - t * 1.5) + Math.sin(dist * 0.08 - t * 2.2) * 0.5;\n  const angular = Math.sin(ang * 6 + t * 0.4) + Math.sin(ang * 3 - t * 0.7) * 0.3;\n  const spiral = Math.sin(dist * 0.05 + ang * 4 - t * 1.8);\n  const pattern2 = Math.tanh((radial + angular * 0.5 + spiral * 0.3) * 2);\n  const blend = reaction2 * 0.5 + pattern2 * 0.3 + fine * 0.2;\n  const v = blend * 0.5 + 0.5;\n  const pal = [\n    [8, 15, 58], [15, 55, 95], [20, 110, 120], [45, 160, 140],\n    [180, 185, 60], [220, 160, 50], [210, 90, 60], [160, 50, 80],\n    [80, 25, 90], [30, 18, 65]\n  ];\n  const idx = v * (pal.length - 1);\n  const i = Math.min(idx | 0, pal.length - 2);\n  const f = idx - i;\n  const sf = f * f * (3 - 2 * f);\n  const vig = 1 - Math.pow(dist / 280, 2.5) * 0.35;\n  const lum = (0.82 + Math.sin(reaction * Math.PI) * 0.18) * vig;\n  return [\n    Math.min(255, (pal[i][0] + (pal[i + 1][0] - pal[i][0]) * sf) * lum) | 0,\n    Math.min(255, (pal[i][1] + (pal[i + 1][1] - pal[i][1]) * sf) * lum) | 0,\n    Math.min(255, (pal[i][2] + (pal[i + 1][2] - pal[i][2]) * sf) * lum) | 0\n  ];\n}",
  "score": {
    "coherence": 8,
    "harmony": 7,
    "complexity": 9,
    "density": 5,
    "total": 29,
    "reason": "The image has strong visual coherence with smooth gradients and no obvious artifacts. Its color harmony is decent but slightly chaotic due to the high saturation. Complexity is high with intricate, swirling patterns. However, density is low — it's mostly abstract art with minimal functional UI information, making it unsuitable for a practical OS interface."
  },
  "timestamp": 1774491591871,
  "filename": "gen25_1774491591871"
}