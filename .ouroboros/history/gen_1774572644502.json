{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const wx = Math.sin(x*0.02+t*0.3)*15 + Math.cos(y*0.015-t*0.2)*10;\n  const wy = Math.cos(y*0.02-t*0.25)*15 + Math.sin(x*0.018+t*0.35)*10;\n  const sx = x + wx, sy = y + wy;\n  const primes = [2, 3, 5, 7, 11, 13];\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.16, 2.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.30, 1.7],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.30, 2.3],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.84, 1.5],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.77, 2.6],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 1.17, 1.9]\n  ];\n  let aS = 0, iS = 0;\n  for (let i = 0; i < 6; i++) {\n    const [sX, sY, f, sp] = src[i];\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    const w = primes[i] / 7;\n    aS += Math.sin(d*f - t*sp) * w;\n    iS += Math.sin(d*f*0.7 + t*sp*0.6 + 1.5) * w;\n  }\n  const act = aS/6, inh = iS/6;\n  const bg = (Math.sin(sx*0.04+sy*0.025+t*0.8) + Math.sin(sx*0.03-sy*0.04+t*1.1))/2 + Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2))*0.4;\n  const saccade = Math.exp(-((dist-50-Math.sin(t*1.55)*30)**2)/800) * Math.sin(t*14)*0.5;\n  const reaction = Math.tanh((act - inh*0.7 + bg*0.3 + spiral + saccade)*2.2);\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  const gX = 1 - Math.min(1, Math.abs(Math.sin(x*0.05))*80);\n  const gY = 1 - Math.min(1, Math.abs(Math.sin(y*0.05))*80);\n  let ui = 0;\n  for (let i = 0; i < 6; i++) {\n    const bx = 12 + i*78, bw = 56*(0.5+0.5*Math.sin(t*primes[i]*0.3));\n    if (y >= 226 && y <= 234) {\n      if (x >= bx && x <= bx+56) ui = Math.max(ui, 0.06);\n      if (x >= bx && x <= bx+bw) ui = Math.max(ui, 0.55 + 0.2*Math.sin(t*2+i));\n    }\n  }\n  const winF = ((x>5&&x<155&&y>5&&y<95)||(x>325&&x<475&&y>5&&y<75)||(x>160&&x<320&&y>130&&y<200)) ? 0.08 : 0;\n  const winB = (((x===5||x===155)&&y>=5&&y<=95)||((x===325||x===475)&&y>=5&&y<=75)||((x===160||x===320)&&y>=130&&y<=200)) ? 0.7 : 0;\n  const txtLine = ((y===20||y===35||y===50||y===65||y===80)&&x>15&&x<145) ? 0.25*Math.sin(x*0.3+t*2)**2 : 0;\n  const layerH = (Math.sin(y*0.08+t*0.5) > 0.92) ? 0.06 : 0;\n  const dataViz = Math.sin(x*0.08)*Math.cos(t*1.5) > 0.7 && y > 100 && y < 125 ? 0.3 : 0;\n  const blend = reaction*0.55 + cell*0.12 + gX*0.04 + gY*0.04 + ui + winF + winB + txtLine + layerH + dataViz;\n  let v = Math.max(0, Math.min(1, blend*0.5+0.5));\n  const pal = [[2,3,18],[4,15,55],[8,50,110],[0,160,190],[0,255,170],[255,230,0],[255,70,15],[255,15,70],[160,0,240],[30,3,15]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const edge = Math.abs(reaction), glow = edge > 0.55 ? 1.0 + (edge-0.55)*1.8 : 1.0;\n  const lum = 0.8 + Math.sin(reaction*Math.PI)*0.2;\n  const uiGlow = (winB > 0.3 || ui > 0.4) ? 1.4 : 1.0;\n  return [\n    Math.min(255, (pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*glow*uiGlow)|0,\n    Math.min(255, (pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*glow*uiGlow)|0,\n    Math.min(255, (pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*glow*uiGlow)|0\n  ];\n}",
  "score": {
    "coherence": 5,
    "harmony": 4,
    "complexity": 9,
    "density": 3,
    "total": 21,
    "reason": "The image features high mathematical complexity with intricate, swirling patterns, but lacks visual coherence due to chaotic, overlapping elements. Color harmony is poor with jarring neon contrasts. Density is low as there's minimal functional information, making it unsuitable for a UI despite its visually rich complexity."
  },
  "timestamp": 1774572644502,
  "filename": "gen928_1774572644502"
}