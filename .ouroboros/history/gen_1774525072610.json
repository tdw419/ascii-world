{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  let wx = Math.sin(x*0.02+t*0.3)*15 + Math.cos(y*0.015-t*0.2)*10;\n  let wy = Math.cos(y*0.02-t*0.25)*15 + Math.sin(x*0.018+t*0.35)*10;\n  wx += Math.sin((x+wx)*0.025+y*0.01+t*0.5)*8;\n  wy += Math.cos((y+wy)*0.025-x*0.01+t*0.45)*8;\n  const sx = x+wx, sy = y+wy;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0, 1.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7, 0.8],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3, 1.2],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5, 0.7],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6, 0.9],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9, 1.1],\n    [80+Math.sin(t*0.45+3)*70, 40+Math.cos(t*0.65+1)*50, 0.11, 2.1, 0.6],\n    [400+Math.cos(t*0.55+2)*60, 220+Math.sin(t*0.75)*40, 0.05, 2.8, 1.3]\n  ];\n  let aS=0, iS=0, mS=0, tw=0;\n  for (const [sX,sY,f,sp,w] of src) {\n    const d = Math.sqrt((sx-sX)**2+(sy-sY)**2);\n    aS += Math.sin(d*f-t*sp)*w;\n    iS += Math.sin(d*f*0.7+t*sp*0.6+1.5)*w;\n    mS += Math.sin(d*f*1.3-t*sp*0.4+3)*w*0.5;\n    tw += w;\n  }\n  const act=aS/tw, inh=iS/tw, mut=mS/tw;\n  const bg = (Math.sin(sx*0.04+sy*0.025+t*0.8)*0.4+Math.sin(sx*0.03-sy*0.04+t*1.1)*0.3+Math.sin((sx+sy)*0.12+t*2.5)*0.15+Math.sin((sx-sy)*0.1-t*2)*0.15+Math.sin(sx*0.08+sy*0.06-t*0.6)*0.2+Math.sin(sx*0.05-sy*0.09+t*1.8)*0.15)/1.35;\n  const dist = Math.sqrt((sx-cx)**2+(sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5)*0.3+Math.sin(dist*0.04+ang*4-t*1.2)*0.3+Math.sin(dist*0.07-ang*5+t*0.8)*0.2+Math.sin(dist*0.03+ang*2-t*2)*0.2)*0.4;\n  const vx = sx*0.08, vy = sy*0.08;\n  const vein = Math.abs(Math.sin(vx+Math.sin(vy+t*0.3)*2))*Math.abs(Math.sin(vy+Math.cos(vx-t*0.2)*2));\n  const lap = Math.sin(sx*0.22+t*0.7)+Math.sin(sy*0.22-t*0.5)+Math.sin((sx+sy)*0.16+t*0.9)+Math.sin((sx-sy)*0.16-t*0.8);\n  const r1 = act-inh*0.7+bg*0.3+spiral;\n  const r2 = r1+mut*0.3+vein*0.15-Math.abs(inh-act)*0.5+lap*0.1;\n  const reaction = Math.tanh(r2*1.8);\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15)+Math.sin((sx+sy)*0.1+t)*0.5+Math.sin(sx*0.08-t*0.4)*Math.cos(sy*0.12+t*0.3)*0.3)*1.5)*0.25;\n  const blend = reaction*0.6+cell*0.2+vein*0.2;\n  const v = blend*0.5+0.5;\n  const pal = [[5,10,48],[8,35,85],[12,65,118],[18,105,140],[35,148,138],[90,172,118],[155,188,82],[195,190,58],[218,162,52],[222,118,58],[198,72,62],[152,42,72],[98,24,62],[48,14,52],[5,10,48]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const lum = 0.72+Math.sin(reaction*Math.PI)*0.28;\n  const edge = Math.abs(reaction)>0.6?1.18:1.0;\n  const glow = Math.max(0,1-dist*0.003)*0.18+0.82;\n  const chroma = 1+Math.sin(reaction*2.5)*0.06;\n  const L = lum*edge*glow;\n  const c1 = pal[i][0]+(pal[i+1][0]-pal[i][0])*f, c2 = pal[i][1]+(pal[i+1][1]-pal[i][1])*f, c3 = pal[i][2]+(pal[i+1][2]-pal[i][2])*f;\n  return [\n    Math.min(255, (c1*chroma*L))|0,\n    Math.min(255, (c2*L))|0,\n    Math.min(255, (c3*(2-chroma)*L))|0\n  ];\n}",
  "score": {
    "coherence": 7,
    "harmony": 6,
    "complexity": 9,
    "density": 4,
    "total": 26,
    "reason": "The image has high complexity with intricate, flowing patterns resembling heatmaps or fluid dynamics. Coherence is good with no obvious artifacts, though the organic shapes may feel slightly chaotic. Harmony is moderate due to the vibrant, contrasting colors that are visually striking but not harmonious. Density is low as there's no clear UI or information structure, making it more decorative than functional for an OS."
  },
  "timestamp": 1774525072609,
  "filename": "gen971_1774525072609"
}