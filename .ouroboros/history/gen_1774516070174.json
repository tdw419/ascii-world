{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const wx = Math.sin(x*0.02+t*0.3)*15 + Math.cos(y*0.015-t*0.2)*10 + Math.sin(x*0.045-y*0.03+t*0.7)*6;\n  const wy = Math.cos(y*0.02-t*0.25)*15 + Math.sin(x*0.018+t*0.35)*10 + Math.cos(x*0.035+y*0.04-t*0.6)*6;\n  const sx = x+wx, sy = y+wy;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0, 0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7, 1.2],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3, 2.4],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5, 3.6],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6, 4.8],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9, 0.8],\n    [80+Math.sin(t*0.45+3)*70, 40+Math.cos(t*0.65+2)*50, 0.11, 2.1, 5.2],\n    [400+Math.cos(t*0.55+1)*60, 200+Math.sin(t*0.75)*70, 0.075, 1.8, 3.0]\n  ];\n  let aS = 0, iS = 0, crossPhase = 0;\n  for (const [sX, sY, f, sp, ph] of src) {\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    aS += Math.sin(d*f - t*sp + ph);\n    iS += Math.sin(d*f*0.7 + t*sp*0.6 + 1.5 + ph*0.5);\n    crossPhase += Math.sin(d*f*1.3 - t*sp*0.8 + ph*0.3) * Math.cos(d*f*0.5 + t*sp*0.4);\n  }\n  const act = aS/8, inh = iS/8, cross = crossPhase/8;\n  const bg = (Math.sin(sx*0.04+sy*0.025+t*0.8) + Math.sin(sx*0.03-sy*0.04+t*1.1))/2 + Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2) + Math.sin(dist*0.07-ang*2+t*0.9)*0.5)*0.3;\n  const fbm = Math.sin(sx*0.08+t*0.4)*Math.sin(sy*0.09-t*0.3) + Math.sin((sx*0.15+sy*0.12)*0.5+t*0.7)*0.5 + Math.sin(sx*0.22-sy*0.18+t*1.2)*0.25;\n  const reaction = Math.tanh((act - inh*0.65 + bg*0.25 + spiral + cross*0.3 + fbm*0.15)*2.0);\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5 + Math.sin(sx*0.2-sy*0.15+t*0.8)*0.3)*1.5)*0.3;\n  const micro = Math.sin(sx*0.35+t*1.5)*Math.sin(sy*0.3-t*1.2)*Math.sin((sx+sy)*0.2+t)*0.15;\n  const blend = reaction*0.65 + cell*0.2 + micro*0.15, v = blend*0.5+0.5;\n  const pal = [[6,12,52],[10,45,85],[12,95,105],[18,155,135],[55,180,120],[180,195,80],[220,160,55],[215,100,60],[180,50,70],[110,25,65],[45,12,55],[6,12,52]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const lum = 0.85+Math.sin(reaction*Math.PI)*0.15;\n  const edge = Math.abs(reaction)>0.65 ? 1.0+Math.abs(reaction)*0.2 : 1.0;\n  const glow = Math.max(0, 1.0-Math.abs(reaction)*1.8)*0.15;\n  return [\n    Math.min(255, ((pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*edge + glow*60)|0),\n    Math.min(255, ((pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*edge + glow*80)|0),\n    Math.min(255, ((pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*edge + glow*120)|0)\n  ];\n}",
  "score": {
    "coherence": 8,
    "harmony": 7,
    "complexity": 9,
    "density": 5,
    "total": 29,
    "reason": "The image has strong visual coherence with smooth gradients and no obvious artifacts. Its color harmony is decent but slightly chaotic due to the high-contrast red-green palette. Complexity is high with intricate, flowing patterns suggesting a mathematical or procedural origin. However, density is low — it's mostly abstract noise with no clear UI elements or information — making it unsuitable for a functional OS interface."
  },
  "timestamp": 1774516070174,
  "filename": "gen706_1774516070174"
}