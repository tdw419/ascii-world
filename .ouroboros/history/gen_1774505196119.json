{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const w1x = Math.sin(x*0.015+t*0.3)*20+Math.cos(y*0.012-t*0.2)*15;\n  const w1y = Math.cos(y*0.018-t*0.25)*20+Math.sin(x*0.014+t*0.35)*15;\n  const wx = w1x+Math.sin((x+w1x)*0.025+t*0.5)*8+Math.cos((y+w1y)*0.02-t*0.4)*6;\n  const wy = w1y+Math.cos((y+w1y)*0.022-t*0.45)*8+Math.sin((x+w1x)*0.019+t*0.55)*6;\n  const sx = x+wx, sy = y+wy;\n  const src = [];\n  for (let i = 0; i < 9; i++) {\n    const a = t*(0.12+i*0.04)+i*Math.PI*0.222;\n    const r = 50+i*22+Math.sin(t*0.25+i)*35;\n    src.push([cx+Math.cos(a)*r+Math.sin(t*0.35+i*1.3)*45, cy+Math.sin(a)*r*0.65+Math.cos(t*0.3+i*0.9)*35, 0.045+(i%3)*0.022, 1.1+Math.sin(i*0.8)*0.7]);\n  }\n  let act=0, inh=0, inh2=0;\n  for (const [sX,sY,f,sp] of src) {\n    const d = Math.sqrt((sx-sX)**2+(sy-sY)**2);\n    const att = 1/(1+d*0.004);\n    act += Math.sin(d*f-t*sp)*att;\n    inh += Math.sin(d*f*0.6+t*sp*0.5+1.2)*att;\n    inh2 += Math.cos(d*f*0.4-t*sp*0.25+2.8)*att;\n  }\n  act/=9; inh/=9; inh2/=9;\n  const bg = (Math.sin(sx*0.033+sy*0.021+t*0.65)+Math.sin(sx*0.026-sy*0.036+t*0.95))/2+Math.sin((sx+sy)*0.095+t*2.1)*0.28+Math.sin((sx-sy)*0.075-t*1.7)*0.22;\n  const dist=Math.sqrt((sx-cx)**2+(sy-cy)**2), ang=Math.atan2(sy-cy,sx-cx);\n  const spiral = Math.sin(dist*0.042-ang*3.5-t*1.2)*0.45+Math.sin(dist*0.032+ang*5.5-t*0.85)*0.3+Math.sin(dist*0.055-ang*2-t*1.9)*0.2;\n  const dist2=Math.sqrt((sx-180)**2+(sy-80)**2), ang2=Math.atan2(sy-80,sx-180);\n  const spiral2 = Math.sin(dist2*0.038+ang2*4-t*1.4)*0.35;\n  const hx=sx*0.075, hy=sy*0.075;\n  const hex = Math.sin((hx+Math.sin(hy*1.732)*0.5)*6.28+(hy+Math.cos(hx*1.732)*0.5)*6.28+t*0.7)*0.2;\n  const reaction = Math.tanh((act-inh*0.55-inh2*0.25+bg*0.22+spiral*0.18+spiral2*0.1+hex)*2.6);\n  const cell = Math.tanh((Math.sin(sx*0.11)*Math.sin(sy*0.11)+Math.sin((sx+sy)*0.085+t*0.45)*0.5+Math.cos((sx-sy)*0.1-t*0.25)*0.3)*1.7)*0.22;\n  const v = (reaction*0.68+cell*0.18+spiral*0.08+spiral2*0.06)*0.5+0.5;\n  const pal = [[4,8,38],[10,38,78],[16,78,108],[22,128,118],[60,162,105],[140,188,78],[210,175,52],[218,118,55],[188,62,72],[128,32,82],[68,22,72],[32,14,55],[4,8,38]];\n  const idx=v*(pal.length-1), i=Math.min(idx|0,pal.length-2), f=idx-i, sf=f*f*(3-2*f);\n  const r0=pal[i][0]+(pal[i+1][0]-pal[i][0])*sf, g0=pal[i][1]+(pal[i+1][1]-pal[i][1])*sf, b0=pal[i][2]+(pal[i+1][2]-pal[i][2])*sf;\n  const lum=0.72+Math.sin(reaction*Math.PI)*0.28;\n  const edge=Math.abs(reaction)>0.62?1.22:(Math.abs(reaction)>0.28?1.06:0.93);\n  const glow=Math.max(0,Math.sin(reaction*Math.PI*2)*0.18);\n  return [Math.min(255,(r0*lum*edge+glow*75)|0), Math.min(255,(g0*lum*edge+glow*55)|0), Math.min(255,(b0*lum*edge+glow*95)|0)];\n}",
  "score": {
    "coherence": 8,
    "harmony": 6,
    "complexity": 9,
    "density": 3,
    "total": 26,
    "reason": "The image has high coherence with smooth, flowing wave patterns and no visual artifacts. Its complexity is high due to intricate mathematical waveforms. However, the color harmony is moderate — the vibrant, saturated palette is visually striking but potentially overwhelming. The density is low as it's purely decorative with no functional UI elements, making it unsuitable for a practical OS interface."
  },
  "timestamp": 1774505196119,
  "filename": "gen412_1774505196119"
}