{
  "code": "(x, y, t) => {\n  let u = x / 55, v = y / 55, p = 0, q = 0;\n  for (let i = 0; i < 7; i++) {\n    let f = 1.1 + i * 0.65, tp = t * (0.12 + i * 0.055) * (i & 1 ? 1 : -1);\n    p += Math.sin(u * f + Math.sin(v * f * 0.7 + tp) * 2.8) / (1 + i * 0.35);\n    q += Math.cos(v * f + Math.cos(u * f * 0.65 - tp * 0.85) * 2.8) / (1 + i * 0.35);\n  }\n  let w1 = Math.sin(p * 2.2 + q * 1.6 + t * 0.18);\n  let w2 = Math.cos(p * 1.7 - q * 2.3 - t * 0.14);\n  let w3 = Math.sin((p + q) * 1.4 + t * 0.22);\n  let rd = w1 * w2 + w3 * 0.35;\n  rd = 1 / (1 + Math.exp(-rd * 4.5));\n  let fine = Math.sin(p * 6 + q * 5 + t * 0.4) * 0.08 * rd;\n  let edge = Math.abs(rd - 0.5) < 0.12 ? 0.15 * (1 - Math.abs(rd - 0.5) / 0.12) : 0;\n  let val = rd + fine + edge;\n  let hue = (0.54 + val * 0.14 + Math.sin(t * 0.08) * 0.04) % 1;\n  let sat = 0.6 + rd * 0.35;\n  let lit = 0.08 + val * 0.52 + edge * 0.3;\n  let c = (1 - Math.abs(2 * lit - 1)) * sat;\n  let hh = hue * 6, xx = c * (1 - Math.abs(hh % 2 - 1)), m = lit - c / 2;\n  let r, g, b;\n  if (hh < 1) { r = c; g = xx; b = 0; }\n  else if (hh < 2) { r = xx; g = c; b = 0; }\n  else if (hh < 3) { r = 0; g = c; b = xx; }\n  else if (hh < 4) { r = 0; g = xx; b = c; }\n  else if (hh < 5) { r = xx; g = 0; b = c; }\n  else { r = c; g = 0; b = xx; }\n  return [Math.max(0, Math.min(1, r + m + edge * 0.1)), Math.max(0, Math.min(1, g + m + edge * 0.15)), Math.max(0, Math.min(1, b + m + edge * 0.2))];\n}",
  "score": {
    "coherence": 10,
    "harmony": 10,
    "complexity": 1,
    "density": 1,
    "total": 22,
    "reason": "The image is a solid black framebuffer, which is perfectly coherent and harmonious with no visual artifacts. However, it contains no mathematical patterns or useful UI information, making its complexity and density extremely low."
  },
  "timestamp": 1774490469572,
  "filename": "gen5_1774490469572"
}