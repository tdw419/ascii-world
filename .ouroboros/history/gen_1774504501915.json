{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const w1 = Math.sin(x * 0.018 + t * 0.35) * 18 + Math.cos(y * 0.013 - t * 0.22) * 12;\n  const w2 = Math.cos(y * 0.016 - t * 0.28) * 14 + Math.sin(x * 0.021 + t * 0.4) * 9;\n  const w3 = Math.sin((x + y) * 0.01 + t * 0.15) * 8;\n  const sx = x + w1 + w3 * 0.5, sy = y + w2 + w3 * 0.5;\n  const src = [\n    [160+Math.sin(t*0.6)*110, 80+Math.cos(t*0.4)*55, 0.075, 2.1],\n    [320+Math.cos(t*0.5)*105, 160+Math.sin(t*0.7)*55, 0.095, 1.8],\n    [240+Math.sin(t*0.3+2)*145, 120+Math.cos(t*0.5+1)*95, 0.055, 2.4],\n    [120+Math.cos(t*0.8+1)*85, 180+Math.sin(t*0.6+3)*65, 0.115, 1.6],\n    [360+Math.sin(t*0.4+4)*95, 60+Math.cos(t*0.9+2)*75, 0.065, 2.7],\n    [240+Math.cos(t*0.35+5)*125, 200+Math.sin(t*0.55)*85, 0.085, 2.0],\n    [80+Math.sin(t*0.45+3)*70, 40+Math.cos(t*0.65+1)*50, 0.13, 1.4],\n    [400+Math.cos(t*0.55+2)*80, 200+Math.sin(t*0.75+4)*60, 0.07, 2.5],\n    [200+Math.sin(t*0.38+6)*100, 30+Math.cos(t*0.48)*70, 0.1, 1.9]\n  ];\n  let aS = 0, iS = 0, hS = 0;\n  for (const [sX, sY, f, sp] of src) {\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    const w = Math.sin(d * 0.003 + t * 0.2) * 0.15 + 1;\n    aS += Math.sin(d * f * w - t * sp);\n    iS += Math.sin(d * f * 0.65 * w + t * sp * 0.55 + 1.5);\n    hS += Math.sin(d * f * 1.4 * w - t * sp * 1.3 + 3.0) * 0.3;\n  }\n  const n = src.length;\n  const act = aS / n, inh = iS / n, har = hS / n;\n  const bg1 = (Math.sin(sx*0.035+sy*0.022+t*0.85) + Math.sin(sx*0.028-sy*0.038+t*1.15))/2;\n  const bg2 = Math.sin((sx+sy)*0.11+t*2.6)*0.18 + Math.sin((sx-sy)*0.095-t*2.1)*0.18;\n  const bg3 = Math.sin(sx*0.07+sy*0.05+t*1.8)*Math.cos(sx*0.04-sy*0.06+t*1.3)*0.15;\n  const bg = bg1 + bg2 + bg3;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const sp1 = (Math.sin(dist*0.045-ang*3.5-t*1.6) + Math.sin(dist*0.035+ang*4.5-t*1.3))*0.35;\n  const sp2 = Math.sin(dist*0.08+ang*2-t*2.5)*Math.cos(dist*0.06-ang*5+t*1.8)*0.15;\n  const micro = (Math.sin(sx*0.22+t*3)*Math.sin(sy*0.22-t*2.5) + Math.sin(sx*0.18-sy*0.18+t*2))*0.12;\n  const rd1 = Math.tanh((act - inh * 0.65 + bg * 0.35 + sp1 + sp2) * 2.0);\n  const rd2 = Math.tanh((act * 0.8 + har - inh * 0.5 + micro) * 1.8);\n  const cell = Math.tanh((Math.sin(sx*0.14)*Math.sin(sy*0.14) + Math.sin((sx+sy)*0.095+t)*0.5)*1.5)*0.25;\n  const pulse = 0.92 + Math.sin(t * 0.7) * 0.08;\n  const blend = (rd1 * 0.5 + rd2 * 0.3 + cell * 0.2) * pulse;\n  const v = blend * 0.5 + 0.5;\n  const pal = [\n    [5, 12, 58], [10, 45, 95], [12, 85, 120], [18, 130, 135],\n    [60, 170, 140], [160, 195, 90], [220, 195, 60], [225, 145, 55],\n    [200, 95, 60], [165, 50, 65], [110, 28, 60], [55, 18, 55], [5, 12, 58]\n  ];\n  const idx = v * (pal.length - 1), i = Math.min(idx | 0, pal.length - 2), f = idx - i;\n  const lum = 0.82 + Math.sin(rd1 * Math.PI) * 0.18;\n  const edge = Math.abs(rd1) > 0.65 ? 1.18 : (Math.abs(rd1) > 0.45 ? 1.08 : 1.0);\n  const shimmer = 0.97 + Math.sin(rd2 * 4 + t * 2) * 0.03;\n  const m = lum * edge * shimmer;\n  return [\n    Math.min(255, (pal[i][0] + (pal[i+1][0] - pal[i][0]) * f) * m) | 0,\n    Math.min(255, (pal[i][1] + (pal[i+1][1] - pal[i][1]) * f) * m) | 0,\n    Math.min(255, (pal[i][2] + (pal[i+1][2] - pal[i][2]) * f) * m) | 0\n  ];\n}",
  "score": {
    "coherence": 8,
    "harmony": 7,
    "complexity": 9,
    "density": 5,
    "total": 29,
    "reason": "The image displays high coherence with smooth, flowing gradients and no visual artifacts. Its color harmony is strong with a warm-cool contrast, though the intensity may reduce readability. Complexity is high due to intricate, mathematically inspired patterns. However, density is low — it's mostly abstract art with minimal functional UI information, making it unsuitable for a practical OS interface."
  },
  "timestamp": 1774504501915,
  "filename": "gen394_1774504501915"
}