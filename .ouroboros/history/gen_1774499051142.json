{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const w1x = Math.sin(x*0.018+t*0.3)*18 + Math.cos(y*0.013-t*0.22)*12;\n  const w1y = Math.cos(y*0.02-t*0.27)*16 + Math.sin(x*0.016+t*0.33)*11;\n  const w2x = Math.sin(x*0.035-t*0.5)*6 + Math.cos(y*0.04+t*0.45)*5;\n  const w2y = Math.cos(y*0.032+t*0.48)*7 + Math.sin(x*0.038-t*0.42)*4;\n  const sx = x + w1x + w2x, sy = y + w1y + w2y;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0, 1.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7, 0.9],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3, 1.1],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5, 0.8],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6, 1.05],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9, 0.95],\n    [200+Math.sin(t*0.25+3)*110, 40+Math.cos(t*0.65+4)*100, 0.055, 2.8, 1.15],\n    [400+Math.cos(t*0.45+1)*70, 200+Math.sin(t*0.35+2)*50, 0.13, 1.3, 0.85]\n  ];\n  let aS = 0, iS = 0, cross = 0;\n  for (let j = 0; j < src.length; j++) {\n    const [sX, sY, f, sp, wt] = src[j];\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    const a = Math.sin(d*f - t*sp);\n    const i = Math.sin(d*f*0.7 + t*sp*0.6 + 1.5);\n    aS += a * wt; iS += i * wt;\n    for (let k = j+1; k < src.length; k++) {\n      const [sX2, sY2, f2] = src[k];\n      const d2 = Math.sqrt((sx-sX2)**2 + (sy-sY2)**2);\n      cross += Math.sin((d*f + d2*f2)*0.5 - t*(sp + src[k][3])*0.25) * wt * src[k][4] * 0.15;\n    }\n  }\n  const n = src.length;\n  const act = aS/n, inh = iS/n;\n  const bg = (Math.sin(sx*0.04+sy*0.025+t*0.8) + Math.sin(sx*0.03-sy*0.04+t*1.1) + Math.sin(sx*0.05+sy*0.06-t*0.6))/3;\n  const fine = Math.sin(sx*0.15+sy*0.12-t*2.2)*Math.sin(sx*0.12-sy*0.14+t*1.8)*0.25;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2) + Math.sin(dist*0.08-ang*2+t*0.9))*0.3;\n  const ring = Math.sin(dist*0.12-t*1.8)*Math.exp(-dist*0.003)*0.4;\n  const feedback = Math.sin(act*3.14 + inh*2.1 + t*0.4)*0.15;\n  const raw = act - inh*0.65 + bg*0.3 + spiral + cross + ring + fine + feedback;\n  const reaction = Math.tanh(raw * 2.0);\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5 + Math.sin((sx-sy)*0.08-t*0.7)*0.3)*1.5)*0.25;\n  const edge = 1.0 - Math.abs(reaction)*0.08;\n  const glow = Math.max(0, Math.abs(reaction)-0.5)*2.0;\n  const blend = reaction*0.65 + cell*0.2 + glow*0.15;\n  const v = Math.max(0, Math.min(1, blend*0.5+0.5));\n  const pal = [\n    [5,10,45],[12,35,85],[18,75,115],[25,125,130],\n    [60,165,120],[140,190,80],[210,190,60],[225,150,55],\n    [215,105,65],[180,60,70],[120,35,65],[70,20,55],[30,12,48],[5,10,45]\n  ];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const sf = f*f*(3-2*f);\n  const lum = 0.85 + Math.sin(reaction*Math.PI)*0.15 + glow*0.1;\n  const shift = Math.sin(ang*0.5+t*0.2)*0.08;\n  return [\n    Math.min(255, Math.max(0, (pal[i][0]+(pal[i+1][0]-pal[i][0])*sf)*lum*(1+shift+glow*0.3)))|0,\n    Math.min(255, Math.max(0, (pal[i][1]+(pal[i+1][1]-pal[i][1])*sf)*lum*(1+glow*0.15)))|0,\n    Math.min(255, Math.max(0, (pal[i][2]+(pal[i+1][2]-pal[i][2])*sf)*lum*(1-shift+glow*0.2)))|0\n  ];\n}",
  "score": {
    "coherence": 7,
    "harmony": 6,
    "complexity": 9,
    "density": 4,
    "total": 26,
    "reason": "The image displays high complexity with intricate, flowing patterns resembling a heat map or topographical visualization. Coherence is good with clear gradients and boundaries, though some areas appear slightly noisy. Harmony is moderate due to the vibrant, contrasting colors which may be visually jarring. Density is low as there's no discernible UI or information structure, making it unsuitable for practical use in an OS interface."
  },
  "timestamp": 1774499051142,
  "filename": "gen237_1774499051142"
}