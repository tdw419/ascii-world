{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const w1x = Math.sin(x*0.02+t*0.3)*15+Math.cos(y*0.015-t*0.2)*10;\n  const w1y = Math.cos(y*0.02-t*0.25)*15+Math.sin(x*0.018+t*0.35)*10;\n  const w2x = Math.sin((x+w1x)*0.012-t*0.15)*8+Math.cos((y+w1y)*0.01+t*0.18)*6;\n  const w2y = Math.cos((y+w1y)*0.013+t*0.12)*8+Math.sin((x+w1x)*0.011-t*0.2)*6;\n  const sx = x+w1x+w2x, sy = y+w1y+w2y;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0, 1.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7, 0.8],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3, 1.2],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5, 0.9],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6, 1.1],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9, 0.7],\n    [80+Math.sin(t*0.45+3)*60, 40+Math.cos(t*0.65+2)*40, 0.14, 2.1, 0.6],\n    [400+Math.cos(t*0.55+1)*70, 200+Math.sin(t*0.75+4)*50, 0.055, 1.8, 1.3]\n  ];\n  let aS=0, iS=0, cross=0;\n  for(let i=0;i<src.length;i++){\n    const [sX,sY,f,sp,w]=src[i];\n    const d=Math.sqrt((sx-sX)**2+(sy-sY)**2);\n    const a=Math.sin(d*f-t*sp), n=Math.sin(d*f*0.7+t*sp*0.6+1.5);\n    aS+=a*w; iS+=n*w;\n    for(let j=i+1;j<src.length;j++){\n      const [sX2,sY2,f2,sp2,w2]=src[j];\n      const d2=Math.sqrt((sx-sX2)**2+(sy-sY2)**2);\n      cross+=Math.sin((d*f+d2*f2)*0.5-t*(sp+sp2)*0.5)*0.03*w*w2;\n    }\n  }\n  const act=aS/src.length, inh=iS/src.length;\n  const bg=(Math.sin(sx*0.04+sy*0.025+t*0.8)+Math.sin(sx*0.03-sy*0.04+t*1.1))/2+Math.sin((sx+sy)*0.12+t*2.5)*0.2+Math.sin((sx-sy)*0.1-t*2)*0.2;\n  const dist=Math.sqrt((sx-cx)**2+(sy-cy)**2), ang=Math.atan2(sy-cy,sx-cx);\n  const spiral=(Math.sin(dist*0.05-ang*3-t*1.5)+Math.sin(dist*0.04+ang*4-t*1.2)+Math.sin(dist*0.07-ang*2+t*0.8))*0.27;\n  const radial=Math.sin(dist*0.03-t*0.6)*0.3*Math.cos(ang*2+t*0.4);\n  const reaction=Math.tanh((act-inh*0.7+bg*0.3+spiral+cross+radial)*2.0);\n  const cell=Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15)+Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  const micro=Math.sin(sx*0.3+t*3)*Math.sin(sy*0.3-t*2.5)*0.08;\n  const blend=reaction*0.6+cell*0.25+micro*0.15, v=blend*0.5+0.5;\n  const hueShift=Math.sin(t*0.15)*0.12+Math.sin(t*0.23+1)*0.06;\n  const pal=[[5,12,52],[12,55,88],[18,105,115],[28,158,135],[85,178,120],[175,190,70],[210,165,55],[205,115,65],[175,65,72],[115,35,72],[65,22,62],[30,15,55],[5,12,52]];\n  const shifted=v+hueShift;\n  const si=((shifted%1)+1)%1;\n  const idx=si*(pal.length-1), pi=Math.min(idx|0,pal.length-2), pf=idx-pi;\n  const sm=pf*pf*(3-2*pf);\n  const lum=0.75+Math.sin(reaction*Math.PI)*0.25;\n  const edge=Math.abs(reaction)>0.65?1.0+Math.abs(reaction)*0.2:1.0;\n  const vig=1.0-Math.pow(dist/280,2.5)*0.3;\n  const glow=Math.max(0,reaction)*0.15;\n  return [\n    Math.min(255,((pal[pi][0]+(pal[pi+1][0]-pal[pi][0])*sm)*lum*edge*vig+glow*40)|0),\n    Math.min(255,((pal[pi][1]+(pal[pi+1][1]-pal[pi][1])*sm)*lum*edge*vig+glow*25)|0),\n    Math.min(255,((pal[pi][2]+(pal[pi+1][2]-pal[pi][2])*sm)*lum*edge*vig+glow*50)|0)\n  ];\n}",
  "score": {
    "coherence": 8,
    "harmony": 7,
    "complexity": 9,
    "density": 4,
    "total": 28,
    "reason": "The image has high coherence with smooth, flowing gradients and no visual artifacts. Its harmony is moderate, with a vibrant but somewhat chaotic color palette that may be visually fatiguing. Complexity is high due to the intricate, organic patterns resembling fluid dynamics or heat maps. However, density is low — it's purely decorative with no functional UI elements, making it unsuitable for a practical operating system interface."
  },
  "timestamp": 1774517563576,
  "filename": "gen747_1774517563576"
}