{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const wx = Math.sin(x*0.02+t*0.3)*15 + Math.cos(y*0.015-t*0.2)*10;\n  const wy = Math.cos(y*0.02-t*0.25)*15 + Math.sin(x*0.018+t*0.35)*10;\n  const sx = x + wx, sy = y + wy;\n  const primes = [2,3,5,7,11,13];\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.16, 2.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.10, 1.7],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.30, 2.3],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.84, 1.5],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.77, 2.6],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 1.17, 1.9]\n  ];\n  let aS = 0, iS = 0;\n  for (let j = 0; j < 6; j++) {\n    const [sX,sY,f,sp] = src[j];\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    const pf = f * (1 + 0.05*Math.sin(t*primes[j]*0.14));\n    aS += Math.sin(d*pf - t*sp);\n    iS += Math.sin(d*pf*0.7 + t*sp*0.6 + 1.5);\n  }\n  const act = aS/6, inh = iS/6;\n  const pGrid = Math.sin(sx*0.0314159*2)*Math.sin(sy*0.0314159*3)*Math.sin((sx+sy)*0.0314159*5)*0.25;\n  const bg = (Math.sin(sx*0.04+sy*0.025+t*0.8) + Math.sin(sx*0.03-sy*0.04+t*1.1))/2 + Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2 + pGrid;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2))*0.4;\n  const saccade = 1.0 + 0.14*Math.sin(t*1.55);\n  const reaction = Math.tanh((act - inh*0.7 + bg*0.3 + spiral)*2.2*saccade);\n  const gx = sx%48, gy = sy%24;\n  const uiGrid = (gx<1||gx>47?0.35:0) + (gy<0.8||gy>23.2?0.25:0);\n  const statusBar = y<11 ? 0.25+0.12*Math.sin(x*0.05+t*2) : 0;\n  const sideBar = x<8 ? 0.2+0.1*Math.sin(y*0.08+t*1.5) : 0;\n  const textLine = Math.sin(sy*2.618)*Math.sin(sx*0.5+t*0.3)>0.88 ? 0.18*Math.sin(sx*0.9+t) : 0;\n  const win1 = sx>18&&sx<158&&sy>18&&sy<108;\n  const wb1 = win1&&(sx<20||sx>156||sy<20||sy>106)?0.55:0;\n  const win2 = sx>318&&sx<468&&sy>28&&sy<108;\n  const wb2 = win2&&(sx<320||sx>466||sy<30||sy>106)?0.5:0;\n  const win3 = sx>100&&sx<380&&sy>140&&sy<230;\n  const wb3 = win3&&(sx<102||sx>378||sy<142||sy>228)?0.45:0;\n  const barH = 6;\n  const bar1 = y>24&&y<24+barH&&x>22&&x<22+(Math.sin(t*0.7)*0.5+0.5)*130 ? 0.3 : 0;\n  const bar2 = y>34&&y<34+barH&&x>22&&x<22+(Math.cos(t*0.5)*0.5+0.5)*130 ? 0.25 : 0;\n  const bar3 = y>44&&y<44+barH&&x>22&&x<22+(Math.sin(t*0.9+1)*0.5+0.5)*130 ? 0.2 : 0;\n  const miniGraph = win3&&y>150&&y<220&&x>105&&x<375;\n  let mg = 0;\n  if (miniGraph) mg = Math.abs(Math.sin(x*0.08+t*1.2)*30 + Math.sin(x*0.15-t*0.8)*15 - (y-185)) < 2 ? 0.4 : 0;\n  const hexDist = Math.sqrt((sx%32-16)**2 + ((sy%28+14*Math.abs(((sx/32|0)%2))-14))**2);\n  const hexGrid = hexDist<0.8 ? 0.12 : 0;\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  const blend = reaction*0.7 + cell*0.2 + uiGrid*0.12 + statusBar + sideBar + textLine + wb1 + wb2 + wb3 + bar1 + bar2 + bar3 + mg + hexGrid;\n  const v = Math.max(0, Math.min(1, blend*0.5+0.5));\n  const pal = [[4,2,18],[8,0,45],[0,30,70],[0,120,140],[0,220,180],[40,255,120],[220,255,40],[255,90,40],[255,30,90],[160,0,220],[60,10,80],[4,2,18]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const lum = 0.65+Math.sin(reaction*Math.PI)*0.35;\n  const edge = Math.abs(reaction)>0.65?1.5:1.0;\n  const neon = v>0.3&&v<0.8?1.35:1.0;\n  const glow = (wb1+wb2+wb3+statusBar)>0.1?1.2:1.0;\n  const m = lum*edge*neon*glow;\n  return [\n    Math.min(255, (pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*m)|0,\n    Math.min(255, (pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*m)|0,\n    Math.min(255, (pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*m)|0\n  ];\n}",
  "score": {
    "coherence": 3,
    "harmony": 4,
    "complexity": 8,
    "density": 5,
    "total": 20,
    "reason": "The image has high visual complexity with intricate patterns but suffers from low coherence due to chaotic, fragmented shapes and artifacts. Color harmony is moderate with high-contrast neon tones, but the arrangement feels jarring. Density is low for a UI, as there's little discernible information or structure, though the patterns are mathematically interesting."
  },
  "timestamp": 1774602861405,
  "filename": "gen1493_1774602861405"
}