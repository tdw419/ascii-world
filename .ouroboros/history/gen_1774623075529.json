{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120, P = [2,3,5,7,11,13];\n  const wx = Math.sin(x*0.02+t*0.3)*15 + Math.cos(y*0.015-t*0.2)*10;\n  const wy = Math.cos(y*0.02-t*0.25)*15 + Math.sin(x*0.018+t*0.35)*10;\n  const sx = x + wx, sy = y + wy;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08*P[0], 2.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1*P[1]/3, 1.7],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06*P[2]/5, 2.3],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12*P[3]/7, 1.5],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07*P[4]/11, 2.6],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09*P[5]/13, 1.9]\n  ];\n  let aS = 0, iS = 0;\n  for (const [sX, sY, f, sp] of src) {\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    aS += Math.sin(d*f - t*sp);\n    iS += Math.sin(d*f*0.7 + t*sp*0.6 + 1.5);\n  }\n  const act = aS/6, inh = iS/6;\n  let bg = 0;\n  for (let k = 0; k < 6; k++) bg += Math.sin(sx*0.04*P[k]/7 + sy*0.025*P[k]/5 + t*0.8*P[k]/13)/P[k];\n  bg += Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2))*0.4;\n  const reaction = Math.tanh((act - inh*0.7 + bg*0.3 + spiral)*2.2);\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  const gX = Math.abs(Math.sin(x*Math.PI/60)) < 0.025 ? 0.12 : 0;\n  const gY = Math.abs(Math.sin(y*Math.PI/30)) < 0.025 ? 0.12 : 0;\n  const barFill = (Math.sin(t*0.5)*0.5+0.5)*220;\n  const sBar = y < 7 ? (x < 120+barFill ? 0.35 : 0.04) : 0;\n  const sLabel = y < 7 && x > 350 && x < 440 ? 0.25 : 0;\n  const m = 10, fw = 2;\n  const frame = ((x<m+fw&&x>m)||(x>469-fw&&x<469)||(y<m+fw&&y>m)||(y>229-fw&&y<229)) ? 0.18 : 0;\n  const titleBg = y>=10&&y<22&&x>=14&&x<180 ? 0.1 : 0;\n  const titleLine = y>=14&&y<16&&x>=20&&x<100+Math.sin(t*0.7)*30 ? 0.2 : 0;\n  let dBars = 0;\n  if (x>=455&&x<472) {\n    const bi = Math.floor(y/16), phase = Math.sin(t*P[bi%6]*0.3+bi*1.1)*0.5+0.5;\n    dBars = (y%16) < phase*13 ? 0.3*phase : 0.02;\n  }\n  let tokens = 0;\n  for (let k = 0; k < 10; k++) {\n    const tx = 25+k*45+Math.sin(t*0.3+k*1.7)*8, ty = 215+Math.cos(t*0.4+k*2.1)*5;\n    const td = Math.sqrt((x-tx)**2+(y-ty)**2);\n    if (td < 2.5) tokens = 0.35+Math.sin(t*P[k%6])*0.1;\n  }\n  const scanLine = Math.abs(Math.sin(y*0.5+t*3)) > 0.98 ? 0.06 : 0;\n  const blend = reaction*0.55 + cell*0.15 + (gX+gY)*0.08 + sBar*0.28 + sLabel*0.15 + frame*0.25 + titleBg*0.12 + titleLine*0.18 + dBars*0.2 + tokens*0.28 + scanLine*0.08;\n  const v = Math.tanh(blend*1.8)*0.5+0.5;\n  const pal = [[2,4,18],[4,15,55],[0,70,110],[0,220,160],[80,255,40],[255,240,0],[255,90,0],[255,0,70],[160,0,100],[60,0,45],[2,4,18]];\n  const idx = v*(pal.length-1), pi = Math.min(idx|0, pal.length-2), pf = idx-pi;\n  const lum = 0.65+Math.abs(reaction)*0.55;\n  const neon = Math.abs(reaction)>0.55 ? 1.45 : 1.0;\n  const uiGlow = (sBar+dBars+tokens+frame+titleLine)>0.1 ? 1.15 : 1.0;\n  const m2 = lum*neon*uiGlow;\n  return [\n    Math.min(255, (pal[pi][0]+(pal[pi+1][0]-pal[pi][0])*pf)*m2)|0,\n    Math.min(255, (pal[pi][1]+(pal[pi+1][1]-pal[pi][1])*pf)*m2)|0,\n    Math.min(255, (pal[pi][2]+(pal[pi+1][2]-pal[pi][2])*pf)*m2)|0\n  ];\n}",
  "score": {
    "coherence": 7,
    "harmony": 6,
    "complexity": 9,
    "density": 4,
    "total": 26,
    "reason": "The image displays high complexity with intricate, flowing patterns and vibrant color transitions, but lacks coherence in structure and harmony due to its chaotic, non-uniform color palette. The density is low for a UI, as it conveys no functional information or layout, making it visually rich but practically useless for an operating system interface."
  },
  "timestamp": 1774623075528,
  "filename": "gen1864_1774623075528"
}