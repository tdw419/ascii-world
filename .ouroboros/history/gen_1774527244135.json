{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const w1x = Math.sin(x*0.018+t*0.25)*18 + Math.cos(y*0.013-t*0.18)*12 + Math.sin((x+y)*0.009+t*0.15)*8;\n  const w1y = Math.cos(y*0.02-t*0.22)*16 + Math.sin(x*0.016+t*0.28)*11 + Math.cos((x-y)*0.011-t*0.12)*7;\n  const w2x = Math.sin(y*0.025+t*0.4)*6 + Math.cos(x*0.03-t*0.35)*4;\n  const w2y = Math.cos(x*0.022+t*0.38)*5 + Math.sin(y*0.028-t*0.42)*4;\n  const sx = x+w1x+w2x, sy = y+w1y+w2y;\n  const src = [\n    [160+Math.sin(t*0.5)*110, 80+Math.cos(t*0.35)*55, 0.075, 2.1, 1.0],\n    [320+Math.cos(t*0.45)*100, 160+Math.sin(t*0.6)*55, 0.095, 1.8, 0.9],\n    [240+Math.sin(t*0.28+2)*150, 120+Math.cos(t*0.42+1)*95, 0.055, 2.4, 1.1],\n    [100+Math.cos(t*0.7+1)*90, 190+Math.sin(t*0.55+3)*65, 0.11, 1.6, 0.85],\n    [380+Math.sin(t*0.38+4)*95, 55+Math.cos(t*0.8+2)*75, 0.065, 2.7, 1.05],\n    [240+Math.cos(t*0.32+5)*130, 210+Math.sin(t*0.48)*85, 0.085, 2.0, 0.95],\n    [60+Math.sin(t*0.6+3)*70, 120+Math.cos(t*0.5+1)*90, 0.1, 1.9, 0.88],\n    [420+Math.cos(t*0.42+2)*80, 130+Math.sin(t*0.65+4)*60, 0.07, 2.5, 1.02]\n  ];\n  let aS=0, iS=0, aS2=0;\n  for (const [sX,sY,f,sp,w] of src) {\n    const d = Math.sqrt((sx-sX)**2+(sy-sY)**2);\n    const wave = Math.sin(d*f - t*sp);\n    const wave2 = Math.sin(d*f*1.618 - t*sp*0.78 + 0.5);\n    aS += wave * w;\n    iS += Math.sin(d*f*0.7+t*sp*0.55+1.5) * w;\n    aS2 += wave2 * w * 0.5;\n  }\n  const n = 8;\n  const act = aS/n, inh = iS/n, act2 = aS2/n;\n  const bg1 = (Math.sin(sx*0.035+sy*0.022+t*0.7)+Math.sin(sx*0.028-sy*0.035+t*0.95))/2;\n  const bg2 = Math.sin((sx+sy)*0.1+t*2.2)*0.25 + Math.sin((sx-sy)*0.085-t*1.8)*0.25;\n  const bg3 = Math.sin(sx*0.06+t*1.5)*Math.cos(sy*0.05-t*1.2)*0.15;\n  const bg = bg1+bg2+bg3;\n  const dist = Math.sqrt((sx-cx)**2+(sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral1 = (Math.sin(dist*0.045-ang*3.5-t*1.3)+Math.sin(dist*0.035+ang*4.5-t*1.0))*0.35;\n  const spiral2 = Math.sin(dist*0.08-ang*2-t*2.5)*0.15;\n  const spiral = spiral1+spiral2;\n  const rd1 = act - inh*0.65 + bg*0.25 + spiral;\n  const rd2 = act2*0.4 - inh*0.3 + Math.sin(act*2.5+t)*0.2;\n  const rd = rd1 + rd2*0.3;\n  const reaction = Math.tanh(rd*2.0);\n  const cell = Math.tanh((Math.sin(sx*0.14)*Math.sin(sy*0.14)+Math.sin((sx+sy)*0.095+t)*0.45+Math.sin(sx*0.08-t*0.7)*Math.cos(sy*0.12+t*0.5)*0.3)*1.4)*0.25;\n  const micro = Math.sin(sx*0.3+sy*0.25+t*3)*Math.cos(sx*0.2-sy*0.35-t*2.5)*0.08;\n  const blend = reaction*0.7+cell*0.22+micro;\n  const v = blend*0.5+0.5;\n  const glow = Math.max(0, reaction-0.5)*2;\n  const pal = [[6,12,52],[10,45,85],[12,85,105],[18,140,130],[55,175,125],[160,190,80],[210,175,60],[215,130,55],[185,80,60],[130,40,65],[70,18,55],[30,10,45],[6,12,52]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const pulse = 0.85+Math.sin(t*0.8)*0.08+Math.sin(t*1.3+1)*0.05;\n  const edge = Math.abs(reaction)>0.65?1.12:1.0;\n  const glowMul = 1+glow*0.35;\n  const lum = pulse*edge*glowMul;\n  const r = (pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum;\n  const g = (pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum;\n  const b = (pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum;\n  const gr = glow*25, gg = glow*40, gb = glow*20;\n  return [Math.min(255,(r+gr)|0), Math.min(255,(g+gg)|0), Math.min(255,(b+gb)|0)];\n}",
  "score": {
    "coherence": 7,
    "harmony": 6,
    "complexity": 9,
    "density": 4,
    "total": 26,
    "reason": "The image displays coherent, swirling patterns with smooth color transitions, indicating good visual integrity. The color harmony is moderate — vibrant but somewhat chaotic due to the high contrast between warm and cool tones. Complexity is high due to the intricate, mathematically generated fractal-like swirls. However, density is low for a UI, as there's minimal information or functional content; it's purely decorative. This makes it visually striking but impractical for a minimal OS interface."
  },
  "timestamp": 1774527244135,
  "filename": "gen27_1774527244135"
}