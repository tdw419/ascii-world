{
  "code": "(x, y, t) => {\n  const masses = [\n    [120+Math.sin(t*0.3)*80, 80+Math.cos(t*0.4)*60, 40, 0.1, 2.2],\n    [360+Math.cos(t*0.25)*70, 160+Math.sin(t*0.35)*80, 35, 0.12, 1.8],\n    [240+Math.sin(t*0.28+2)*130, 120+Math.cos(t*0.45+1)*80, 45, 0.07, 2.5],\n    [80+Math.cos(t*0.7+1)*60, 200+Math.sin(t*0.5+3)*55, 30, 0.14, 1.6],\n    [400+Math.sin(t*0.38+4)*70, 50+Math.cos(t*0.5+2)*60, 38, 0.08, 2.8],\n    [240+Math.cos(t*0.3+5)*110, 200+Math.sin(t*0.48)*75, 42, 0.11, 2.0],\n    [180+Math.sin(t*0.22+3)*100, 40+Math.cos(t*0.38)*50, 28, 0.13, 1.4],\n    [320+Math.cos(t*0.32+2)*85, 220+Math.sin(t*0.37+1)*55, 33, 0.09, 2.3]\n  ];\n  let gx = 0, gy = 0, gPot = 0, act = 0, inh = 0, minD = 999, mSpiral = 0;\n  for (let i = 0; i < 8; i++) {\n    const [mx, my, m, f, sp] = masses[i];\n    const dx = mx - x, dy = my - y, d2 = dx*dx + dy*dy + 200, d = Math.sqrt(d2);\n    gx += dx * m / d2 * 600; gy += dy * m / d2 * 600; gPot += m / d;\n    const sd = Math.sqrt((x+gx*0.25-mx)**2 + (y+gy*0.25-my)**2);\n    act += Math.sin(sd*f - t*sp); inh += Math.sin(sd*f*0.65 + t*sp*0.55 + 1.8);\n    minD = Math.min(minD, sd);\n    if (i < 4) { const md = Math.sqrt((x+gx*0.25-mx)**2 + (y+gy*0.25-my)**2); const ma = Math.atan2(y+gy*0.25-my, x+gx*0.25-mx); mSpiral += Math.sin(md*0.08 - ma*3 - t*2 + i*1.5) * 0.2; }\n  }\n  const wx = Math.sin(x*0.018+t*0.25)*10 + Math.cos(y*0.022-t*0.18)*8 + Math.sin((x+y)*0.01+t*0.15)*6;\n  const wy = Math.cos(y*0.02-t*0.22)*10 + Math.sin(x*0.016+t*0.3)*8 + Math.cos((x-y)*0.012-t*0.2)*6;\n  const sx = x + wx + gx*0.25, sy = y + wy + gy*0.25;\n  act /= 8; inh /= 8;\n  const kinetic = (act*act + inh*inh)*0.5, potential = gPot*0.12;\n  const totalE = Math.sqrt(kinetic + potential);\n  const bg = (Math.sin(sx*0.05+sy*0.03+t*0.9) + Math.sin(sx*0.035-sy*0.05+t*1.3))/2 + Math.sin((sx+sy)*0.15+t*3)*0.15 + Math.sin((sx-sy)*0.12-t*2.5)*0.15;\n  const dist = Math.sqrt((sx-240)**2 + (sy-120)**2), ang = Math.atan2(sy-120, sx-240);\n  const spiral = (Math.sin(dist*0.06-ang*4-t*1.8) + Math.sin(dist*0.045+ang*5-t*1.4))*0.35;\n  const reaction = Math.tanh((act - inh*0.6 + bg*0.2 + spiral + mSpiral)*3.2);\n  const cell = Math.tanh(Math.sin(minD*0.12)*2.0)*0.2;\n  const eMod = Math.tanh(totalE*0.7);\n  const v = (reaction*0.7 + cell*0.2 + eMod*0.1)*0.5+0.5;\n  const pal = [[2,2,12],[5,15,50],[20,5,80],[90,0,140],[200,0,200],[255,20,100],[255,120,40],[255,220,30],[80,255,100],[0,255,230],[30,160,255],[2,2,12]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const sharp = Math.pow(Math.abs(reaction), 0.5);\n  const lum = (0.5 + sharp*0.9) * (Math.abs(reaction)>0.55 ? 1.0+(Math.abs(reaction)-0.55)*1.8 : 1.0) * (1.0+eMod*0.35);\n  return [Math.min(255,(pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum)|0, Math.min(255,(pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum)|0, Math.min(255,(pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum)|0];\n}",
  "score": {
    "coherence": 7,
    "harmony": 5,
    "complexity": 9,
    "density": 3,
    "total": 24,
    "reason": "The image displays high mathematical complexity with fractal-like patterns and smooth gradients, but lacks visual coherence due to chaotic color transitions. The color harmony is poor with overly saturated, clashing hues (cyan, magenta, yellow) that reduce readability. The density is very low for a UI, as it conveys no functional information, making it purely decorative."
  },
  "timestamp": 1774536825015,
  "filename": "gen53_1774536825015"
}