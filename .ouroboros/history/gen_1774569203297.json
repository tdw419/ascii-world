{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const primes = [2, 3, 5, 7, 11, 13, 17, 19];\n  const wx = Math.sin(x*0.02+t*0.3)*15 + Math.cos(y*0.015-t*0.2)*10 + Math.sin((x+y)*0.01+t*0.5)*5;\n  const wy = Math.cos(y*0.02-t*0.25)*15 + Math.sin(x*0.018+t*0.35)*10 + Math.cos((x-y)*0.012-t*0.4)*5;\n  const sx = x + wx, sy = y + wy;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08*primes[0], 2.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.03*primes[1], 1.7],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.02*primes[2], 2.3],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.02*primes[3], 1.5],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.01*primes[4], 2.6],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.007*primes[5], 1.9]\n  ];\n  let aS = 0, iS = 0;\n  for (const [sX, sY, f, sp] of src) {\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    aS += Math.sin(d*f - t*sp);\n    iS += Math.sin(d*f*0.7 + t*sp*0.6 + 1.5);\n  }\n  const act = aS/6, inh = iS/6;\n  const bg = (Math.sin(sx*0.04+sy*0.025+t*0.8) + Math.sin(sx*0.03-sy*0.04+t*1.1))/2 + Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2))*0.4;\n  const reaction = Math.tanh((act - inh*0.7 + bg*0.3 + spiral)*2.2);\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  let blend = reaction*0.75 + cell*0.25;\n  const layerIdx = Math.min((y/30)|0, 7);\n  const layerMod = Math.sin(layerIdx*primes[layerIdx]*0.5+t)*0.12;\n  blend += layerMod;\n  const gx = Math.abs(Math.sin(sx*Math.PI/40)) < 0.015 ? 0.2 : 0;\n  const gy = Math.abs(Math.sin(sy*Math.PI/20)) < 0.015 ? 0.12 : 0;\n  blend += gx + gy;\n  const inWin = x>25&&x<195&&y>35&&y<145;\n  const wBrd = inWin?(x<28||x>192||y<38||y>142?0.35:0):0;\n  blend += wBrd;\n  const tLine = inWin&&y>50&&y<138?(Math.sin(x*1.3+((y-50)/7|0)*4.1+t*0.2)>0.35?0.1:0):0;\n  blend += tLine;\n  const vizR = x>385&&x<472&&y>28&&y<215;\n  let vB = 0;\n  if (vizR) { const bI=((y-28)/10)|0; const bH=Math.sin(bI*primes[bI%8]*0.6+t*1.2)*0.5+0.5; vB=(x-385)<bH*75?bH*0.25:0; }\n  blend += vB;\n  const barT = y>1&&y<7, barB = y>233&&y<238;\n  if (barT||barB) {\n    const fill = Math.sin(x*0.04+t*2)*0.5+0.5;\n    const p = Math.floor(x/60)%8;\n    const glow = fill > 0.4+primes[p]*0.02 ? 1 : 0;\n    return glow ? [0,255,220] : [15,15,40];\n  }\n  const primeViz = x>210&&x<370&&y>150&&y<230;\n  let pV = 0;\n  if (primeViz) {\n    const col = ((x-210)/20)|0;\n    const row = ((y-150)/10)|0;\n    const isPrime = primes.includes(col*row+2) ? 1 : 0;\n    pV = isPrime ? 0.2 : 0;\n    const pulse = Math.sin(t*3+col*0.5+row*0.7)*0.5+0.5;\n    pV *= pulse;\n  }\n  blend += pV;\n  let v = blend*0.5+0.5;\n  const pal = [[4,2,18],[8,10,60],[15,0,90],[50,0,160],[130,0,255],[220,0,200],[255,20,80],[255,100,0],[255,220,20],[20,255,180],[0,180,255],[4,2,18]];\n  const idx = Math.max(0, Math.min(v*(pal.length-1), pal.length-1.001));\n  const i = idx|0, f = idx-i;\n  const lum = 0.85+Math.sin(reaction*Math.PI)*0.15;\n  const edge = Math.abs(reaction)>0.65?1.35:1.0;\n  const neon = Math.abs(reaction)>0.8?1.2:1.0;\n  return [\n    Math.min(255, (pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*edge*neon)|0,\n    Math.min(255, (pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*edge*neon)|0,\n    Math.min(255, (pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*edge*neon)|0\n  ];\n}",
  "score": {
    "coherence": 1,
    "harmony": 1,
    "complexity": 1,
    "density": 1,
    "total": 4,
    "reason": "Error: Vision API error: 400 Bad Request"
  },
  "timestamp": 1774569203296,
  "filename": "gen843_1774569203296"
}