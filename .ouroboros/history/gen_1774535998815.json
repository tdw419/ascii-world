{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const wx = Math.sin(x*0.018+t*0.25)*20 + Math.cos(y*0.012-t*0.18)*14 + Math.sin((x+y)*0.008+t*0.4)*8;\n  const wy = Math.cos(y*0.02-t*0.22)*18 + Math.sin(x*0.015+t*0.3)*12 + Math.cos((x-y)*0.009-t*0.35)*7;\n  const sx = x + wx, sy = y + wy;\n  const wells = [\n    [160+Math.sin(t*0.4)*120, 80+Math.cos(t*0.3)*60, 80+Math.sin(t*0.7)*40, 0.09, 2.2],\n    [320+Math.cos(t*0.35)*100, 160+Math.sin(t*0.5)*70, 70+Math.cos(t*0.6)*30, 0.11, 1.8],\n    [240+Math.sin(t*0.25+2)*150, 120+Math.cos(t*0.4+1)*100, 90+Math.sin(t*0.5)*35, 0.07, 2.5],\n    [100+Math.cos(t*0.6+1)*90, 190+Math.sin(t*0.45+3)*70, 60+Math.cos(t*0.8)*25, 0.13, 1.6],\n    [380+Math.sin(t*0.3+4)*80, 50+Math.cos(t*0.7+2)*80, 75+Math.sin(t*0.55)*30, 0.08, 2.8],\n    [200+Math.cos(t*0.28+5)*110, 210+Math.sin(t*0.42)*90, 85+Math.cos(t*0.65)*35, 0.1, 2.0],\n    [280+Math.sin(t*0.38+3)*70, 40+Math.cos(t*0.52+4)*50, 55+Math.sin(t*0.75)*20, 0.14, 1.4],\n    [60+Math.cos(t*0.45+2)*60, 120+Math.sin(t*0.55+1)*80, 65+Math.cos(t*0.7)*25, 0.12, 2.1]\n  ];\n  let act = 0, inh = 0, pot = 0;\n  for (const [wX, wY, mass, f, sp] of wells) {\n    const d = Math.sqrt((sx-wX)**2 + (sy-wY)**2);\n    const grav = mass / (d + 20);\n    pot += grav;\n    const wave = Math.sin(d*f - t*sp);\n    const kinetic = wave * grav * 0.3;\n    act += wave + kinetic;\n    inh += Math.sin(d*f*0.6 + t*sp*0.5 + 1.8) * grav * 0.4;\n  }\n  act /= 8; inh /= 8; pot /= 8;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const cascade = Math.sin(pot*0.15 - dist*0.03 + t*0.8) * Math.cos(ang*2 - t*0.3);\n  const spiral = (Math.sin(dist*0.04-ang*4-t*1.8) + Math.sin(dist*0.06+ang*3-t*1.3))*0.35;\n  const vortex = Math.sin(dist*0.08 - t*2.0 + ang*6) * Math.exp(-dist*0.005) * 0.5;\n  const turb = (Math.sin(sx*0.035+sy*0.022+t*0.9) + Math.sin(sx*0.028-sy*0.038+t*1.3) + Math.sin((sx+sy)*0.015+t*1.8))/3;\n  const cell = Math.sin(sx*0.12)*Math.sin(sy*0.12) + Math.sin((sx-sy)*0.08+t*0.7)*0.6 + Math.sin(sx*0.06+sy*0.09-t*0.5)*0.4;\n  const raw = act - inh*0.65 + cascade*0.4 + spiral*0.3 + vortex + turb*0.25 + cell*0.15;\n  const rd1 = Math.tanh(raw * 2.8);\n  const rd2 = Math.tanh((raw - 0.3) * 3.5 + Math.sin(sx*0.08+t*1.2)*0.5);\n  const reaction = Math.tanh((rd1*0.6 + rd2*0.4) * 2.5);\n  const glow = Math.max(0, reaction - 0.3) * 2.5;\n  const edge = Math.exp(-Math.pow((Math.abs(reaction) - 0.85)*8, 2)) * 1.8;\n  const v = reaction*0.5+0.5;\n  const pal = [[2,2,12],[5,10,35],[8,30,60],[0,80,120],[0,180,200],[20,255,200],[180,255,100],[255,220,50],[255,140,30],[255,60,80],[220,20,120],[120,10,80],[40,5,30],[2,2,12]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  let r = pal[i][0]+(pal[i+1][0]-pal[i][0])*f;\n  let g = pal[i][1]+(pal[i+1][1]-pal[i][1])*f;\n  let b = pal[i][2]+(pal[i+1][2]-pal[i][2])*f;\n  const neonBoost = glow * 0.6;\n  r += neonBoost * 0.3; g += neonBoost * 0.8; b += neonBoost * 0.5;\n  r += edge * 0.4; g += edge * 0.7; b += edge * 0.5;\n  const contrast = 1.0 + Math.abs(reaction)*0.4 + edge*0.5;\n  r *= contrast; g *= contrast; b *= contrast;\n  return [Math.min(255, Math.max(0, r))|0, Math.min(255, Math.max(0, g))|0, Math.min(255, Math.max(0, b))|0];\n}",
  "score": {
    "coherence": 9,
    "harmony": 8,
    "complexity": 10,
    "density": 6,
    "total": 33,
    "reason": "The image has high coherence with smooth, flowing organic shapes and no visual artifacts. Its harmony is strong with vibrant, contrasting neon colors against a dark background, creating an aesthetically pleasing and readable display. Complexity is maximal due to the intricate, mathematically generated patterns. However, density is low as the image is mostly decorative with minimal functional UI elements, making it less useful for conveying information in a minimal OS context."
  },
  "timestamp": 1774535998815,
  "filename": "gen37_1774535998815"
}