{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const wx = Math.sin(x * 0.02 + t * 0.3) * 12 + Math.cos(y * 0.015 - t * 0.2) * 8;\n  const wy = Math.cos(y * 0.02 - t * 0.25) * 12 + Math.sin(x * 0.018 + t * 0.35) * 8;\n  const sx = x + wx, sy = y + wy;\n  const primes = [2, 3, 5, 7, 11, 13, 17, 19];\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08*primes[0], 2.0*primes[1]/3],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1*primes[2]/5, 1.7*primes[3]/7],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06*primes[4]/11, 2.3*primes[5]/13],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12*primes[6]/17, 1.5*primes[7]/19],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07*primes[0]*primes[1]/6, 2.6],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09*primes[2]*primes[3]/35, 1.9]\n  ];\n  let aS = 0, iS = 0;\n  for (const [sX, sY, f, sp] of src) {\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    aS += Math.sin(d*f - t*sp);\n    iS += Math.sin(d*f*0.7 + t*sp*0.6 + 1.5);\n  }\n  const act = aS/6, inh = iS/6;\n  const bg = (Math.sin(sx*0.04+sy*0.025+t*0.8) + Math.sin(sx*0.03-sy*0.04+t*1.1))/2 + Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2))*0.4;\n  const reaction = Math.tanh((act - inh*0.7 + bg*0.3 + spiral)*2.2);\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  const blend = reaction*0.75 + cell*0.25, v = blend*0.5+0.5;\n  const pal = [[4,8,32],[0,40,80],[0,180,160],[0,255,200],[180,255,100],[255,200,0],[255,80,120],[180,0,80],[60,0,40],[4,8,32]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const lum = 0.85+Math.sin(reaction*Math.PI)*0.3, edge = Math.abs(reaction)>0.65?1.3:1.0;\n  let r = (pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*edge;\n  let g = (pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*edge;\n  let b = (pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*edge;\n  const gridX = Math.abs(((sx*0.5)%primes[2]) - primes[2]*0.5), gridY = Math.abs(((sy*0.5)%primes[3]) - primes[3]*0.5);\n  const grid = Math.max(0, 1 - Math.min(gridX, gridY)*0.8)*0.15;\n  r += grid*0, g += grid*180, b += grid*255;\n  const scanY = (y%primes[4])<1 ? 0.12 : 0;\n  const hBar = (y<3||y>236)?1:(y<6||y>233)?0.5:0;\n  const vBar = (x<2||x>477)?1:(x<4||x>475)?0.5:0;\n  const frame = Math.max(hBar, vBar);\n  const winL = x>18&&x<155&&y>14&&y<95, winR = x>325&&x<462&&y>14&&y<95;\n  const winBL = x>18&&x<155&&y>145&&y<226, winBR = x>325&&x<462&&y>145&&y<226;\n  const winC = x>165&&x<315&&y>50&&y<190;\n  const isWin = winL||winR||winBL||winBR||winC;\n  const wBorder = isWin ? (Math.min(x-18,155-x,y-14,95-y,325-x,x-462,y-145,226-y,165-x,315-x,y-50,190-y)<2?0.9:0) : 0;\n  const wTitle = isWin ? (y<20||((winL||winR)&&y<19)||((winBL||winBR)&&y<151)||(winC&&y<55)?0.3:0) : 0;\n  const uiMask = frame*0.6 + wBorder*0.7 + wTitle*0.15;\n  r = r*(1-uiMask) + uiMask*20, g = g*(1-uiMask) + uiMask*220, b = b*(1-uiMask) + uiMask*255;\n  const primePulse = Math.sin(t*primes[1]*0.5)*0.5+0.5;\n  const neonGlow = Math.max(0, Math.abs(reaction)-0.5)*2;\n  r += neonGlow*40*primePulse, g += neonGlow*80*(1-primePulse), b += neonGlow*60;\n  r += scanY*40, g += scanY*60, b += scanY*80;\n  return [Math.min(255,r)|0, Math.min(255,g)|0, Math.min(255,b)|0];\n}",
  "score": {
    "coherence": 1,
    "harmony": 1,
    "complexity": 1,
    "density": 1,
    "total": 4,
    "reason": "Error: Vision API error: 400 Bad Request"
  },
  "timestamp": 1774567178502,
  "filename": "gen781_1774567178502"
}