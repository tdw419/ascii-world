{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const w1x = Math.sin(x*0.018+t*0.3)*18 + Math.cos(y*0.013-t*0.22)*12;\n  const w1y = Math.cos(y*0.02-t*0.28)*18 + Math.sin(x*0.016+t*0.33)*12;\n  const mx = x+w1x, my = y+w1y;\n  const w2x = Math.sin(mx*0.025-t*0.5)*8 + Math.cos(my*0.03+t*0.4)*6;\n  const w2y = Math.cos(my*0.022+t*0.45)*8 + Math.sin(mx*0.028-t*0.55)*6;\n  const sx = mx+w2x, sy = my+w2y;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0, 1.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7, 0.9],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3, 1.1],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5, 0.85],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6, 1.05],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9, 0.95],\n    [80+Math.sin(t*0.45+3)*70, 40+Math.cos(t*0.65+1)*40, 0.14, 2.1, 0.8],\n    [400+Math.cos(t*0.55+2)*60, 200+Math.sin(t*0.75+4)*50, 0.11, 1.8, 1.15],\n    [200+Math.sin(t*0.25+1)*110, 220+Math.cos(t*0.45+3)*60, 0.055, 2.4, 1.0],\n    [300+Math.cos(t*0.7+3)*80, 100+Math.sin(t*0.5+2)*70, 0.095, 2.0, 0.9]\n  ];\n  let aS = 0, iS = 0, hS = 0;\n  for (const [sX, sY, f, sp, wt] of src) {\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    aS += Math.sin(d*f - t*sp) * wt;\n    iS += Math.sin(d*f*0.7 + t*sp*0.6 + 1.5) * wt;\n    hS += Math.sin(d*f*1.3 - t*sp*0.8 + 3.0) * wt;\n  }\n  const n = src.length, act = aS/n, inh = iS/n, harm = hS/n;\n  const bg = (Math.sin(sx*0.04+sy*0.025+t*0.8) + Math.sin(sx*0.03-sy*0.04+t*1.1) + Math.sin(sx*0.07+sy*0.05-t*1.5)*0.5 + Math.sin(sx*0.11-sy*0.09+t*2.2)*0.3)/3 + Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2) + Math.sin(dist*0.08-ang*2+t*0.9)*0.4)*0.35;\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5 + Math.sin(sx*0.22-sy*0.18+t*1.3)*0.3)*1.5)*0.25;\n  const grain = Math.sin(sx*0.35+t*3)*Math.sin(sy*0.35-t*2.5)*0.08;\n  const breath = Math.sin(dist*0.015-t*0.6)*0.15;\n  const raw = act - inh*0.7 + bg*0.3 + spiral + harm*0.15 + cell + grain + breath;\n  const reaction = Math.tanh(raw*2.0);\n  const reaction2 = Math.tanh((reaction + Math.sin(dist*0.03-ang*5+t)*0.3 - cell*0.5)*1.8);\n  const blend = reaction*0.6 + reaction2*0.4, v = blend*0.5+0.5;\n  const pal = [[5,12,55],[10,35,85],[15,65,115],[18,100,130],[20,140,135],[60,165,120],[140,180,80],[200,185,55],[215,155,60],[210,115,65],[185,75,65],[140,45,60],[90,25,55],[45,15,50],[15,10,52]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const lum = 0.82+Math.sin(reaction*Math.PI)*0.18;\n  const edge = Math.abs(blend)>0.65?1.12:1.0;\n  const peak = blend>0.7?1.08:1.0;\n  const m = lum*edge*peak;\n  return [\n    Math.min(255, (pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*m)|0,\n    Math.min(255, (pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*m)|0,\n    Math.min(255, (pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*m)|0\n  ];\n}",
  "score": {
    "coherence": 8,
    "harmony": 7,
    "complexity": 9,
    "density": 6,
    "total": 30,
    "reason": "The image displays high coherence with smooth, flowing patterns and no visible artifacts. Its color harmony is strong with a vibrant, contrasting palette (teal, orange, black) that is visually appealing but slightly aggressive. Complexity is high due to the intricate, fractal-like organic forms. However, density is low — it's mostly abstract art with minimal functional UI elements, making it less useful for conveying information in a minimal OS context."
  },
  "timestamp": 1774494275154,
  "filename": "gen100_1774494275154"
}