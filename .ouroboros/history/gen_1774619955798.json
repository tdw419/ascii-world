{
  "code": "(x, y, t) => {\n  const P = [2, 3, 5, 7, 11, 13];\n  const cx = 240, cy = 120;\n  const wx = Math.sin(x*0.02+t*0.3)*15 + Math.cos(y*P[2]*0.003-t*0.2)*10;\n  const wy = Math.cos(y*0.02-t*0.25)*15 + Math.sin(x*P[3]*0.0026+t*0.35)*10;\n  const sx = x+wx, sy = y+wy;\n  const src = [\n    [160+Math.sin(t*0.6)*100,80+Math.cos(t*0.4)*50,0.08*P[0],2.0],\n    [320+Math.cos(t*0.5)*100,160+Math.sin(t*0.7)*50,0.08*P[1],1.7],\n    [240+Math.sin(t*0.3+2)*140,120+Math.cos(t*0.5+1)*90,0.08*P[2],2.3],\n    [120+Math.cos(t*0.8+1)*80,180+Math.sin(t*0.6+3)*60,0.08*P[3],1.5],\n    [360+Math.sin(t*0.4+4)*90,60+Math.cos(t*0.9+2)*70,0.08*P[4],2.6],\n    [240+Math.cos(t*0.35+5)*120,200+Math.sin(t*0.55)*80,0.08*P[5],1.9],\n    [80+Math.sin(t*0.45)*60,40+Math.cos(t*0.65)*40,0.08*P[0]*P[1],2.1],\n    [400+Math.cos(t*0.55)*70,200+Math.sin(t*0.75)*50,0.08*P[1]*P[2],1.8]\n  ];\n  let aS=0, iS=0;\n  for(const[sX,sY,f,sp]of src){const d=Math.sqrt((sx-sX)**2+(sy-sY)**2);aS+=Math.sin(d*f-t*sp);iS+=Math.sin(d*f*0.7+t*sp*0.6+1.5);}\n  const act=aS/8, inh=iS/8;\n  const bg=(Math.sin(sx*0.04+sy*0.025+t*0.8)+Math.sin(sx*P[0]*0.015-sy*P[1]*0.01+t*1.1))/2+Math.sin((sx+sy)*0.12+t*2.5)*0.2+Math.sin((sx-sy)*P[2]*0.014-t*2)*0.2;\n  const dist=Math.sqrt((sx-cx)**2+(sy-cy)**2), ang=Math.atan2(sy-cx,sx-cx);\n  const spiral=(Math.sin(dist*0.05-ang*P[3]-t*1.5)+Math.sin(dist*0.04+ang*P[2]-t*1.2))*0.4;\n  const layerBand=Math.floor(y/40)%6;\n  const layerMod=Math.sin(sx*0.1*P[layerBand]+t*(0.5+layerBand*0.15))*0.12;\n  const reaction=Math.tanh((act-inh*0.7+bg*0.3+spiral+layerMod)*2.2);\n  const cell=Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15)+Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  const gridX=Math.abs((sx%60)-30)<0.4?1:0, gridY=Math.abs((sy%30)-15)<0.4?1:0, grid=Math.max(gridX,gridY);\n  const w1x=sx>18&&sx<222&&(Math.abs(sx-18)<1.2||Math.abs(sx-222)<1.2)?1:0;\n  const w1y=sy>13&&sy<107&&(Math.abs(sy-13)<1.2||Math.abs(sy-107)<1.2)?1:0;\n  const w2x=sx>248&&sx<462&&(Math.abs(sx-248)<1.2||Math.abs(sx-462)<1.2)?1:0;\n  const w2y=sy>128&&sy<227&&(Math.abs(sy-128)<1.2||Math.abs(sy-227)<1.2)?1:0;\n  const frames=Math.max(w1x,w1y,w2x,w2y);\n  const statusB=(sy>1&&sy<11)?1:0;\n  const statusPulse=(Math.sin(sx*0.05+t*2)*0.5+0.5)*statusB;\n  const txtL1=(sy>24&&sy<31&&sx>28&&sx<212)?(Math.sin(sx*1.7+t*0.1)>0.3?0.5:0):0;\n  const txtL2=(sy>34&&sy<41&&sx>28&&sx<180)?(Math.sin(sx*2.1+t*0.08+1)>0.35?0.35:0):0;\n  const txtL3=(sy>44&&sy<51&&sx>28&&sx<195)?(Math.sin(sx*1.9+t*0.12+2)>0.3?0.4:0):0;\n  const txtL4=(sy>139&&sy<146&&sx>258&&sx<445)?(Math.sin(sx*1.6+t*0.09+3)>0.3?0.45:0):0;\n  const txtLines=txtL1;\n  return [0, 0, 0];\n}",
  "score": {
    "coherence": 10,
    "harmony": 10,
    "complexity": 1,
    "density": 1,
    "total": 22,
    "reason": "The image is a solid black framebuffer, which is perfectly coherent and harmonious with no visual artifacts. However, it contains no patterns or information, making its complexity and density extremely low. This is a minimal, empty state suitable for a boot screen or uninitialized display."
  },
  "timestamp": 1774619955798,
  "filename": "gen1806_1774619955798"
}