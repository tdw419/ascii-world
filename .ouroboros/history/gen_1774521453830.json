{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  let wx = Math.sin(x * 0.02 + t * 0.3) * 15 + Math.cos(y * 0.015 - t * 0.2) * 10;\n  let wy = Math.cos(y * 0.02 - t * 0.25) * 15 + Math.sin(x * 0.018 + t * 0.35) * 10;\n  wx += Math.sin((x + y) * 0.01 + t * 0.5) * 8 + Math.cos((x - y * 0.7) * 0.015 - t * 0.4) * 6;\n  wy += Math.cos((x - y) * 0.012 - t * 0.45) * 8 + Math.sin((y + x * 0.6) * 0.013 + t * 0.38) * 6;\n  const sx = x + wx, sy = y + wy;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0, 0.7],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7, 0.5],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3, 0.8],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5, 0.6],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6, 0.4],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9, 0.65],\n    [80+Math.sin(t*0.65+3)*70, 40+Math.cos(t*0.75)*90, 0.11, 2.1, 0.55],\n    [400+Math.cos(t*0.45+1)*60, 200+Math.sin(t*0.85+2)*50, 0.05, 2.8, 0.75]\n  ];\n  let aS = 0, iS = 0, cross = 0;\n  for (let si = 0; si < src.length; si++) {\n    const [sX, sY, f, sp, amp] = src[si];\n    const d = Math.sqrt((sx - sX) ** 2 + (sy - sY) ** 2);\n    const wave = Math.sin(d * f - t * sp);\n    const iwave = Math.sin(d * f * 0.7 + t * sp * 0.6 + 1.5);\n    aS += wave * amp;\n    iS += iwave * amp;\n    for (let sj = si + 1; sj < src.length; sj++) {\n      const [sX2, sY2, f2, sp2] = src[sj];\n      const d2 = Math.sqrt((sx - sX2) ** 2 + (sy - sY2) ** 2);\n      cross += Math.sin((d * f + d2 * f2) * 0.5 - t * (sp + sp2) * 0.4) * 0.08;\n    }\n  }\n  const n = src.length;\n  const act = aS / n, inh = iS / n;\n  const bg = (Math.sin(sx * 0.04 + sy * 0.025 + t * 0.8) + Math.sin(sx * 0.03 - sy * 0.04 + t * 1.1)) / 2;\n  const bg2 = (Math.sin((sx + sy) * 0.12 + t * 2.5) + Math.sin((sx - sy) * 0.1 - t * 2)) * 0.2;\n  const bg3 = Math.sin(sx * 0.065 + t * 1.8) * Math.cos(sy * 0.055 - t * 1.3) * 0.15;\n  const dist = Math.sqrt((sx - cx) ** 2 + (sy - cy) ** 2);\n  const ang = Math.atan2(sy - cy, sx - cx);\n  const spiral = (Math.sin(dist * 0.05 - ang * 3 - t * 1.5) + Math.sin(dist * 0.04 + ang * 4 - t * 1.2)) * 0.35;\n  const spiral2 = Math.sin(dist * 0.08 - ang * 5 + t * 2.2) * Math.cos(dist * 0.06 + ang * 2 - t * 1.8) * 0.15;\n  const fine = Math.sin(sx * 0.2 + sy * 0.15 + t * 3) * Math.sin(sx * 0.18 - sy * 0.22 - t * 2.5) * 0.12;\n  const cell = Math.tanh((Math.sin(sx * 0.15) * Math.sin(sy * 0.15) + Math.sin((sx + sy) * 0.1 + t) * 0.5) * 1.5) * 0.25;\n  const voronoi = (() => {\n    let md = 99;\n    for (let i = 0; i < src.length; i++) {\n      const vx = src[i][0] + Math.sin(t * 0.3 + i * 1.7) * 20;\n      const vy = src[i][1] + Math.cos(t * 0.25 + i * 2.1) * 20;\n      const d = Math.sqrt((sx - vx) ** 2 + (sy - vy) ** 2);\n      if (d < md) md = d;\n    }\n    return Math.sin(md * 0.15 - t) * 0.2;\n  })();\n  const field = act - inh * 0.65 + (bg + bg2 + bg3) * 0.35 + spiral + spiral2 + cross + fine + cell + voronoi;\n  const reaction = Math.tanh(field * 2.0);\n  const energy = Math.tanh((Math.abs(act) + Math.abs(inh) * 0.5 + Math.abs(cross) * 3) * 1.2);\n  const blend = reaction * 0.7 + cell * 0.15 + voronoi * 0.15;\n  const v = blend * 0.5 + 0.5;\n  const pal = [\n    [5, 10, 42], [10, 35, 85], [15, 70, 120], [20, 120, 130],\n    [60, 165, 140], [160, 195, 100], [220, 195, 65], [230, 145, 60],\n    [210, 90, 70], [170, 50, 80], [110, 25, 70], [50, 12, 55], [5, 10, 42]\n  ];\n  const idx = v * (pal.length - 1);\n  const i = Math.min(idx | 0, pal.length - 2);\n  const f = idx - i;\n  const lum = 0.75 + energy * 0.2 + Math.sin(reaction * Math.PI) * 0.15;\n  const edge = Math.abs(reaction) > 0.72 ? 1.12 + energy * 0.08 : 0.92 + energy * 0.05;\n  const glow = Math.max(0, reaction - 0.6) * 0.5;\n  return [\n    Math.min(255, ((pal[i][0] + (pal[i + 1][0] - pal[i][0]) * f) * lum * edge + glow * 60) | 0),\n    Math.min(255, ((pal[i][1] + (pal[i + 1][1] - pal[i][1]) * f) * lum * edge + glow * 40) | 0),\n    Math.min(255, ((pal[i][2] + (pal[i + 1][2] - pal[i][2]) * f) * lum * edge + glow * 25) | 0)\n  ];\n}",
  "score": {
    "coherence": 7,
    "harmony": 6,
    "complexity": 9,
    "density": 4,
    "total": 26,
    "reason": "The image has high complexity with intricate, organic patterns resembling a heat map or fluid simulation. Coherence is good with smooth transitions but lacks clear structure. Harmony is moderate due to vibrant, contrasting colors that may be visually fatiguing. Density is low — it's mostly abstract noise with no discernible UI elements or information, making it unsuitable for a functional OS interface."
  },
  "timestamp": 1774521453829,
  "filename": "gen857_1774521453829"
}