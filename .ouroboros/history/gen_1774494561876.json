{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const wx1 = Math.sin(x*0.02+t*0.3)*15 + Math.cos(y*0.015-t*0.2)*10;\n  const wy1 = Math.cos(y*0.02-t*0.25)*15 + Math.sin(x*0.018+t*0.35)*10;\n  const wx2 = Math.sin((x+wx1)*0.035+t*0.5)*8 + Math.cos((y+wy1)*0.03-t*0.4)*6;\n  const wy2 = Math.cos((y+wy1)*0.03-t*0.45)*8 + Math.sin((x+wx1)*0.028+t*0.55)*6;\n  const sx = x + wx1 + wx2, sy = y + wy1 + wy2;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9],\n    [400+Math.sin(t*0.65+3)*70, 120+Math.cos(t*0.45+2)*80, 0.11, 2.1],\n    [80+Math.cos(t*0.75+1)*90, 40+Math.sin(t*0.85+4)*50, 0.13, 1.3]\n  ];\n  let aS = 0, iS = 0, hS = 0;\n  for (const [sX, sY, f, sp] of src) {\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    aS += Math.sin(d*f - t*sp);\n    iS += Math.sin(d*f*0.7 + t*sp*0.6 + 1.5);\n    hS += Math.sin(d*f*1.3 - t*sp*0.4 + 3.0);\n  }\n  const n = 8, act = aS/n, inh = iS/n, har = hS/n;\n  const bg = (Math.sin(sx*0.04+sy*0.025+t*0.8) + Math.sin(sx*0.03-sy*0.04+t*1.1))/2 + Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2 + Math.sin(sx*0.07+t*0.3)*Math.cos(sy*0.06-t*0.4)*0.15;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2))*0.4 + (Math.sin(dist*0.08+ang*2-t*2.0) + Math.sin(dist*0.06-ang*5+t*1.8))*0.2;\n  const gx = sx*0.08, gy = sy*0.08, igx = Math.floor(gx), igy = Math.floor(gy);\n  let minD = 999, secD = 999;\n  for (let dx = -1; dx <= 1; dx++) {\n    for (let dy = -1; dy <= 1; dy++) {\n      const nx = igx+dx, ny = igy+dy;\n      const vx = nx + Math.sin(nx*1.7+ny*2.3+t*0.8)*0.4, vy = ny + Math.cos(nx*2.1+ny*1.3-t*0.6)*0.4;\n      const dd = (gx-vx)**2 + (gy-vy)**2;\n      if (dd < minD) { secD = minD; minD = dd; } else if (dd < secD) secD = dd;\n    }\n  }\n  const voronoi = (Math.sqrt(secD)-Math.sqrt(minD))*4;\n  const rd1 = Math.tanh((act - inh*0.7 + bg*0.3 + spiral)*2.2);\n  const rd2 = Math.tanh((har*0.6 - act*0.3 + bg*0.2 + voronoi*0.5 - 1.5)*2.0);\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  const blend = rd1*0.5 + rd2*0.2 + cell*0.15 + voronoi*0.08*rd1, v = blend*0.5+0.5;\n  const pal = [[5,10,45],[10,45,90],[15,85,120],[25,140,130],[80,175,100],[180,185,65],[230,170,50],[225,130,60],[200,85,70],[150,50,75],[90,25,65],[40,15,55],[15,10,50],[5,10,45]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const lum = 0.75+Math.sin(rd1*Math.PI)*0.25, edge = Math.abs(rd1)>0.75?1.2:Math.abs(rd1)>0.5?1.05:0.95;\n  const glow = Math.max(0, rd1-0.3)*0.3;\n  return [\n    Math.min(255, ((pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*edge+glow*60)|0),\n    Math.min(255, ((pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*edge+glow*30)|0),\n    Math.min(255, ((pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*edge+glow*50)|0)\n  ];\n}",
  "score": {
    "coherence": 7,
    "harmony": 6,
    "complexity": 9,
    "density": 5,
    "total": 27,
    "reason": "The image displays high complexity with intricate, swirling patterns and fractal-like structures. Coherence is good with clear visual groupings, though some areas appear noisy. Harmony is moderate due to the vibrant, contrasting colors that may be visually fatiguing. Density is low as the image is abstract and lacks functional UI elements, making it unsuitable for conveying information efficiently in an OS context."
  },
  "timestamp": 1774494561876,
  "filename": "gen107_1774494561876"
}