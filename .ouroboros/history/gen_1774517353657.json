{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const wx1 = Math.sin(x*0.018+t*0.25)*18 + Math.cos(y*0.013-t*0.18)*12;\n  const wy1 = Math.cos(y*0.018-t*0.22)*18 + Math.sin(x*0.015+t*0.3)*12;\n  const sx1 = x+wx1, sy1 = y+wy1;\n  const wx2 = Math.sin(sx1*0.025-t*0.15)*10 + Math.cos(sy1*0.02+t*0.2)*8;\n  const wy2 = Math.cos(sy1*0.025+t*0.18)*10 + Math.sin(sx1*0.022-t*0.25)*8;\n  const sx = sx1+wx2, sy = sy1+wy2;\n  const src = [\n    [cx+Math.cos(t*0.4)*120+Math.sin(t*0.7)*40, cy+Math.sin(t*0.35)*80+Math.cos(t*0.6)*30, 0.07, 2.0],\n    [cx+Math.cos(t*0.5+2.1)*100, cy+Math.sin(t*0.45+1.3)*90, 0.09, 1.7],\n    [cx+Math.cos(t*0.3+4.2)*140, cy+Math.sin(t*0.55+3.1)*100, 0.055, 2.3],\n    [cx+Math.cos(t*0.65+1.0)*80, cy+Math.sin(t*0.5+5.0)*70, 0.11, 1.5],\n    [cx+Math.cos(t*0.38+3.5)*110, cy+Math.sin(t*0.72+0.5)*85, 0.065, 2.6],\n    [cx+Math.cos(t*0.42+5.2)*95, cy+Math.sin(t*0.58+2.8)*95, 0.085, 1.9],\n    [cx+Math.cos(t*0.28+0.8)*130, cy+Math.sin(t*0.48+4.5)*75, 0.075, 2.1],\n    [cx+Math.cos(t*0.55+3.8)*70, cy+Math.sin(t*0.62+1.7)*110, 0.1, 1.6]\n  ];\n  let act=0, inh=0, act2=0, inh2=0;\n  for (const [sX, sY, f, sp] of src) {\n    const d = Math.sqrt((sx-sX)**2+(sy-sY)**2);\n    act += Math.sin(d*f-t*sp);\n    inh += Math.sin(d*f*0.7+t*sp*0.6+1.5);\n    act2 += Math.sin(d*f*1.4-t*sp*0.8+0.7);\n    inh2 += Math.cos(d*f*0.5+t*sp*0.4+2.3);\n  }\n  act/=8; inh/=8; act2/=8; inh2/=8;\n  const bg = (Math.sin(sx*0.035+sy*0.022+t*0.7)+Math.sin(sx*0.028-sy*0.035+t*0.95)+Math.sin(sx*0.055+sy*0.04-t*1.3)*0.6+Math.sin((sx+sy)*0.11+t*2.2)*0.3+Math.sin((sx-sy)*0.09-t*1.8)*0.3+Math.sin(sx*0.08*Math.cos(t*0.1)+sy*0.06*Math.sin(t*0.15)+t*0.5)*0.4)/4.2;\n  const dist = Math.sqrt((sx-cx)**2+(sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.045-ang*3-t*1.3)+Math.sin(dist*0.038+ang*4-t*1.0)+Math.sin(dist*0.06-ang*2-t*1.8)*0.5)*0.3;\n  const cx2=160+Math.sin(t*0.2)*60, cy2=100+Math.cos(t*0.15)*40;\n  const dist2=Math.sqrt((sx-cx2)**2+(sy-cy2)**2), ang2=Math.atan2(sy-cy2, sx-cx2);\n  const spiral2=(Math.sin(dist2*0.05-ang2*2.5-t*1.1)+Math.sin(dist2*0.04+ang2*3.5-t*0.9))*0.2;\n  const react1=Math.tanh((act-inh*0.65+bg*0.3+spiral)*2.2);\n  const react2=Math.tanh((act2-inh2*0.5+spiral2+bg*0.2)*2.0);\n  const cross=Math.tanh((react1*0.6+react2*0.4-react1*react2*0.8)*1.8);\n  const cell1=Math.sin(sx*0.14)*Math.sin(sy*0.14)+Math.sin((sx+sy)*0.09+t*0.6)*0.4;\n  const cell2=Math.sin(sx*0.11-sy*0.08+t*0.4)*Math.cos(sx*0.07+sy*0.12-t*0.3);\n  const cell=Math.tanh((cell1+cell2*0.5)*1.3)*0.25;\n  const edge=Math.abs(cross)>0.65?1.0:0.0;\n  const glow=Math.exp(-Math.abs(cross)*3)*0.15;\n  const blend=cross*0.65+cell*0.2+glow*0.15, v=blend*0.5+0.5;\n  const pal=[[6,12,52],[12,45,88],[18,95,115],[28,155,138],[120,195,145],[210,210,95],[225,155,65],[195,85,65],[145,45,75],[75,22,65],[25,12,55],[6,12,52]];\n  const idx=v*(pal.length-1), i=Math.min(idx|0, pal.length-2), f=idx-i;\n  const lum=0.82+Math.sin(cross*Math.PI)*0.18, edgeBoost=1.0+edge*0.2, fl=lum*edgeBoost;\n  return [Math.min(255,(pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*fl)|0,Math.min(255,(pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*fl)|0,Math.min(255,(pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*fl)|0];\n}",
  "score": {
    "coherence": 8,
    "harmony": 7,
    "complexity": 9,
    "density": 5,
    "total": 29,
    "reason": "The image has high coherence with smooth, flowing patterns and no visible artifacts. Its color harmony is strong with a warm-cool contrast, though the intensity may reduce readability. Complexity is high due to intricate, organic shapes. However, density is low — it's purely decorative with no functional UI elements, making it unsuitable for a practical OS interface."
  },
  "timestamp": 1774517353657,
  "filename": "gen741_1774517353657"
}