{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const w1x = Math.sin(x*0.015+t*0.3)*20 + Math.cos(y*0.02-t*0.2)*15;\n  const w1y = Math.cos(y*0.018-t*0.25)*18 + Math.sin(x*0.022+t*0.35)*12;\n  const w2x = Math.sin(x*0.035+t*0.5)*8 + Math.cos(y*0.04-t*0.45)*6;\n  const w2y = Math.cos(y*0.038-t*0.55)*7 + Math.sin(x*0.032+t*0.6)*5;\n  const sx = x + w1x + w2x, sy = y + w1y + w2y;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0, 1.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7, 0.8],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3, 1.2],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5, 0.9],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6, 1.1],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9, 0.7],\n    [80+Math.sin(t*0.45+3)*60, 40+Math.cos(t*0.65+1)*40, 0.14, 2.1, 0.6],\n    [400+Math.cos(t*0.55+2)*70, 200+Math.sin(t*0.75+4)*50, 0.11, 1.8, 0.85]\n  ];\n  let aS = 0, iS = 0, cross = 0;\n  for (const [sX, sY, f, sp, w] of src) {\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    const w1 = Math.sin(d*f - t*sp), w2 = Math.sin(d*f*0.7 + t*sp*0.6 + 1.5);\n    aS += w1*w; iS += w2*w; cross += w1*w2*w*0.3;\n  }\n  const n = 8, act = aS/n, inh = iS/n, cr = cross/n;\n  const bg1 = (Math.sin(sx*0.04+sy*0.025+t*0.8)+Math.sin(sx*0.03-sy*0.04+t*1.1))/2;\n  const bg2 = (Math.sin(sx*0.08-sy*0.06+t*1.5)+Math.cos(sx*0.07+sy*0.09-t*1.3))/2;\n  const bg = bg1*0.5+bg2*0.3+(Math.sin((sx+sy)*0.12+t*2.5)*0.3+Math.sin((sx-sy)*0.1-t*2)*0.3)*0.2;\n  const dist = Math.sqrt((sx-cx)**2+(sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5)+Math.sin(dist*0.04+ang*4-t*1.2))*0.4+Math.sin(dist*0.08+ang*2-t*2.5)*0.2;\n  const cX = sx*0.12, cY = sy*0.12, ci = Math.floor(cX), cj = Math.floor(cY);\n  let m1 = 99, m2 = 99;\n  for (let di = -1; di <= 1; di++) for (let dj = -1; dj <= 1; dj++) {\n    const nx = ci+di, ny = cj+dj;\n    const px = nx+0.5+Math.sin(nx*12.9898+ny*78.233+t*0.5)*0.45;\n    const py = ny+0.5+Math.cos(nx*43.758+ny*12.989+t*0.4)*0.45;\n    const d = Math.sqrt((cX-px)**2+(cY-py)**2);\n    if (d < m1) { m2 = m1; m1 = d; } else if (d < m2) m2 = d;\n  }\n  const vor = (m2-m1)*3;\n  const reaction = Math.tanh((act+cr*0.5-inh*0.7-bg*0.3-spiral)*2.2);\n  const cellC = Math.tanh(vor-0.5)*0.4;\n  const interf = Math.sin(reaction*3+cellC*4+t*0.8)*0.3;\n  const blend = reaction*0.6+cellC*0.2+interf*0.2, v = blend*0.5+0.5;\n  const pal = [[5,12,45],[12,45,85],[20,85,120],[25,130,130],[45,160,120],[140,180,80],[210,190,60],[220,150,55],[200,100,60],[160,55,70],[100,30,65],[50,18,55],[15,12,48],[5,12,45]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i, sf = f*f*(3-2*f);\n  const lum = 0.85+Math.sin(reaction*Math.PI)*0.15;\n  const edge = Math.abs(reaction)>0.75?1.12:(Math.abs(cellC)>0.3?1.05:1.0);\n  const glow = Math.max(0,1-Math.abs(blend))*0.3;\n  return [\n    Math.min(255,((pal[i][0]+(pal[i+1][0]-pal[i][0])*sf)*lum*edge+glow*40)|0),\n    Math.min(255,((pal[i][1]+(pal[i+1][1]-pal[i][1])*sf)*lum*edge+glow*30)|0),\n    Math.min(255,((pal[i][2]+(pal[i+1][2]-pal[i][2])*sf)*lum*edge+glow*50)|0)\n  ];\n}",
  "score": {
    "coherence": 8,
    "harmony": 7,
    "complexity": 9,
    "density": 6,
    "total": 30,
    "reason": "The image displays high coherence with smooth, flowing patterns and no visual artifacts. Its color harmony is strong with a warm-cool contrast, though the intensity may reduce readability. Complexity is high due to the intricate, mathematically inspired waveforms. However, density is low for a UI, as it lacks clear information or functional elements, making it more decorative than practical."
  },
  "timestamp": 1774532821545,
  "filename": "gen197_1774532821545"
}