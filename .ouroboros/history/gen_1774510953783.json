{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const w1x = Math.sin(x*0.018+t*0.25)*18 + Math.cos(y*0.013-t*0.18)*12 + Math.sin((x+y)*0.008+t*0.4)*8;\n  const w1y = Math.cos(y*0.016-t*0.22)*18 + Math.sin(x*0.014+t*0.28)*12 + Math.cos((x-y)*0.009-t*0.35)*8;\n  const sx = x + w1x, sy = y + w1y;\n  const w2x = Math.sin(sx*0.025-t*0.15)*8 + Math.cos(sy*0.02+t*0.2)*6;\n  const w2y = Math.cos(sy*0.023+t*0.18)*8 + Math.sin(sx*0.018-t*0.22)*6;\n  const fx = sx + w2x, fy = sy + w2y;\n  const src = [\n    [160+Math.sin(t*0.5)*110, 75+Math.cos(t*0.35)*55, 0.075, 2.2, 1.0],\n    [320+Math.cos(t*0.45)*110, 165+Math.sin(t*0.6)*55, 0.095, 1.8, 0.85],\n    [240+Math.sin(t*0.28+2)*150, 120+Math.cos(t*0.42+1)*95, 0.055, 2.5, 1.1],\n    [110+Math.cos(t*0.75+1)*85, 185+Math.sin(t*0.55+3)*65, 0.11, 1.6, 0.9],\n    [370+Math.sin(t*0.38+4)*95, 55+Math.cos(t*0.85+2)*75, 0.065, 2.8, 0.95],\n    [240+Math.cos(t*0.32+5)*130, 205+Math.sin(t*0.5)*85, 0.085, 2.0, 1.05],\n    [60+Math.sin(t*0.6+3)*70, 100+Math.cos(t*0.7+1)*60, 0.1, 1.4, 0.8],\n    [420+Math.cos(t*0.33+2)*80, 140+Math.sin(t*0.48+4)*70, 0.07, 2.4, 1.0]\n  ];\n  let aS = 0, iS = 0, cross = 0;\n  for (let i = 0; i < src.length; i++) {\n    const [sX, sY, f, sp, wt] = src[i];\n    const d = Math.sqrt((fx-sX)**2 + (fy-sY)**2);\n    const a = Math.sin(d*f - t*sp);\n    const inh = Math.sin(d*f*0.65 + t*sp*0.55 + 1.5);\n    aS += a * wt;\n    iS += inh * wt;\n    for (let j = i+1; j < src.length; j++) {\n      const [sX2, sY2, f2] = src[j];\n      const d2 = Math.sqrt((fx-sX2)**2 + (fy-sY2)**2);\n      cross += Math.sin(d*f + d2*f2 - t*(sp+src[j][3])*0.5) * 0.04 * wt * src[j][4];\n    }\n  }\n  const n = 8;\n  const act = aS/n, inh = iS/n;\n  const bg = (Math.sin(fx*0.035+fy*0.022+t*0.7) + Math.sin(fx*0.028-fy*0.038+t*0.95) + Math.sin(fx*0.018+fy*0.045-t*0.6))/3;\n  const dist = Math.sqrt((fx-cx)**2 + (fy-cy)**2), ang = Math.atan2(fy-cy, fx-cx);\n  const spiral = (Math.sin(dist*0.045-ang*3.5-t*1.3) + Math.sin(dist*0.035+ang*4.5-t*1.0) + Math.sin(dist*0.06-ang*2-t*1.8)*0.5)/2.5;\n  const turb = (Math.sin(fx*0.08+Math.sin(fy*0.06+t)*2+t*1.5) + Math.sin(fy*0.09+Math.cos(fx*0.05-t)*2-t*1.2))*0.2;\n  const fine = Math.sin(fx*0.22+fy*0.18+t*3)*Math.sin(fx*0.19-fy*0.24-t*2.5)*0.15;\n  const raw = act - inh*0.65 + bg*0.25 + spiral*0.4 + cross + turb + fine;\n  const reaction = Math.tanh(raw*2.0);\n  const laplacian = Math.sin(fx*0.12)*Math.sin(fy*0.12) + Math.sin((fx+fy)*0.08+t*0.8)*0.6 + Math.sin(fx*0.18)*Math.cos(fy*0.16-t)*0.3;\n  const cell = Math.tanh(laplacian*1.2)*0.25;\n  const blend = reaction*0.7 + cell*0.3;\n  const v = blend*0.5+0.5;\n  const pal = [[6,12,52],[10,42,82],[14,85,108],[18,130,125],[45,165,120],[140,190,85],[210,195,60],[220,150,55],[195,95,60],[160,50,70],[100,28,65],[55,18,58],[30,12,48],[6,12,52]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const r0 = pal[i][0]+(pal[i+1][0]-pal[i][0])*f, g0 = pal[i][1]+(pal[i+1][1]-pal[i][1])*f, b0 = pal[i][2]+(pal[i+1][2]-pal[i][2])*f;\n  const absR = Math.abs(reaction), edge = 1.0 + (absR > 0.65 ? (absR-0.65)*1.2 : 0);\n  const glow = Math.max(0, absR-0.5)*0.6;\n  const lum = 0.82 + Math.sin(reaction*Math.PI)*0.18;\n  return [\n    Math.min(255, (r0*lum*edge + glow*40)|0),\n    Math.min(255, (g0*lum*edge + glow*25)|0),\n    Math.min(255, (b0*lum*edge + glow*15)|0)\n  ];\n}",
  "score": {
    "coherence": 6,
    "harmony": 5,
    "complexity": 8,
    "density": 3,
    "total": 22,
    "reason": "The image has high visual complexity with intricate, swirling patterns but lacks coherence due to chaotic color transitions and no clear structure. Color harmony is low because of high contrast between warm and cool tones. Density is very low — it's mostly abstract noise with no discernible UI elements or information — making it unsuitable for a functional OS interface."
  },
  "timestamp": 1774510953783,
  "filename": "gen567_1774510953783"
}