{
  "code": "(x, y, t) => {\n  const s = [\n    [160 + Math.sin(t * 0.6) * 100, 80 + Math.cos(t * 0.4) * 50],\n    [320 + Math.cos(t * 0.5) * 100, 160 + Math.sin(t * 0.7) * 50],\n    [240 + Math.sin(t * 0.3 + 2) * 140, 120 + Math.cos(t * 0.5 + 1) * 90],\n    [100 + Math.cos(t * 0.8 + 4) * 80, 180 + Math.sin(t * 0.6 + 3) * 60],\n    [380 + Math.sin(t * 0.4 + 1) * 70, 60 + Math.cos(t * 0.9 + 2) * 40],\n    [240 + Math.cos(t * 0.35 + 5) * 160, 200 + Math.sin(t * 0.45 + 4) * 30]\n  ];\n  const fr = [0.08, 0.1, 0.06, 0.09, 0.11, 0.07], sp = [2, 1.7, 2.3, 1.9, 2.1, 1.5];\n  const d = s.map(p => Math.sqrt((x - p[0]) ** 2 + (y - p[1]) ** 2));\n  const w = d.map((di, i) => Math.sin(di * fr[i] - t * sp[i]));\n  const activator = (w[0] + w[1] * 0.9 + w[2] * 0.8 + w[3] * 0.7 + w[4] * 0.6 + w[5] * 0.5) / 4.5;\n  const wx = x + Math.sin(y * 0.02 + t * 0.3) * 20, wy = y + Math.cos(x * 0.02 - t * 0.4) * 20;\n  const inhibitor = (Math.sin(wx * 0.05 + wy * 0.03 + t * 0.8) + Math.sin(wx * 0.03 - wy * 0.05 + t * 1.1) + Math.sin((wx + wy) * 0.02 + t * 0.5)) / 3;\n  const detail = Math.sin((x + y) * 0.15 + t * 3) * 0.15 + Math.sin((x - y) * 0.12 - t * 2.5) * 0.15 + Math.sin(x * 0.18 + t * 4) * 0.08;\n  const r1 = activator - inhibitor * 0.8 + detail;\n  const spot1 = Math.tanh(r1 * 2.5);\n  const r2 = Math.tanh((w[0] + w[2]) * 1.5 - (w[1] + w[3]) * 1.2 + detail * 2) * Math.tanh(activator * 3 - inhibitor * 2);\n  const cx = 240, cy = 120, dist = Math.sqrt((x - cx) ** 2 + (y - cy) ** 2);\n  const ang = Math.atan2(y - cy, x - cx);\n  const p2 = Math.tanh((Math.sin(dist * 0.04 - t * 1.5) + Math.sin(ang * 6 + t * 0.4) * 0.5 + Math.sin(ang * 3 - dist * 0.03 + t * 0.8) * 0.3) * 2);\n  const fine = Math.tanh((w[4] + w[5] - inhibitor) * 3 + detail * 3);\n  const blend = spot1 * 0.45 + p2 * 0.25 + r2 * 0.2 + fine * 0.1;\n  const v = blend * 0.5 + 0.5;\n  const pal = [[8,15,58],[15,85,115],[35,165,145],[225,185,55],[220,100,65],[165,55,85],[8,15,58]];\n  const idx = v * (pal.length - 1), i = Math.min(idx | 0, pal.length - 2), f = idx - i;\n  const lum = 0.82 + Math.sin(r1 * Math.PI) * 0.12 + Math.sin(r2 * 2) * 0.06;\n  return [\n    Math.min(255, (pal[i][0] + (pal[i+1][0] - pal[i][0]) * f) * lum) | 0,\n    Math.min(255, (pal[i][1] + (pal[i+1][1] - pal[i][1]) * f) * lum) | 0,\n    Math.min(255, (pal[i][2] + (pal[i+1][2] - pal[i][2]) * f) * lum) | 0\n  ];\n}",
  "score": {
    "coherence": 7,
    "harmony": 6,
    "complexity": 9,
    "density": 4,
    "total": 26,
    "reason": "The image displays coherent, flowing patterns with a harmonious color gradient (reds, oranges, teals, dark blues), but lacks clear structure or readable information. Its complexity is high due to intricate, organic shapes and gradients. However, its density is low — it's visually rich but not useful for UI or conveying information — making it more artistic than functional for an OS."
  },
  "timestamp": 1774491314167,
  "filename": "gen14_1774491314167"
}