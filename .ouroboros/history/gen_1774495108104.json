{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  let wx = Math.sin(x * 0.02 + t * 0.3) * 15 + Math.cos(y * 0.015 - t * 0.2) * 10;\n  let wy = Math.cos(y * 0.02 - t * 0.25) * 15 + Math.sin(x * 0.018 + t * 0.35) * 10;\n  wx += Math.sin((x + y) * 0.01 + t * 0.5) * 8 + Math.cos(x * 0.008 - y * 0.012 + t * 0.15) * 12;\n  wy += Math.cos((x - y) * 0.012 - t * 0.4) * 8 + Math.sin(y * 0.01 + x * 0.007 + t * 0.2) * 10;\n  const sx = x + wx, sy = y + wy;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0, 0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7, 1.2],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3, 2.5],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5, 3.8],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6, 5.1],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9, 0.7],\n    [80+Math.sin(t*0.45+3)*70, 40+Math.cos(t*0.65)*90, 0.11, 2.1, 4.2],\n    [400+Math.cos(t*0.55+2)*60, 200+Math.sin(t*0.75+1)*50, 0.085, 1.8, 2.9]\n  ];\n  let aS = 0, iS = 0, cross = 0;\n  for (let i = 0; i < src.length; i++) {\n    for (let j = i + 1; j < src.length; j++) {\n      const [sX1, sY1, f1, sp1] = src[i], [sX2, sY2, f2, sp2] = src[j];\n      const d1 = Math.sqrt((sx-sX1)**2 + (sy-sY1)**2);\n      const d2 = Math.sqrt((sx-sX2)**2 + (sy-sY2)**2);\n      cross += Math.sin(d1*f1 - t*sp1) * Math.sin(d2*f2 - t*sp2) * 0.15;\n    }\n    const [sX, sY, f, sp, ph] = src[i];\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    aS += Math.sin(d*f - t*sp + ph);\n    iS += Math.sin(d*f*0.7 + t*sp*0.6 + 1.5 + ph);\n  }\n  const act = aS/8, inh = iS/8;\n  const bg = (Math.sin(sx*0.04+sy*0.025+t*0.8) + Math.sin(sx*0.03-sy*0.04+t*1.1))/2 + Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2 + Math.sin(sx*0.065+Math.sin(sy*0.05+t)*2+t*1.5)*0.15;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2) + Math.sin(dist*0.07-ang*2+t*0.9))*0.27;\n  const turb = Math.sin(sx*0.08+Math.sin(sy*0.06+t*0.7)*3+t)*0.2 + Math.cos(sy*0.07+Math.cos(sx*0.05-t*0.5)*2.5-t*0.8)*0.2;\n  const reaction = Math.tanh((act - inh*0.65 + bg*0.25 + spiral + cross + turb)*2.0);\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5 + Math.sin(sx*0.2-sy*0.12+t*1.3)*0.3)*1.5)*0.3;\n  const pulse = 0.95 + Math.sin(t*0.4)*0.05 + Math.sin(t*0.17)*0.03;\n  const blend = (reaction*0.7 + cell*0.2 + cross*0.1) * pulse, v = blend*0.5+0.5;\n  const pal = [[5,12,52],[10,45,95],[18,95,125],[30,155,145],[80,185,120],[180,195,65],[220,155,50],[215,95,55],[180,50,60],[120,25,65],[60,15,55],[5,12,52]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const sm = f*f*(3-2*f);\n  const lum = 0.82+Math.sin(reaction*Math.PI)*0.18;\n  const edge = Math.abs(reaction)>0.65?1.12:Math.abs(reaction)>0.4?1.04:1.0;\n  const depth = 0.92 + Math.sin(ang*2+dist*0.02+t*0.3)*0.08;\n  return [\n    Math.min(255, (pal[i][0]+(pal[i+1][0]-pal[i][0])*sm)*lum*edge*depth)|0,\n    Math.min(255, (pal[i][1]+(pal[i+1][1]-pal[i][1])*sm)*lum*edge*depth)|0,\n    Math.min(255, (pal[i][2]+(pal[i+1][2]-pal[i][2])*sm)*lum*edge*depth)|0\n  ];\n}",
  "score": {
    "coherence": 8,
    "harmony": 7,
    "complexity": 9,
    "density": 5,
    "total": 29,
    "reason": "The image has strong visual coherence with smooth gradients and no obvious artifacts. Its color harmony is decent but slightly chaotic due to the high-contrast palette. Complexity is high with intricate, flowing patterns. However, density is low — it's mostly abstract noise with minimal functional UI information, making it unsuitable for a practical OS interface."
  },
  "timestamp": 1774495108104,
  "filename": "gen125_1774495108104"
}