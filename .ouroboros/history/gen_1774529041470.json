{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const wx = Math.sin(x * 0.02 + t * 0.3) * 18 + Math.cos(y * 0.015 - t * 0.2) * 12 + Math.sin((x+y)*0.01+t*0.5)*6;\n  const wy = Math.cos(y * 0.02 - t * 0.25) * 18 + Math.sin(x * 0.018 + t * 0.35) * 12 + Math.cos((x-y)*0.012-t*0.4)*6;\n  let sx = x + wx, sy = y + wy;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0, 1.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7, 0.9],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3, 1.1],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5, 0.8],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6, 1.2],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9, 0.95],\n    [60+Math.sin(t*0.45+3)*70, 40+Math.cos(t*0.65+2)*50, 0.11, 2.1, 0.85],\n    [400+Math.cos(t*0.55+1)*60, 220+Math.sin(t*0.75+4)*40, 0.13, 1.8, 1.05]\n  ];\n  let aS = 0, iS = 0, a2 = 0, i2 = 0;\n  for (const [sX, sY, f, sp, amp] of src) {\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    aS += Math.sin(d*f - t*sp) * amp;\n    iS += Math.sin(d*f*0.7 + t*sp*0.6 + 1.5) * amp;\n    a2 += Math.sin(d*f*1.3 - t*sp*0.8 + 0.7) * amp * 0.5;\n    i2 += Math.cos(d*f*0.5 + t*sp*0.4 + 2.1) * amp * 0.5;\n  }\n  const act = (aS + a2) / 12, inh = (iS + i2) / 12;\n  const bg = (Math.sin(sx*0.04+sy*0.025+t*0.8) + Math.sin(sx*0.03-sy*0.04+t*1.1))/2 + Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2 + Math.sin(sx*0.065+Math.sin(t*0.3)*sy*0.05+t*1.5)*0.15;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2) + Math.sin(dist*0.065-ang*2-t*0.9+1))*0.28;\n  const warp = Math.sin(sx*0.03+t*0.7)*Math.cos(sy*0.025-t*0.5)*0.15;\n  const raw = act - inh*0.7 + bg*0.3 + spiral + warp;\n  let rd = raw;\n  for (let i = 0; i < 3; i++) {\n    const lap = Math.sin(rd*3.5+t*(1+i*0.3))*0.12 + Math.cos(rd*2.8-t*(0.8+i*0.2))*0.08;\n    rd = Math.tanh(rd*2.2 + lap - 0.05);\n  }\n  const reaction = rd;\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5 + Math.sin(sx*0.2-t*0.8)*Math.cos(sy*0.18+t*0.6)*0.3)*1.5)*0.25;\n  const micro = Math.sin(sx*0.35+sy*0.28+t*3)*Math.cos(sx*0.22-sy*0.31-t*2.5)*0.08;\n  const blend = reaction*0.7 + cell*0.2 + micro, v = blend*0.5+0.5;\n  const pal = [[6,12,52],[12,42,88],[16,78,118],[18,118,128],[38,158,118],[120,178,72],[198,178,48],[218,138,58],[198,88,62],[148,48,72],[88,28,68],[42,18,58],[6,12,52]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const sm = f*f*(3-2*f);\n  const lum = 0.75+Math.sin(reaction*Math.PI)*0.25;\n  const edge = Math.abs(reaction)>0.65?1.2:Math.abs(reaction)>0.35?1.05:0.95;\n  const glow = Math.max(0, Math.sin(reaction*4)*0.15);\n  return [\n    Math.min(255, (pal[i][0]+(pal[i+1][0]-pal[i][0])*sm)*lum*edge + glow*60)|0,\n    Math.min(255, (pal[i][1]+(pal[i+1][1]-pal[i][1])*sm)*lum*edge + glow*40)|0,\n    Math.min(255, (pal[i][2]+(pal[i+1][2]-pal[i][2])*sm)*lum*edge + glow*80)|0\n  ];\n}",
  "score": {
    "coherence": 8,
    "harmony": 7,
    "complexity": 9,
    "density": 6,
    "total": 30,
    "reason": "The image displays coherent, flowing organic patterns with smooth color transitions (harmony), suggesting a complex mathematical or algorithmic origin (complexity). However, the high color saturation and abstract nature reduce its utility as a UI (density). The visual integrity is strong with no obvious artifacts, making it aesthetically pleasing but not functional for a minimal OS interface."
  },
  "timestamp": 1774529041470,
  "filename": "gen86_1774529041470"
}