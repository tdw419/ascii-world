{
  "code": "(x, y, t) => {\n  const primes = [2, 3, 5, 7, 11, 13, 17, 19];\n  const wx = Math.sin(x * 0.02 + t * 0.3) * 15 + Math.cos(y * 0.015 - t * 0.2) * 10;\n  const wy = Math.cos(y * 0.02 - t * 0.25) * 15 + Math.sin(x * 0.018 + t * 0.35) * 10;\n  const sx = x + wx, sy = y + wy;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08*primes[0], 2.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1*primes[1]/3, 1.7],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06*primes[2]/5, 2.3],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12*primes[3]/7, 1.5],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07*primes[4]/11, 2.6],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09*primes[5]/13, 1.9],\n    [60+Math.sin(t*0.45)*70, 40+Math.cos(t*0.65)*50, 0.11*primes[6]/17, 2.1],\n    [400+Math.cos(t*0.55)*60, 200+Math.sin(t*0.75)*40, 0.13*primes[7]/19, 1.8]\n  ];\n  let aS = 0, iS = 0;\n  for (let i = 0; i < src.length; i++) {\n    const d = Math.sqrt((sx-src[i][0])**2 + (sy-src[i][1])**2);\n    aS += Math.sin(d*src[i][2] - t*src[i][3]);\n    iS += Math.sin(d*src[i][2]*0.7 + t*src[i][3]*0.6 + 1.5);\n  }\n  const act = aS/8, inh = iS/8;\n  const bg = (Math.sin(sx*0.04+sy*0.025+t*0.8) + Math.sin(sx*0.03-sy*0.04+t*1.1))/2 + Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2;\n  const dist = Math.sqrt((sx-240)**2 + (sy-120)**2), ang = Math.atan2(sy-120, sx-240);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2))*0.4;\n  const reaction = Math.tanh((act - inh*0.7 + bg*0.3 + spiral)*2.5);\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  let blend = reaction*0.75 + cell*0.25;\n  const gx = (x % 20 < 1 || y % 20 < 1) ? 0.12 : 0;\n  const p1 = x >= 8 && x <= 155 && y >= 6 && y <= 52;\n  const p2 = x >= 163 && x <= 310 && y >= 6 && y <= 52;\n  const p3 = x >= 318 && x <= 472 && y >= 6 && y <= 52;\n  const p4 = x >= 8 && x <= 200 && y >= 58 && y <= 110;\n  const p5 = x >= 208 && x <= 472 && y >= 58 && y <= 110;\n  const isPanel = p1 || p2 || p3 || p4 || p5;\n  let panelVal = 0;\n  if (p1) {\n    const px = x - 8, py = y - 6;\n    const titleBar = py < 10;\n    const wave = Math.sin(px*0.15 + t*3)*0.5+0.5;\n    const bar = px < 8 + wave * 140 && py >= 16 && py <= 44;\n    panelVal = titleBar ? 0.7 : bar ? 0.9 : 0.05;\n  } else if (p2) {\n    const px = x - 163, py = y - 6;\n    const titleBar = py < 10;\n    const nodeY = [18, 28, 38];\n    const layerX = [10, 60, 110];\n    let nv = 0;\n    for (let li = 0; li < 3; li++) for (let ni = 0; ni < 3; ni++) {\n      const nd = Math.sqrt((px-layerX[li])**2 + (py-nodeY[ni])**2);\n      if (nd < 5) nv = Math.max(nv, 0.6 + Math.sin(t*2+li+ni)*0.4);\n    }\n    panelVal = titleBar ? 0.7 : Math.max(nv, 0);\n    for (let li = 0; li < 2; li++) for (let ni = 0; ni < 3; ni++) for (let nj = 0; nj < 3; nj++) {\n      const ax = layerX[li], bx = layerX[li+1], ay = nodeY[ni], by = nodeY[nj];\n      const steps = 12;\n      for (let s = 0; s <= steps; s++) {\n        const fx = ax + (bx-ax)*s/steps, fy = ay + (by-ay)*s/steps;\n        if (Math.abs(px-fx) < 0.8 && Math.abs(py-fy) < 0.8) panelVal = Math.max(panelVal, 0.25 + Math.sin(t*1.5+li*ni+nj)*0.15);\n      }\n    }\n  } else if (p3) {\n    const px = x - 318, py = y - 6;\n    const titleBar = py < 10;\n    const barH = 4, gap = 2;\n    let bv = 0;\n    for (let i = 0; i < primes.length && i < 6; i++) {\n      const by = 14 + i * (barH + gap);\n      const bw = (primes[i] / 19) * 140;\n      if (py >= by && py < by + barH) bv = px < bw ? 0.85 : 0;\n    }\n    panelVal = titleBar ? 0.7 : bv;\n  } else if (p4) {\n    const px = x - 8, py = y - 58;\n    const titleBar = py < 10;\n    const isPrime = primes.indexOf(py - 14) >= 0;\n    const numLine = py >= 14 && py <= 44 && (py - 14) % 5 < 4;\n    panelVal = titleBar ? 0.7 : numLine ? (isPrime ? 0.9 : 0.2) : 0;\n    if (numLine && isPrime) {\n      const tick = Math.sin(px * 0.5 + t * 4) > 0.8 ? 1.0 : 0.9;\n      panelVal = tick;\n    }\n  } else if (p5) {\n    const px = x - 208, py = y - 58;\n    const titleBar = py < 10;\n    const wave1 = Math.sin(px*0.08 + t*2)*10;\n    const wave2 = Math.cos(px*0.12 + t*1.5)*8;\n    const wave3 = Math.sin(px*0.06 - t*1.8)*12;\n    const cy1 = 22 + wave1, cy2 = 28 + wave2, cy3 = 38 + wave3;\n    let wv = 0;\n    if (Math.abs(py - cy1) < 1.5) wv = 0.8;\n    if (Math.abs(py - cy2) < 1.5) wv = 0.7;\n    if (Math.abs(py - cy3) < 1.5) wv = 0.75;\n    panelVal = titleBar ? 0.7 : wv;\n  }\n  const pBorder = isPanel && (x === 8 || x === 155 || x === 163 || x === 310 || x === 318 || x === 472 || x === 200 || x === 208 || y === 6 || y === 52 || y === 58 || y === 110 || (p1 && (y===16||y===44)) || (p2 && y===10) || (p3 && y===10) || (p4 && (y===14||y===44)) || (p5 && y===10));\n  const pBordVal = pBorder ? 0.5 : 0;\n  blend = isPanel ? panelVal : blend;\n  const v = blend*0.5+0.5;\n  const pal = [[5,2,25],[10,10,60],[0,180,255],[0,255,180],[255,0,200],[255,255,100],[255,80,120],[255,255,255]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const lum = 0.85+Math.sin(reaction*Math.PI)*0.25;\n  const neon = Math.pow(Math.abs(reaction), 0.6);\n  const boost = isPanel ? 1.1 + panelVal * 0.4 : 1.0 + neon * 0.5;\n  const bordBoost = pBordVal > 0 ? 2.5 : 1.0;\n  return [\n    Math.min(255, Math.max(0, ((pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*boost*bordBoost + gx*20 + pBordVal*80)|0)),\n    Math.min(255, Math.max(0, ((pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*boost*bordBoost + gx*40 + pBordVal*120)|0)),\n    Math.min(255, Math.max(0, ((pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*boost*bordBoost + gx*60 + pBordVal*200)|0))\n  ];\n}",
  "score": {
    "coherence": 5,
    "harmony": 4,
    "complexity": 8,
    "density": 3,
    "total": 20,
    "reason": "The image has high visual complexity with abstract patterns and gradients, but lacks coherence due to chaotic color bleeding and unclear structure. Harmony is low because the vibrant, clashing colors are visually jarring. Density is poor — it's mostly decorative with minimal functional UI elements — making it unsuitable for a minimal OS interface."
  },
  "timestamp": 1774620807468,
  "filename": "gen1822_1774620807468"
}