{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const wx = Math.sin(x * 0.02 + t * 0.3) * 12 + Math.cos(y * 0.015 - t * 0.2) * 8;\n  const wy = Math.cos(y * 0.02 - t * 0.25) * 12 + Math.sin(x * 0.018 + t * 0.35) * 8;\n  const sx = x + wx, sy = y + wy;\n  const primes = [2, 3, 5, 7, 11, 13];\n  const src = [\n    [180+Math.sin(t*0.6)*80, 90+Math.cos(t*0.4)*40, 0.08, 2.0],\n    [340+Math.cos(t*0.5)*70, 150+Math.sin(t*0.7)*40, 0.1, 1.7],\n    [260+Math.sin(t*0.3+2)*100, 120+Math.cos(t*0.5+1)*60, 0.06, 2.3],\n    [140+Math.cos(t*0.8+1)*60, 170+Math.sin(t*0.6+3)*40, 0.12, 1.5],\n    [370+Math.sin(t*0.4+4)*60, 70+Math.cos(t*0.9+2)*50, 0.07, 2.6],\n    [250+Math.cos(t*0.35+5)*80, 190+Math.sin(t*0.55)*50, 0.09, 1.9]\n  ];\n  let aS = 0, iS = 0;\n  for (let i = 0; i < 6; i++) {\n    const [sX, sY, f, sp] = src[i];\n    const d = Math.sqrt((sx - sX) ** 2 + (sy - sY) ** 2);\n    const pf = f * primes[i] * 0.12;\n    aS += Math.sin(d * pf - t * sp);\n    iS += Math.sin(d * pf * 0.7 + t * sp * 0.6 + 1.5);\n  }\n  const act = aS / 6, inh = iS / 6;\n  const bg = (Math.sin(sx * 0.08 + sy * 0.075 + t * 0.8) + Math.sin(sx * 0.15 - sy * 0.28 + t * 1.1)) / 2\n    + Math.sin((sx + sy) * 0.12 + t * 2.5) * 0.2\n    + Math.sin((sx - sy) * 0.13 - t * 2) * 0.2;\n  const dist = Math.sqrt((sx - cx) ** 2 + (sy - cy) ** 2), ang = Math.atan2(sy - cy, sx - cx);\n  const spiral = (Math.sin(dist * 0.05 - ang * 3 - t * 1.5) + Math.sin(dist * 0.04 + ang * 4 - t * 1.2)) * 0.4;\n  const reaction = Math.tanh((act - inh * 0.7 + bg * 0.3 + spiral) * 2.4);\n  const cell = Math.tanh((Math.sin(sx * 0.15) * Math.sin(sy * 0.15) + Math.sin((sx + sy) * 0.1 + t) * 0.5) * 1.5) * 0.3;\n  let uiGlow = 0, uiHue = 0;\n  const inPanel = sx > 4 && sx < 95 && sy > 4 && sy < 232;\n  if (inPanel) {\n    const header = sy > 4 && sy < 18;\n    if (header) uiGlow = 0.9, uiHue = 0;\n    else {\n      const row = ((sy - 22) / 18) | 0;\n      if (row >= 0 && row < 11) {\n        const ly = 22 + row * 18, lx = 8;\n        const isBorder = sy < ly + 1 || sy > ly + 16 || sx < lx || sx > 91;\n        if (isBorder) uiGlow = 0.25, uiHue = 0.5;\n        else {\n          const pVal = primes[Math.min(row, 5)];\n          const barW = 40 + pVal * 4 + Math.sin(t * 0.5 + row) * 8;\n          const inBar = sx > lx + 4 && sx < lx + barW && sy > ly + 5 && sy < ly + 12;\n          if (inBar) { uiGlow = 0.7 + Math.sin(t + row) * 0.2; uiHue = row / 11; }\n          const nodeX = lx + 55 + (row % 3) * 12, nodeY = ly + 3 + ((row / 3) | 0) * 6;\n          const nd = Math.sqrt((sx - nodeX) ** 2 + (sy - nodeY) ** 2);\n          if (nd < 3) { uiGlow = Math.max(uiGlow, 0.8 + Math.sin(t * pVal * 0.2) * 0.2); uiHue = pVal / 13; }\n        }\n      }\n    }\n  }\n  const inStatus = sx > 100 && sx < 475 && sy > 220 && sy < 236;\n  if (inStatus) {\n    const isBorder = sy < 222 || sy > 233 || sx < 101 || sx > 474;\n    if (isBorder) { uiGlow = 0.3; uiHue = 0.5; }\n    else {\n      const segs = 6, segW = 60;\n      const seg = ((sx - 105) / segW) | 0;\n      if (seg >= 0 && seg < segs) {\n        const fill = 0.3 + Math.abs(Math.sin(t * 0.4 + seg * 1.1)) * 0.65;\n        const fillX = 105 + seg * segW;\n        if (sx > fillX && sx < fillX + segW * fill) { uiGlow = 0.85; uiHue = seg / segs; }\n      }\n    }\n  }\n  const gridPhase = (sx * 0.05 + sy * 0.03 + t * 0.1);\n  const gridMajor = (Math.abs(((sx + 0.5) % 60) - 0.5) < 0.6 || Math.abs(((sy + 0.5) % 40) - 0.5) < 0.6) ? 0.06 : 0;\n  const gridMinor = (Math.abs(((sx + 0.5) % 15) - 0.5) < 0.3 || Math.abs(((sy + 0.5) % 10) - 0.5) < 0.3) ? 0.02 : 0;\n  const scanline = Math.sin(sy * 0.8 + t * 3) > 0.95 ? 0.08 : 0;\n  const baseBlend = reaction * 0.65 + cell * 0.2;\n  const uiBlend = uiGlow * 0.9;\n  const totalBlend = baseBlend * (1 - Math.min(1, uiGlow * 1.5)) + uiBlend + gridMajor + gridMinor + scanline;\n  const v = Math.max(0, Math.min(1, totalBlend * 0.5 + 0.5));\n  const pal = [\n    [3, 2, 18], [8, 20, 72], [0, 90, 130], [0, 235, 190], [80, 255, 120],\n    [255, 240, 20], [255, 90, 40], [255, 40, 110], [190, 30, 255], [50, 15, 130], [3, 2, 18]\n  ];\n  const idx = v * (pal.length - 1), i = Math.min(idx | 0, pal.length - 2), f = idx - i;\n  let r = pal[i][0] + (pal[i + 1][0] - pal[i][0]);\n  return [0, 0, 0];\n}",
  "score": {
    "coherence": 10,
    "harmony": 10,
    "complexity": 1,
    "density": 1,
    "total": 22,
    "reason": "The image is a solid black framebuffer, which is perfectly coherent and harmonious with no visual artifacts. However, it contains no mathematical patterns or useful UI information, making its complexity and density extremely low."
  },
  "timestamp": 1774571772254,
  "filename": "gen903_1774571772254"
}