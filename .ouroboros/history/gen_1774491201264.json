{
  "code": "(x, y, t) => {\n  const s = [\n    [120 + Math.sin(t * 0.6) * 100, 60 + Math.cos(t * 0.4) * 40],\n    [360 + Math.cos(t * 0.5) * 110, 180 + Math.sin(t * 0.7) * 50],\n    [240 + Math.sin(t * 0.3 + 2) * 150, 120 + Math.cos(t * 0.5 + 1) * 80],\n    [80 + Math.cos(t * 0.8 + 3) * 70, 200 + Math.sin(t * 0.45 + 2) * 60],\n    [400 + Math.sin(t * 0.35 + 5) * 80, 40 + Math.cos(t * 0.55 + 4) * 30]\n  ];\n  const freqs = [0.08, 0.1, 0.06, 0.09, 0.07], speeds = [2, 1.7, 2.3, 1.9, 2.6];\n  let activator = 0, inhibitor = 0;\n  for (let i = 0; i < 5; i++) {\n    const d = Math.sqrt((x - s[i][0]) ** 2 + (y - s[i][1]) ** 2);\n    activator += Math.sin(d * freqs[i] - t * speeds[i]);\n    inhibitor += Math.sin(d * freqs[i] * 0.7 + t * speeds[i] * 0.4 + i);\n  }\n  activator /= 5; inhibitor /= 5;\n  const wx = Math.sin(x * 0.04 + y * 0.02 + t * 0.8) * 0.4 + Math.sin(x * 0.02 - y * 0.04 + t * 1.1) * 0.4;\n  const wy = Math.sin(x * 0.03 + y * 0.05 - t * 0.6) * 0.4 + Math.sin(-x * 0.05 + y * 0.03 + t * 0.9) * 0.4;\n  const wx2 = Math.sin((x + y) * 0.12 + t * 2.5) * 0.2, wy2 = Math.sin((x - y) * 0.1 - t * 2) * 0.2;\n  const dx = x + wx * 30 + wx2 * 15, dy = y + wy * 30 + wy2 * 15;\n  const cx = 240, cy = 120, dist = Math.sqrt((dx - cx) ** 2 + (dy - cy) ** 2);\n  const ang = Math.atan2(dy - cy, dx - cx);\n  const rad1 = Math.sin(dist * 0.04 - t * 1.5), ang1 = Math.sin(ang * 5 + t * 0.4);\n  const rad2 = Math.sin(dist * 0.06 + t * 0.8), ang2 = Math.sin(ang * 3 - t * 0.6);\n  const micro = Math.sin(dx * 0.15 + dy * 0.1 + t * 3) * 0.08 + Math.sin(dx * 0.1 - dy * 0.15 - t * 2.2) * 0.08;\n  const reaction1 = Math.tanh((activator - inhibitor * 0.75 + micro) * 2.8);\n  const reaction2 = Math.tanh((rad1 + ang1 * 0.4 + rad2 * 0.3 + ang2 * 0.2) * 2.2);\n  const feedback = Math.sin(reaction1 * Math.PI * 2 + t * 0.5) * 0.15;\n  const blend = reaction1 * 0.55 + reaction2 * 0.35 + feedback;\n  const v = blend * 0.5 + 0.5;\n  const pal = [\n    [8, 12, 48], [15, 55, 95], [25, 115, 130], [180, 195, 90],\n    [220, 165, 55], [195, 85, 50], [120, 35, 65], [45, 18, 55], [8, 12, 48]\n  ];\n  const idx = v * (pal.length - 1), i = Math.min(idx | 0, pal.length - 2), f = idx - i;\n  const edge = Math.abs(reaction1) < 0.3 ? 1 : 0.92;\n  const lum = (0.82 + Math.sin(blend * Math.PI * 1.5) * 0.18) * edge;\n  const glow = Math.max(0, 1 - dist / 250) * 0.12 * (0.5 + 0.5 * Math.sin(t * 1.2));\n  return [\n    Math.min(255, ((pal[i][0] + (pal[i+1][0] - pal[i][0]) * f) * lum + glow * 60) | 0),\n    Math.min(255, ((pal[i][1] + (pal[i+1][1] - pal[i][1]) * f) * lum + glow * 80) | 0),\n    Math.min(255, ((pal[i][2] + (pal[i+1][2] - pal[i][2]) * f) * lum + glow * 100) | 0)\n  ];\n}",
  "score": {
    "coherence": 8,
    "harmony": 7,
    "complexity": 9,
    "density": 5,
    "total": 29,
    "reason": "The image displays coherent, flowing organic patterns with strong color harmony using a warm-cool gradient. Its complexity is high due to intricate, non-repeating fractal-like forms. However, the density is low for a UI — it's mostly decorative with minimal functional information — making it visually rich but practically sparse for an operating system interface."
  },
  "timestamp": 1774491201264,
  "filename": "gen10_1774491201264"
}