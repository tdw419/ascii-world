{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const w1x = Math.sin(x*0.02+t*0.3)*15 + Math.cos(y*0.015-t*0.2)*10;\n  const w1y = Math.cos(y*0.02-t*0.25)*15 + Math.sin(x*0.018+t*0.35)*10;\n  const w2x = Math.sin((x+y)*0.012-t*0.15)*8 + Math.cos((x-y)*0.01+t*0.22)*6;\n  const w2y = Math.cos((x-y)*0.013+t*0.18)*8 + Math.sin((x+y)*0.011-t*0.28)*6;\n  const sx = x+w1x+w2x, sy = y+w1y+w2y;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0, 1.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7, 0.85],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3, 1.1],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5, 0.9],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6, 1.05],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9, 0.95],\n    [80+Math.sin(t*0.45+3)*70, 40+Math.cos(t*0.65+1)*55, 0.11, 2.1, 0.88],\n    [400+Math.cos(t*0.55+2)*85, 200+Math.sin(t*0.75+4)*65, 0.055, 2.8, 1.12]\n  ];\n  let aS=0, iS=0, aS2=0, iS2=0;\n  for (const [sX,sY,f,sp,w] of src) {\n    const d = Math.sqrt((sx-sX)**2+(sy-sY)**2);\n    const a = Math.sin(d*f-t*sp), b = Math.sin(d*f*0.7+t*sp*0.6+1.5);\n    aS += a*w; iS += b*w;\n    const d2 = Math.sqrt((sx-sY)**2+(sy-sX)**2);\n    aS2 += Math.sin(d2*f*0.8-t*sp*1.1)*w;\n    iS2 += Math.sin(d2*f*0.5+t*sp*0.4+2.8)*w;\n  }\n  const act = aS/8, inh = iS/8, act2 = aS2/8, inh2 = iS2/8;\n  const bg = (Math.sin(sx*0.04+sy*0.025+t*0.8)+Math.sin(sx*0.03-sy*0.04+t*1.1))/2\n    + Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2\n    + Math.sin(sx*0.065+sy*0.045-t*1.6)*0.15;\n  const dist = Math.sqrt((sx-cx)**2+(sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const sp1 = Math.sin(dist*0.05-ang*3-t*1.5), sp2 = Math.sin(dist*0.04+ang*4-t*1.2);\n  const spiral = (sp1+sp2)*0.4 + Math.sin(dist*0.08-ang*2+t*0.9)*0.2;\n  const moire = Math.sin(dist*0.12)*Math.sin(ang*5+t*0.7)*0.3;\n  const ripple = Math.sin(dist*0.15-t*3)*Math.exp(-dist*0.008)*0.5;\n  const r1 = Math.tanh((act-inh*0.7+bg*0.3+spiral)*2.2);\n  const r2 = Math.tanh((act2-inh2*0.6-bg*0.2+moire)*1.8);\n  const cross = Math.tanh((r1*r2+moire+ripple)*1.6);\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15)+Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  const voronoi = (() => {\n    let md = 99, sd = 99;\n    for (const [sX,sY] of src) {\n      const d = Math.sqrt((sx-sX)**2+(sy-sY)**2);\n      if(d<md){sd=md;md=d;}else if(d<sd)sd=d;\n    }\n    return Math.tanh((sd-md)*0.15)*0.4;\n  })();\n  const blend = r1*0.4+r2*0.2+cross*0.15+cell*0.15+voronoi*0.1;\n  const v = blend*0.5+0.5;\n  const pal = [[5,12,48],[12,45,82],[18,85,108],[25,130,125],[160,165,75],[195,140,60],[185,90,65],[145,50,70],[90,25,60],[40,15,55],[5,12,48]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const sm = f*f*(3-2*f);\n  const lum = 0.85+Math.sin(r1*Math.PI)*0.15+cross*0.08;\n  const edge = Math.abs(r1)>0.65||Math.abs(cross)>0.6?1.12:1.0;\n  const glow = Math.exp(-Math.abs(blend)*2)*0.3;\n  return [\n    Math.min(255, ((pal[i][0]+(pal[i+1][0]-pal[i][0])*sm)*lum*edge + glow*60)|0),\n    Math.min(255, ((pal[i][1]+(pal[i+1][1]-pal[i][1])*sm)*lum*edge + glow*40)|0),\n    Math.min(255, ((pal[i][2]+(pal[i+1][2]-pal[i][2])*sm)*lum*edge + glow*80)|0)\n  ];\n}",
  "score": {
    "coherence": 9,
    "harmony": 8,
    "complexity": 9,
    "density": 3,
    "total": 29,
    "reason": "The image has high coherence with smooth, flowing gradients and no visual artifacts. Its harmony is strong with a balanced, warm-cool color palette that is aesthetically pleasing. Complexity is high due to the intricate, organic patterns. However, density is low — it's purely decorative with no functional UI elements, making it unsuitable for conveying information in a minimal OS context."
  },
  "timestamp": 1774492148398,
  "filename": "gen45_1774492148398"
}