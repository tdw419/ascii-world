{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const w1x = Math.sin(x*0.018+t*0.25)*18 + Math.cos(y*0.013-t*0.18)*12;\n  const w1y = Math.cos(y*0.02-t*0.22)*18 + Math.sin(x*0.015+t*0.3)*12;\n  const sx = x + w1x, sy = y + w1y;\n  const w2x = Math.sin(sx*0.025+sy*0.01+t*0.4)*8 + Math.cos(sy*0.03-t*0.35)*6;\n  const w2y = Math.cos(sy*0.022-sx*0.012+t*0.45)*8 + Math.sin(sx*0.028+t*0.38)*6;\n  const wx = sx + w2x, wy = sy + w2y;\n  const src = [\n    [160+Math.sin(t*0.5)*110, 70+Math.cos(t*0.35)*55, 0.07, 2.2, 0.6],\n    [320+Math.cos(t*0.45)*100, 170+Math.sin(t*0.65)*50, 0.09, 1.8, 0.5],\n    [240+Math.sin(t*0.28+2)*150, 110+Math.cos(t*0.48+1)*95, 0.055, 2.5, 0.7],\n    [100+Math.cos(t*0.75+1)*90, 190+Math.sin(t*0.55+3)*65, 0.11, 1.4, 0.4],\n    [370+Math.sin(t*0.38+4)*95, 50+Math.cos(t*0.85+2)*75, 0.065, 2.8, 0.65],\n    [240+Math.cos(t*0.32+5)*130, 210+Math.sin(t*0.5)*85, 0.08, 2.0, 0.55],\n    [60+Math.sin(t*0.6+3)*70, 120+Math.cos(t*0.7+1)*80, 0.1, 1.6, 0.45],\n    [400+Math.cos(t*0.42+2)*80, 130+Math.sin(t*0.58+4)*70, 0.075, 2.4, 0.6]\n  ];\n  let aS = 0, iS = 0, feed = 0;\n  for (const [sX, sY, f, sp, w] of src) {\n    const d = Math.sqrt((wx-sX)**2 + (wy-sY)**2);\n    const wave = Math.sin(d*f - t*sp);\n    const env = Math.exp(-d*0.003) * w;\n    aS += wave * env;\n    iS += Math.sin(d*f*0.65 + t*sp*0.55 + 1.8) * env * 0.8;\n    feed += Math.sin(d*f*0.4 - t*sp*0.3 + 2.5) * env * 0.5;\n  }\n  const n8 = Math.sin(wx*0.04+wy*0.025+t*0.7) + Math.sin(wx*0.03-wy*0.035+t*1.0);\n  const n16 = Math.sin(wx*0.08+wy*0.06-t*1.3)*0.5 + Math.sin(wx*0.07-wy*0.09+t*1.6)*0.5;\n  const n32 = Math.sin(wx*0.16+wy*0.13+t*2.2)*0.25 + Math.sin(wx*0.14-wy*0.18-t*2.5)*0.25;\n  const fbm = (n8 + n16*0.5 + n32*0.25) / 1.75;\n  const dist = Math.sqrt((wx-cx)**2 + (wy-cy)**2), ang = Math.atan2(wy-cy, wx-cx);\n  const sp1 = Math.sin(dist*0.045-ang*3.5-t*1.3);\n  const sp2 = Math.sin(dist*0.035+ang*4.5-t*1.0);\n  const sp3 = Math.sin(dist*0.06-ang*2-t*1.8)*0.5;\n  const spiral = (sp1 + sp2 + sp3) / 2.5;\n  const pulse = Math.sin(dist*0.08-t*3)*Math.exp(-dist*0.005)*0.4;\n  const cellA = Math.sin(wx*0.16)*Math.sin(wy*0.16);\n  const cellB = Math.sin((wx+wy)*0.11+t*0.8)*Math.sin((wx-wy)*0.09-t*0.6);\n  const cell = (cellA + cellB*0.6) / 1.6;\n  const act = aS/8, inh = iS/8, fd = feed/8;\n  const rd = Math.tanh((act*1.2 - inh*0.8 + fd*0.4 + fbm*0.25 + spiral*0.35 + pulse)*2.0);\n  const cellRd = Math.tanh((cell + Math.sin(rd*Math.PI*2)*0.3)*1.8);\n  const blend = rd*0.6 + cellRd*0.4;\n  const v = blend*0.5+0.5;\n  const edge = 1.0 - Math.pow(Math.abs(blend), 8)*0.15;\n  const glow = Math.pow(Math.max(0, blend), 3)*0.3;\n  const pal = [\n    [5,12,52],[10,35,85],[12,65,105],[18,110,125],\n    [45,155,145],[120,185,130],[200,195,75],\n    [225,150,55],[215,95,65],[180,50,75],\n    [120,25,65],[60,15,55],[5,12,52]\n  ];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const lum = 0.82 + Math.sin(rd*Math.PI)*0.18;\n  const r = (pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*edge + glow*180;\n  const g = (pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*edge + glow*140;\n  const b = (pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*edge + glow*80;\n  return [Math.min(255, r)|0, Math.min(255, g)|0, Math.min(255, b)|0];\n}",
  "score": {
    "coherence": 7,
    "harmony": 6,
    "complexity": 9,
    "density": 4,
    "total": 26,
    "reason": "The image has high visual complexity with swirling, organic patterns, but lacks coherence due to abstract, non-UI elements. Color harmony is moderate with warm/cool contrast, but not optimized for readability. Density is low — it's decorative, not informational — making it unsuitable for a functional OS UI."
  },
  "timestamp": 1774514954964,
  "filename": "gen677_1774514954964"
}