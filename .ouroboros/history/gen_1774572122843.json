{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const wx = Math.sin(x*0.02+t*0.3)*15 + Math.cos(y*0.015-t*0.2)*10;\n  const wy = Math.cos(y*0.02-t*0.25)*15 + Math.sin(x*0.018+t*0.35)*10;\n  const sx = x+wx, sy = y+wy;\n  const primes = [2,3,5,7,11,13,17,19];\n  const src = primes.map((p,i) => {\n    const a = (i/primes.length)*Math.PI*2+t*0.15;\n    const r = 40+p*8+Math.sin(t*0.4+i)*20;\n    return [240+Math.cos(a)*r+Math.sin(t*(0.3+i*0.05))*30, 120+Math.sin(a)*r+Math.cos(t*(0.25+i*0.07))*25, 0.03+p*0.005, 1.2+Math.sin(i*1.5)*0.6];\n  });\n  let aS=0, iS=0;\n  for(const[sX,sY,f,sp]of src){const d=Math.sqrt((sx-sX)**2+(sy-sY)**2);aS+=Math.sin(d*f-t*sp);iS+=Math.sin(d*f*0.7+t*sp*0.6+1.5);}\n  const act=aS/src.length, inh=iS/src.length;\n  const bg=(Math.sin(sx*0.04+sy*0.025+t*0.8)+Math.sin(sx*0.03-sy*0.04+t*1.1))/2+Math.sin((sx+sy)*0.12+t*2.5)*0.2+Math.sin((sx-sy)*0.1-t*2)*0.2;\n  const dist=Math.sqrt((sx-cx)**2+(sy-cy)**2), ang=Math.atan2(sy-cy,sx-cx);\n  const spiral=(Math.sin(dist*0.05-ang*3-t*1.5)+Math.sin(dist*0.04+ang*4-t*1.2))*0.4;\n  const primeMod=Math.sin(x*0.1)*Math.cos(y*0.07)*Math.sin(t*0.5)*0.15;\n  const primeGlow=Math.sin(dist*0.08-t*1.8)*Math.cos(ang*primes[(Math.floor(t*0.5)%primes.length)])*0.1;\n  const reaction=Math.tanh((act-inh*0.7+bg*0.3+spiral+primeMod+primeGlow)*2.2);\n  const cell=Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15)+Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  const px=Math.floor(x/120), py=Math.floor(y/60), lx=x%120, ly=y%60;\n  const gridLine=((lx<1||ly<1)?0.15:0)+((x%120<1||y%60<1)?0.25:0);\n  const topBar=ly<10?1:0, barFill=Math.sin(t*2+px*1.2)*0.5+0.5;\n  const barActive=topBar&&(lx/120)<barFill?1:0;\n  const layerStrip=(ly>22&&ly<25)?1:0;\n  const layerAct=layerStrip?Math.max(0,Math.sin(x*0.15+t*3)*Math.cos(x*0.08-t*2)):0;\n  const dataLine=(ly>28&&ly<30)?1:0;\n  const dataFlicker=dataLine?(((x*7+y*13+Math.floor(t*8)*3)%17)<6?0.5:0):0;\n  const sparkLine=(ly>35&&ly<50)?1:0;\n  const sparkVal=sparkLine?Math.sin(lx*0.2+t*4+py*2)*0.4+Math.sin(lx*0.5-t*6)*0.2+Math.cos(lx*0.1+t*2)*0.3:0;\n  const sparkActive=sparkLine&&(ly-35)<(sparkVal*0.5+0.5)*15?1:0;\n  const miniBar=(ly>52&&ly<58)?1:0;\n  const miniH=miniBar?Math.abs(Math.sin(lx*0.3+t*1.5+px))*(ly-52)/6:0;\n  const miniActive=miniBar&&miniH>0.7?1:0;\n  const blend=reaction*0.55+cell*0.12+gridLine*0.06+barActive*0.08+layerAct*0.06+dataFlicker*0.04+sparkActive*0.05+miniActive*0.04;\n  const v=blend*0.5+0.5;\n  const pal=[[2,3,18],[4,12,55],[8,35,110],[0,160,240],[0,240,190],[160,0,240],[240,40,140],[240,190,40],[255,255,255]];\n  const idx=v*(pal.length-1), i=Math.min(idx|0,pal.length-2), f=idx-i;\n  const lum=0.82+Math.sin(reaction*Math.PI)*0.18, edge=Math.abs(reaction)>0.7?1.35:1.0;\n  const neonGlow=Math.max(0,Math.abs(reaction)-0.5)*2.5;\n  let r=(pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*edge+neonGlow*25;\n  let g=(pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*edge+neonGlow*15;\n  let b=(pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*edge+neonGlow*45;\n  if(barActive){r=0;g=255;b=180;}\n  if(layerStrip){r+=layerAct*80;g+=layerAct*180;b+=layerAct*255;}\n  if(dataFlicker>0){r+=30;g+=70;b+=90;}\n  if(sparkActive){r+=20;g+=200;b+=255;}\n  if(miniActive){r+=240;g+=40;b+=140;}\n  if(gridLine>0.2){r+=15;g+=40;b+=80;}\n  return[Math.min(255,r)|0,Math.min(255,g)|0,Math.min(255,b)|0];\n}",
  "score": {
    "coherence": 5,
    "harmony": 6,
    "complexity": 9,
    "density": 3,
    "total": 23,
    "reason": "The image has high visual complexity with fractal-like patterns and vibrant color transitions, but suffers from low coherence due to visible horizontal artifacts and inconsistent structure. Color harmony is decent with complementary neon tones, but the density is very low for a UI, as it lacks functional information or layout. This is more of an artistic or demo render than a practical interface."
  },
  "timestamp": 1774572122842,
  "filename": "gen913_1774572122842"
}