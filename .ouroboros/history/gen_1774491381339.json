{
  "code": "(x, y, t) => {\n  const s = (i, j, k, l, m) => [i + Math.sin(t * j + k) * l, m + Math.cos(t * j * 0.8 + k * 1.3) * l * 0.6];\n  const p = [\n    s(80, 0.5, 0, 90, 60), s(400, 0.4, 1.5, 80, 180),\n    s(240, 0.35, 3, 110, 120), s(160, 0.6, 4.5, 70, 200),\n    s(360, 0.45, 2.8, 85, 50), s(120, 0.55, 5.2, 60, 140)\n  ];\n  const d = p.map(q => Math.sqrt((x - q[0]) ** 2 + (y - q[1]) ** 2));\n  const f = [0.07, 0.09, 0.055, 0.085, 0.065, 0.1];\n  const sp = [2.1, 1.8, 2.5, 1.5, 2.8, 1.3];\n  const w = d.map((v, i) => Math.sin(v * f[i] - t * sp[i]));\n  const w2 = d.map((v, i) => Math.cos(v * f[i] * 1.3 + t * sp[i] * 0.7));\n  const a1 = w.reduce((a, b) => a + b, 0) / w.length;\n  const a2 = w2.reduce((a, b) => a + b, 0) / w2.length;\n  const g1 = Math.sin(x * 0.04 + y * 0.025 + t * 0.6);\n  const g2 = Math.sin(x * 0.025 - y * 0.04 + t * 0.9);\n  const g3 = Math.sin((x + y) * 0.08 + t * 1.5) * 0.3;\n  const g4 = Math.sin((x - y) * 0.06 - t * 1.2) * 0.3;\n  const inh = (g1 + g2) * 0.5 + g3 + g4;\n  const cx = 240, cy = 120;\n  const dist = Math.sqrt((x - cx) ** 2 + (y - cy) ** 2);\n  const ang = Math.atan2(y - cy, x - cx);\n  const r1 = Math.sin(dist * 0.035 - t * 1.2);\n  const r2 = Math.sin(ang * 8 + t * 0.3);\n  const r3 = Math.sin(ang * 3 - dist * 0.02 + t * 0.8) * 0.4;\n  const spiral = (r1 + r2 * 0.4 + r3) * 0.6;\n  const rx = (x % 40 - 20) / 20, ry = (y % 40 - 20) / 20;\n  const cell = Math.sin(rx * 3.14) * Math.sin(ry * 3.14) * 0.2;\n  const feed = a1 * 0.45 + spiral * 0.25 + cell;\n  const kill = (a2 + inh * 0.6) * 0.4;\n  const reaction = feed - kill;\n  const react2 = Math.tanh(reaction * 2.2);\n  const detail = Math.sin(reaction * 4 + t) * Math.sin(reaction * 2.5 - t * 0.7);\n  const edge = (1 - react2 * react2) * Math.abs(detail) * 0.6;\n  const v = Math.tanh((react2 * 0.7 + edge) * 1.8) * 0.5 + 0.5;\n  const h = t * 0.03;\n  const pal = [\n    [8, 15, 58], [15, 60, 120], [20, 140, 135],\n    [180, 200, 80], [230, 160, 50], [200, 80, 60],\n    [140, 40, 90], [60, 20, 100], [8, 15, 58]\n  ];\n  const idx = v * (pal.length - 1);\n  const i = Math.min(idx | 0, pal.length - 2);\n  const f2 = idx - i;\n  const sf = idx * 0.8;\n  const si = Math.min(sf | 0, pal.length - 2);\n  const sf2 = sf - si;\n  const r = pal[i][0] + (pal[i + 1][0] - pal[i][0]) * f2;\n  const g = pal[si][1] + (pal[si + 1][1] - pal[si][1]) * sf2;\n  const b = pal[i][2] + (pal[i + 1][2] - pal[i][2]) * f2;\n  const lum = 0.88 + Math.sin(reaction * Math.PI * 1.5) * 0.12;\n  return [\n    Math.min(255, Math.max(0, r * lum)) | 0,\n    Math.min(255, Math.max(0, g * lum)) | 0,\n    Math.min(255, Math.max(0, b * lum)) | 0\n  ];\n}",
  "score": {
    "coherence": 7,
    "harmony": 6,
    "complexity": 9,
    "density": 4,
    "total": 26,
    "reason": "The image has high complexity with intricate, organic patterns and a central star-like feature, but lacks coherence due to inconsistent shapes and a somewhat chaotic layout. Harmony is moderate with a pleasing color palette, but the density is low — it's mostly abstract noise with minimal UI utility. This would be visually interesting but not practical for a functional OS interface."
  },
  "timestamp": 1774491381338,
  "filename": "gen16_1774491381338"
}