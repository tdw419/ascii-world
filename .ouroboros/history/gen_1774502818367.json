{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  let wx = 0, wy = 0, sx = x, sy = y;\n  for (let i = 0; i < 3; i++) {\n    wx += Math.sin(sx * 0.02 + t * 0.3 + i * 1.7) * 12 / (i + 1) + Math.cos(sy * 0.018 - t * 0.25 + i * 2.1) * 8 / (i + 1);\n    wy += Math.cos(sy * 0.02 - t * 0.28 + i * 1.3) * 12 / (i + 1) + Math.sin(sx * 0.016 + t * 0.32 + i * 1.9) * 8 / (i + 1);\n    sx = x + wx; sy = y + wy;\n  }\n  const src = [\n    [120+Math.sin(t*0.4)*90, 60+Math.cos(t*0.35)*45, 0.09, 2.1, 1.0],\n    [360+Math.cos(t*0.45)*80, 80+Math.sin(t*0.55)*55, 0.11, 1.8, 0.8],\n    [200+Math.sin(t*0.3+2)*110, 140+Math.cos(t*0.5+1)*70, 0.07, 2.5, 1.2],\n    [320+Math.cos(t*0.6+1)*70, 180+Math.sin(t*0.4+3)*50, 0.13, 1.6, 0.9],\n    [80+Math.sin(t*0.5+4)*60, 160+Math.cos(t*0.7+2)*60, 0.08, 2.8, 1.1],\n    [400+Math.cos(t*0.35+5)*50, 200+Math.sin(t*0.45)*70, 0.1, 2.0, 0.7],\n    [240+Math.sin(t*0.25+3)*100, 40+Math.cos(t*0.6+4)*30, 0.065, 2.3, 1.3],\n    [180+Math.cos(t*0.55+2)*75, 220+Math.sin(t*0.3+5)*40, 0.12, 1.7, 0.85]\n  ];\n  let aS = 0, iS = 0, cross = 0;\n  for (let j = 0; j < src.length; j++) {\n    const [sX, sY, f, sp, w] = src[j];\n    const d = Math.sqrt((sx - sX) ** 2 + (sy - sY) ** 2);\n    aS += Math.sin(d * f - t * sp) * w;\n    iS += Math.sin(d * f * 0.65 + t * sp * 0.55 + 1.5) * w;\n    for (let k = j + 1; k < src.length; k++) {\n      const [sX2, sY2, f2, sp2] = src[k];\n      const d2 = Math.sqrt((sx - sX2) ** 2 + (sy - sY2) ** 2);\n      cross += Math.sin((d * f - t * sp) + (d2 * f2 - t * sp2)) * 0.08;\n    }\n  }\n  const tw = src.reduce((s, v) => s + v[4], 0);\n  aS /= tw; iS /= tw;\n  const dist = Math.sqrt((sx - cx) ** 2 + (sy - cy) ** 2);\n  const ang = Math.atan2(sy - cy, sx - cx);\n  const spiral1 = Math.sin(dist * 0.045 - ang * 3.5 - t * 1.4) * 0.35;\n  const spiral2 = Math.sin(dist * 0.035 + ang * 5 - t * 0.9 + 2.0) * 0.25;\n  const spiral3 = Math.sin(dist * 0.06 - ang * 2 - t * 2.1 + 4.0) * 0.15;\n  const bg1 = Math.sin(sx * 0.035 + sy * 0.028 + t * 0.7);\n  const bg2 = Math.sin(sx * 0.025 - sy * 0.04 + t * 0.95);\n  const bg3 = Math.sin((sx + sy) * 0.1 + t * 2.2) * 0.3;\n  const bg4 = Math.sin((sx - sy) * 0.08 - t * 1.8) * 0.25;\n  const bg = (bg1 + bg2) / 2 + bg3 + bg4;\n  const voronoi = (() => {\n    let md = 99, md2 = 99;\n    for (let i = 0; i < 6; i++) {\n      const vx = 80 + (i % 3) * 160 + Math.sin(t * 0.3 + i * 2.1) * 40;\n      const vy = 60 + ((i / 3) | 0) * 120 + Math.cos(t * 0.25 + i * 1.7) * 30;\n      const d = Math.sqrt((sx - vx) ** 2 + (sy - vy) ** 2);\n      if (d < md) { md2 = md; md = d; } else if (d < md2) { md2 = d; }\n    }\n    return Math.sin(md2 * 0.15 - md * 0.1 + t * 0.5) * 0.3;\n  })();\n  const feedback = Math.tanh(aS * 1.8 - iS * 1.2) * 0.6;\n  const feedforward = Math.tanh(aS * aS - iS * iS * 0.5) * 0.4;\n  const rd = feedback + feedforward;\n  const total = rd * 0.45 + bg * 0.2 + (spiral1 + spiral2 + spiral3) * 0.2 + cross * 0.08 + voronoi * 0.07;\n  const v = Math.tanh(total * 2.0) * 0.5 + 0.5;\n  const pal = [\n    [5, 10, 42], [10, 35, 75], [15, 72, 100], [20, 118, 120],\n    [80, 170, 140], [180, 200, 120], [230, 195, 80], [220, 150, 65],\n    [200, 95, 75], [165, 50, 80], [100, 25, 70], [40, 12, 55], [5, 10, 42]\n  ];\n  const idx = v * (pal.length - 1);\n  const i = Math.min(idx | 0, pal.length - 2);\n  const f = idx - i;\n  const lum = 0.85 + Math.sin(rd * Math.PI * 1.5) * 0.15;\n  const glow = Math.exp(-Math.abs(rd) * 0.8) * 0.3;\n  const edge = Math.abs(rd) > 0.55 ? 1.2 : 1.0;\n  const br = 1.0 + glow;\n  return [\n    Math.min(255, ((pal[i][0] + (pal[i + 1][0] - pal[i][0]) * f) * lum * edge * br) | 0),\n    Math.min(255, ((pal[i][1] + (pal[i + 1][1] - pal[i][1]) * f) * lum * edge * br) | 0),\n    Math.min(255, ((pal[i][2] + (pal[i + 1][2] - pal[i][2]) * f) * lum * edge * br) | 0)\n  ];\n}",
  "score": {
    "coherence": 7,
    "harmony": 6,
    "complexity": 9,
    "density": 3,
    "total": 25,
    "reason": "The image has high visual complexity with smooth, flowing color gradients and organic shapes, but lacks coherence in structured patterns. Color harmony is moderate with a warm-cool contrast, but the vibrancy reduces readability. Density is very low for a UI, as it conveys no functional information — suitable for a background or abstract theme, not for interactive elements."
  },
  "timestamp": 1774502818367,
  "filename": "gen348_1774502818367"
}