{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const w1x = Math.sin(x*0.018+t*0.25)*20+Math.cos(y*0.013-t*0.18)*15;\n  const w1y = Math.cos(y*0.02-t*0.22)*20+Math.sin(x*0.015+t*0.3)*15;\n  const w2x = Math.sin((x+w1x)*0.03+t*0.15)*10+Math.cos((y+w1y)*0.025-t*0.12)*8;\n  const w2y = Math.cos((y+w1y)*0.028-t*0.17)*10+Math.sin((x+w1x)*0.022+t*0.2)*8;\n  const sx = x+w1x+w2x, sy = y+w1y+w2y;\n  const src = [\n    [160+Math.sin(t*0.6)*100,80+Math.cos(t*0.4)*50,0.08,2.0,0.7],\n    [320+Math.cos(t*0.5)*100,160+Math.sin(t*0.7)*50,0.1,1.7,0.6],\n    [240+Math.sin(t*0.3+2)*140,120+Math.cos(t*0.5+1)*90,0.06,2.3,0.8],\n    [120+Math.cos(t*0.8+1)*80,180+Math.sin(t*0.6+3)*60,0.12,1.5,0.5],\n    [360+Math.sin(t*0.4+4)*90,60+Math.cos(t*0.9+2)*70,0.07,2.6,0.9],\n    [240+Math.cos(t*0.35+5)*120,200+Math.sin(t*0.55)*80,0.09,1.9,0.65],\n    [80+Math.sin(t*0.45+3)*110,40+Math.cos(t*0.65+2)*60,0.11,1.8,0.75],\n    [400+Math.cos(t*0.55+1)*70,220+Math.sin(t*0.75+4)*50,0.065,2.4,0.85]\n  ];\n  let act = 0, inh = 0;\n  for (const [sX,sY,f,sp,w] of src) {\n    const d = Math.sqrt((sx-sX)**2+(sy-sY)**2);\n    act += w*Math.sin(d*f-t*sp);\n    inh += w*Math.sin(d*f*0.65+t*sp*0.55+1.2);\n  }\n  act /= src.length; inh /= src.length;\n  const bg1 = (Math.sin(sx*0.04+sy*0.025+t*0.8)+Math.sin(sx*0.03-sy*0.04+t*1.1))/2;\n  const bg2 = (Math.sin(sx*0.08+sy*0.06-t*1.3)+Math.sin(sx*0.07-sy*0.09+t*1.6))/2*0.5;\n  const bg3 = (Math.sin(sx*0.16+sy*0.12+t*2.0)+Math.sin(sx*0.14-sy*0.18-t*2.3))/2*0.25;\n  const bg = bg1+bg2+bg3;\n  const dist = Math.sqrt((sx-cx)**2+(sy-cy)**2);\n  const ang = Math.atan2(sy-cy,sx-cx);\n  const sp1 = Math.sin(dist*0.05-ang*3-t*1.5)*Math.exp(-dist*0.003);\n  const sp2 = Math.sin(dist*0.04+ang*4-t*1.2)*Math.exp(-dist*0.004);\n  const rings = Math.sin(dist*0.12-t*0.8)*Math.exp(-dist*0.005);\n  const spiral = (sp1+sp2+rings)*0.35;\n  const sc = [[120,60],[360,180],[240,200],[80,180],[400,60]];\n  let localRD = 0;\n  for (let i = 0; i < sc.length; i++) {\n    const ld = Math.sqrt((sx-sc[i][0])**2+(sy-sc[i][1])**2);\n    const la = Math.sin(ld*0.1-t*(1.5+i*0.3))*Math.exp(-ld*0.008);\n    const li = Math.sin(ld*0.07+t*(0.8+i*0.2)+i)*Math.exp(-ld*0.006);\n    localRD += Math.tanh((la-li*0.6)*2);\n  }\n  localRD /= sc.length;\n  const coupling = act-inh*0.65+bg*0.25+spiral+localRD*0.3;\n  const feedback = Math.sin(coupling*2.5)*(1-Math.exp(-Math.abs(coupling)*0.5));\n  const reaction = Math.tanh(coupling*2.0+feedback*0.3);\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15)+Math.sin((sx+sy)*0.1+t)*0.5+Math.sin((sx-sy)*0.12-t*0.7)*0.3)*1.5)*0.3;\n  const fine = (Math.sin(sx*0.3+t*3)*Math.cos(sy*0.28-t*2.5)+Math.sin(sx*0.22-sy*0.25+t*2.8))*0.15;\n  const blend = reaction*0.6+cell*0.2+fine*0.05+spiral*0.15;\n  const v = blend*0.5+0.5;\n  const pal = [\n    [6,12,42],[12,45,78],[18,85,108],[25,138,132],[60,185,160],\n    [180,210,140],[225,195,80],[210,130,75],[180,70,70],[100,30,55],[40,18,50],[6,12,42]\n  ];\n  const idx = v*(pal.length-1);\n  const i = Math.min(idx|0,pal.length-2);\n  const sf = (1-Math.cos((idx-i)*Math.PI))*0.5;\n  const lum = 0.85+Math.sin(reaction*Math.PI)*0.15;\n  const edgeMag = Math.abs(reaction);\n  const edge = 1.0+(edgeMag>0.6?(edgeMag-0.6)*0.4:0);\n  const vig = 1.0-Math.sqrt((x-cx)**2+(y-cy)**2)*0.001;\n  const br = lum*edge*Math.max(0.7,vig);\n  return [0, 0, 0];\n}",
  "score": {
    "coherence": 10,
    "harmony": 10,
    "complexity": 1,
    "density": 1,
    "total": 22,
    "reason": "The image is a solid black framebuffer, which is perfectly coherent and harmonious with no visual artifacts. However, it contains no mathematical patterns or useful UI information, making its complexity and density extremely low."
  },
  "timestamp": 1774494091893,
  "filename": "gen96_1774494091893"
}