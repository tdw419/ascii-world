{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const w1x = Math.sin(x*0.018+t*0.28)*18 + Math.cos(y*0.013-t*0.22)*12;\n  const w1y = Math.cos(y*0.019-t*0.3)*16 + Math.sin(x*0.016+t*0.33)*11;\n  const w2x = Math.sin((x+y)*0.012+t*0.5)*8;\n  const w2y = Math.cos((x-y)*0.014-t*0.45)*9;\n  const sx = x+w1x+w2x, sy = y+w1y+w2y;\n  const src = [\n    [120+Math.sin(t*0.5)*90, 60+Math.cos(t*0.4)*40, 0.09, 2.1],\n    [360+Math.cos(t*0.45)*80, 80+Math.sin(t*0.6)*45, 0.11, 1.8],\n    [240+Math.sin(t*0.3+2)*130, 40+Math.cos(t*0.5+1)*30, 0.07, 2.4],\n    [80+Math.cos(t*0.7+1)*60, 160+Math.sin(t*0.55+3)*50, 0.13, 1.6],\n    [400+Math.sin(t*0.38+4)*70, 180+Math.cos(t*0.85+2)*55, 0.08, 2.7],\n    [200+Math.cos(t*0.32+5)*100, 200+Math.sin(t*0.48)*60, 0.1, 2.0],\n    [300+Math.sin(t*0.6+3)*50, 120+Math.cos(t*0.35+4)*80, 0.065, 2.5],\n    [160+Math.cos(t*0.55+2)*75, 130+Math.sin(t*0.7+1)*55, 0.085, 1.9],\n    [350+Math.sin(t*0.42)*65, 50+Math.cos(t*0.65+3)*40, 0.075, 2.2]\n  ];\n  let aS=0, iS=0, cS=0;\n  for(const[sX,sY,f,sp]of src){\n    const d=Math.sqrt((sx-sX)**2+(sy-sY)**2);\n    const w=Math.sin(d*f*0.3-t*0.4)*0.3+1;\n    aS+=Math.sin(d*f-t*sp)*w;\n    iS+=Math.sin(d*f*0.65+t*sp*0.55+1.5)*w;\n    cS+=Math.sin(d*f*1.3-t*sp*0.8+3.0)*w*0.4;\n  }\n  const n=src.length, act=aS/n, inh=iS/n, cor=cS/n;\n  const bg=(Math.sin(sx*0.035+sy*0.022+t*0.75)+Math.sin(sx*0.028-sy*0.038+t*1.05))/2\n    +Math.sin((sx+sy)*0.11+t*2.3)*0.18+Math.sin((sx-sy)*0.095-t*1.9)*0.18\n    +Math.sin(sx*0.06+t*1.6)*Math.cos(sy*0.055-t*1.3)*0.12;\n  const dist=Math.sqrt((sx-cx)**2+(sy-cy)**2), ang=Math.atan2(sy-cy,sx-cx);\n  const spiral=(Math.sin(dist*0.045-ang*3.5-t*1.4)+Math.sin(dist*0.035+ang*4.5-t*1.1))*0.35\n    +Math.sin(dist*0.08-ang*2-t*2.5)*0.15;\n  const voroX=Math.floor(sx/35),voroY=Math.floor(sy/35);\n  let minD=99;\n  for(let dx=-1;dx<=1;dx++)for(let dy=-1;dy<=1;dy++){\n    const nx=voroX+dx,ny=voroY+dy;\n    const h=Math.sin(nx*12.9898+ny*78.233+t*0.3)*43758.5453;\n    const pX=(nx+0.5+(h-Math.floor(h))*0.7-0.35)*35,pY=(ny+0.5+(Math.sin(h*0.7)*0.5+0.5)*0.7-0.35)*35;\n    minD=Math.min(minD,Math.sqrt((sx-pX)**2+(sy-pY)**2));\n  }\n  const voro=Math.sin(minD*0.35-t*1.2)*0.6;\n  const feed=act-inh*0.65+bg*0.28+spiral+voro*0.15+cor*0.2;\n  const rd1=Math.tanh(feed*2.0);\n  const rd2=Math.tanh((feed+rd1*0.4-cS*0.15)*1.8);\n  const cell=Math.tanh((Math.sin(sx*0.14)*Math.sin(sy*0.14)+Math.sin((sx+sy)*0.095+t)*0.45)*1.4)*0.25;\n  const fine=Math.sin(sx*0.25+t*3)*Math.sin(sy*0.22-t*2.5)*0.08;\n  const blend=rd2*0.6+rd1*0.15+cell*0.15+fine;\n  const v=blend*0.5+0.5;\n  const edge=Math.abs(rd2)>0.65?1.12:1.0;\n  const glow=Math.max(0,rd2)*0.15;\n  const pal=[\n    [6,12,52],[10,45,85],[12,85,115],[18,130,135],[60,165,130],\n    [170,185,80],[210,170,65],[220,125,65],[195,75,60],[145,45,55],\n    [90,25,50],[45,15,42],[6,12,52]\n  ];\n  const idx=v*(pal.length-1), i=Math.min(idx|0,pal.length-2), f=idx-i;\n  const lum=0.78+Math.sin(rd2*Math.PI)*0.22;\n  return[\n    Math.min(255,((pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*edge+glow*80)|0),\n    Math.min(255,((pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*edge+glow*60)|0),\n    Math.min(255,((pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*edge+glow*40)|0)\n  ];\n}",
  "score": {
    "coherence": 8,
    "harmony": 7,
    "complexity": 9,
    "density": 6,
    "total": 30,
    "reason": "The image displays coherent, flowing patterns with a harmonious color gradient (red/yellow/blue) that creates visual appeal. Its complexity is high due to intricate, mathematically inspired swirls. However, the density is low for a UI, as it lacks clear informational content or functional elements, making it more decorative than practical."
  },
  "timestamp": 1774531029328,
  "filename": "gen142_1774531029328"
}