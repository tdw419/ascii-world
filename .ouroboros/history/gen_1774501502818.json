{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const wx1 = Math.sin(x * 0.02 + t * 0.3) * 15 + Math.cos(y * 0.015 - t * 0.2) * 10;\n  const wy1 = Math.cos(y * 0.02 - t * 0.25) * 15 + Math.sin(x * 0.018 + t * 0.35) * 10;\n  const sx1 = x + wx1, sy1 = y + wy1;\n  const wx2 = Math.sin(sx1 * 0.012 + t * 0.15) * 8 + Math.cos(sy1 * 0.01 - t * 0.18) * 6;\n  const wy2 = Math.cos(sy1 * 0.013 - t * 0.12) * 8 + Math.sin(sx1 * 0.011 + t * 0.2) * 6;\n  const sx = sx1 + wx2, sy = sy1 + wy2;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0, 0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7, 1],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3, 2],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5, 3],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6, 4],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9, 5],\n    [80+Math.sin(t*0.45+3)*70, 40+Math.cos(t*0.65)*50, 0.11, 2.1, 6],\n    [400+Math.cos(t*0.55+2)*60, 220+Math.sin(t*0.75+1)*40, 0.085, 1.8, 7],\n    [200+Math.sin(t*0.38+1)*110, 210+Math.cos(t*0.58+3)*70, 0.095, 2.4, 8]\n  ];\n  let aS = 0, iS = 0, hS = 0;\n  for (const [sX, sY, f, sp, si] of src) {\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    const phase = si * 0.698;\n    aS += Math.sin(d*f - t*sp + phase);\n    iS += Math.sin(d*f*0.7 + t*sp*0.6 + 1.5 + phase);\n    hS += Math.sin(d*f*0.45 - t*sp*0.4 + phase*2) * 0.5;\n  }\n  const act = aS/9, inh = iS/9, harm = hS/9;\n  const bg1 = (Math.sin(sx*0.04+sy*0.025+t*0.8) + Math.sin(sx*0.03-sy*0.04+t*1.1))/2;\n  const bg2 = Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2;\n  const bg3 = Math.sin(sx*0.08+Math.sin(sy*0.06+t)*3+t*0.5)*0.15;\n  const bg = bg1 + bg2 + bg3;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2))*0.4;\n  const spiral2 = Math.sin(dist*0.03+ang*5+t*0.8)*0.2;\n  const fractal = Math.sin(sx*0.3+Math.sin(sy*0.3+Math.sin(sx*0.2+t)*2)*2+t)*0.15;\n  const feed = act - inh*0.7 + bg*0.3 + spiral + spiral2 + fractal + harm*0.4;\n  const reaction = Math.tanh(feed * 2.2);\n  const cell = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  const membrane = Math.sin(Math.abs(reaction)*12)*0.08;\n  const blend = reaction*0.7 + cell*0.2 + membrane*0.1, v = blend*0.5+0.5;\n  const pal = [[6,12,52],[10,48,82],[14,88,108],[18,128,118],[42,158,112],[120,175,88],[188,168,58],[198,128,62],[178,78,62],[128,42,58],[68,22,52],[6,12,52]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const lum = 0.75 + Math.sin(reaction*Math.PI)*0.25;\n  const edge = Math.abs(reaction)>0.65 ? 1.0 + (Math.abs(reaction)-0.65)*0.6 : 1.0;\n  const glow = Math.max(0, 1 - Math.abs(reaction)*1.5)*0.15;\n  return [\n    Math.min(255, ((pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*edge + glow*40)|0),\n    Math.min(255, ((pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*edge + glow*60)|0),\n    Math.min(255, ((pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*edge + glow*80)|0)\n  ];\n}",
  "score": {
    "coherence": 7,
    "harmony": 6,
    "complexity": 9,
    "density": 4,
    "total": 26,
    "reason": "The image has high visual complexity with intricate, swirling patterns but lacks coherence due to chaotic transitions and no clear focal point. Color harmony is moderate with a warm-cool contrast, but it's not optimized for readability. Density is low as it's purely decorative with no functional UI elements, making it unsuitable for a minimal OS interface."
  },
  "timestamp": 1774501502817,
  "filename": "gen307_1774501502817"
}