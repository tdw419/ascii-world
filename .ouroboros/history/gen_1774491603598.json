{
  "code": "(x, y, t) => {\n  const s = (px, py, ax, ay, bx, by, ta, tb) => {\n    const sx = ax + Math.sin(ta) * bx, sy = ay + Math.cos(tb) * by;\n    return Math.sqrt((px - sx) ** 2 + (py - sy) ** 2);\n  };\n  const d1 = s(x, y, 100, 60, 80, 40, t * 0.6, t * 0.4);\n  const d2 = s(x, y, 380, 180, 70, 45, t * 0.5, t * 0.7);\n  const d3 = s(x, y, 240, 30, 120, 25, t * 0.3 + 2, t * 0.5 + 1);\n  const d4 = s(x, y, 60, 200, 60, 35, t * 0.4 + 4, t * 0.6 + 3);\n  const d5 = s(x, y, 420, 50, 55, 30, t * 0.7 + 1, t * 0.35 + 2);\n  const w1 = Math.sin(d1 * 0.08 - t * 2), w2 = Math.sin(d2 * 0.1 - t * 1.7);\n  const w3 = Math.sin(d3 * 0.06 - t * 2.3), w4 = Math.sin(d4 * 0.09 - t * 1.9);\n  const w5 = Math.sin(d5 * 0.07 - t * 2.1);\n  const wx = Math.sin(x * 0.04 + t * 0.3) * 8, wy = Math.sin(y * 0.05 + t * 0.4) * 6;\n  const nx = x + wx, ny = y + wy;\n  const w6 = Math.sin(nx * 0.05 + ny * 0.03 + t * 0.8);\n  const w7 = Math.sin(nx * 0.03 - ny * 0.05 + t * 1.1);\n  const w8 = Math.sin((nx + ny) * 0.15 + t * 3) * 0.12;\n  const w9 = Math.sin((nx - ny) * 0.12 - t * 2.5) * 0.12;\n  const w10 = Math.sin(Math.sqrt(nx * nx + ny * ny) * 0.06 + t * 1.2) * 0.2;\n  const act = (w1 + w2 + w3 + w4 + w5) / 5;\n  const inh = (w6 + w7) / 2;\n  const reaction = act - inh * 0.7 + w8 + w9 + w10;\n  const cx = 240, cy = 120;\n  const dx = x - cx, dy = y - cy;\n  const dist = Math.sqrt(dx * dx + dy * dy);\n  const ang = Math.atan2(dy, dx);\n  const spiral = Math.sin(dist * 0.05 - ang * 3 + t * 1.2);\n  const petal = Math.sin(ang * 5 + t * 0.6) * Math.cos(dist * 0.03 - t * 0.8);\n  const weave = Math.sin(dist * 0.08 + ang * 2 - t * 1.5) * 0.3;\n  const pat2 = Math.tanh((spiral + petal * 0.6 + weave) * 1.8);\n  const grain = Math.sin(x * 1.7 + y * 2.3 + t * 5) * 0.03;\n  const blend = Math.tanh(reaction * 2.5) * 0.55 + pat2 * 0.4 + grain;\n  const v = blend * 0.5 + 0.5;\n  const pal = [\n    [8, 15, 58], [15, 60, 110], [20, 120, 135], [55, 175, 145],\n    [180, 195, 80], [225, 165, 55], [210, 95, 60], [140, 40, 75], [8, 15, 58]\n  ];\n  const idx = v * (pal.length - 1), i = Math.min(idx | 0, pal.length - 2), f = idx - i;\n  const edge = Math.abs(blend) < 0.15 ? 1.0 : 0.0;\n  const lum = 0.82 + Math.sin(reaction * Math.PI) * 0.12 + edge * 0.25;\n  return [\n    Math.min(255, (pal[i][0] + (pal[i+1][0] - pal[i][0]) * f) * lum) | 0,\n    Math.min(255, (pal[i][1] + (pal[i+1][1] - pal[i][1]) * f) * lum) | 0,\n    Math.min(255, (pal[i][2] + (pal[i+1][2] - pal[i][2]) * f) * lum) | 0\n  ];\n}",
  "score": {
    "coherence": 8,
    "harmony": 7,
    "complexity": 9,
    "density": 6,
    "total": 30,
    "reason": "The image displays coherent, flowing organic patterns with smooth color gradients (coherence 8). Its color palette is harmonious with warm and cool tones blending well (harmony 7). The complexity is high due to intricate, non-repeating fractal-like structures (complexity 9). However, the density is low for a UI as it lacks clear information or functional elements, making it more decorative than utilitarian (density 6)."
  },
  "timestamp": 1774491603598,
  "filename": "gen26_1774491603598"
}