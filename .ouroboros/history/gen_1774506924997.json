{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const wx = Math.sin(x*0.018+t*0.3)*18 + Math.cos(y*0.013-t*0.22)*12 + Math.sin((x+y)*0.01+t*0.5)*8;\n  const wy = Math.cos(y*0.018-t*0.28)*18 + Math.sin(x*0.015+t*0.32)*12 + Math.cos((x-y)*0.012-t*0.45)*8;\n  const sx = x + wx, sy = y + wy;\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9],\n    [80+Math.sin(t*0.45+3)*70, 40+Math.cos(t*0.65+1)*50, 0.11, 2.1],\n    [400+Math.cos(t*0.55+2)*60, 200+Math.sin(t*0.75+4)*55, 0.085, 1.8]\n  ];\n  let aS = 0, iS = 0, aS2 = 0, iS2 = 0;\n  for (const [sX, sY, f, sp] of src) {\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    aS += Math.sin(d*f - t*sp);\n    iS += Math.sin(d*f*0.7 + t*sp*0.6 + 1.5);\n    aS2 += Math.sin(d*f*2.1 - t*sp*1.3 + 0.5) * 0.5;\n    iS2 += Math.sin(d*f*1.5 + t*sp*0.9 + 2.8) * 0.5;\n  }\n  const act = (aS + aS2)/8, inh = (iS + iS2)/8;\n  const bg = (Math.sin(sx*0.04+sy*0.025+t*0.8) + Math.sin(sx*0.03-sy*0.04+t*1.1) + Math.sin(sx*0.08+sy*0.06-t*1.5)*0.5)/2.5 + Math.sin((sx+sy)*0.12+t*2.5)*0.2 + Math.sin((sx-sy)*0.1-t*2)*0.2 + Math.sin(sx*0.06-t*0.7)*Math.sin(sy*0.07+t*0.6)*0.3;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.05-ang*3-t*1.5) + Math.sin(dist*0.04+ang*4-t*1.2) + Math.sin(dist*0.08-ang*2-t*2.1)*0.5)*0.35;\n  const cx2 = 240+Math.sin(t*0.3)*80, cy2 = 120+Math.cos(t*0.4)*60;\n  const dist2 = Math.sqrt((sx-cx2)**2 + (sy-cy2)**2), ang2 = Math.atan2(sy-cy2, sx-cx2);\n  const spiral2 = (Math.sin(dist2*0.06-ang2*2.5-t*1.8) + Math.sin(dist2*0.045+ang2*3.5-t*1.0))*0.25;\n  const reaction = Math.tanh((act - inh*0.7 + bg*0.3 + spiral + spiral2)*2.2);\n  const cell1 = Math.tanh((Math.sin(sx*0.15)*Math.sin(sy*0.15) + Math.sin((sx+sy)*0.1+t)*0.5)*1.5)*0.3;\n  const cell2 = Math.tanh((Math.sin(sx*0.08+1)*Math.sin(sy*0.08+2) + Math.sin((sx-sy)*0.06-t*0.8)*0.4)*1.2)*0.2;\n  const detail = Math.sin(sx*0.3+t*1.5)*Math.sin(sy*0.3-t*1.2)*0.1 * Math.max(0, Math.abs(reaction)-0.5);\n  const blend = reaction*0.65 + cell1*0.15 + cell2*0.1 + detail*0.1, v = blend*0.5+0.5;\n  const pal = [[6,12,52],[12,45,85],[18,85,115],[25,135,125],[65,175,130],[180,195,90],[220,175,70],[215,125,75],[185,75,65],[130,40,55],[70,22,48],[6,12,52]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const sf = f*f*(3-2*f);\n  const lum = 0.85 + Math.sin(reaction*Math.PI)*0.15 + Math.sin(t*0.4)*0.05;\n  const edgeMag = Math.abs(reaction), edge = edgeMag > 0.65 ? 1.0 + (edgeMag - 0.65)*0.6 : 1.0;\n  const tempShift = Math.sin(ang + t*0.2) * 0.08;\n  const r0 = pal[i][0] + (pal[i+1][0]-pal[i][0])*sf;\n  const g0 = pal[i][1] + (pal[i+1][1]-pal[i][1])*sf;\n  const b0 = pal[i][2] + (pal[i+1][2]-pal[i][2])*sf;\n  return [\n    Math.min(255, Math.max(0, (r0*(1+tempShift) + g0*tempShift*0.3)*lum*edge))|0,\n    Math.min(255, Math.max(0, (g0 - tempShift*15)*lum*edge))|0,\n    Math.min(255, Math.max(0, (b0*(1-tempShift) + r0*tempShift*0.2)*lum*edge))|0\n  ];\n}",
  "score": {
    "coherence": 7,
    "harmony": 6,
    "complexity": 9,
    "density": 4,
    "total": 26,
    "reason": "The image has high complexity with intricate, flowing patterns but lacks coherence due to abstract, non-structured forms. Color harmony is moderate with a warm-cool contrast, but it's visually noisy. Density is low as it conveys no functional UI information, making it unsuitable for a minimal OS interface."
  },
  "timestamp": 1774506924997,
  "filename": "gen458_1774506924997"
}