{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  const w1 = Math.sin(x*0.018+t*0.25)*12 + Math.cos(y*0.013-t*0.18)*8;\n  const w2 = Math.cos(x*0.011-t*0.15)*6 + Math.sin(y*0.022+t*0.3)*9;\n  const sx = x + w1 + w2*Math.sin(t*0.4), sy = y + w2 + w1*Math.cos(t*0.35);\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.08, 2.0, 1.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.1, 1.7, 0.9],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.06, 2.3, 1.1],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.12, 1.5, 0.85],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.07, 2.6, 1.05],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.09, 1.9, 0.95],\n    [80+Math.sin(t*0.45+3)*70, 40+Math.cos(t*0.65)*50, 0.14, 2.1, 0.8],\n    [400+Math.cos(t*0.55+2)*60, 200+Math.sin(t*0.75+1)*70, 0.055, 2.4, 1.15],\n    [200+Math.sin(t*0.38+1)*110, 220+Math.cos(t*0.48+4)*60, 0.11, 1.6, 0.9]\n  ];\n  let aS=0, iS=0, cS=0;\n  for (const [sX,sY,f,sp,w] of src) {\n    const d = Math.sqrt((sx-sX)**2+(sy-sY)**2);\n    const att = w / (1 + d*0.003);\n    aS += Math.sin(d*f - t*sp) * att;\n    iS += Math.sin(d*f*0.7 + t*sp*0.6 + 1.5) * att;\n    cS += Math.sin(d*f*1.3 - t*sp*0.8 + 3.0) * att * 0.5;\n  }\n  const act = aS/9, inh = iS/9, cross = cS/9;\n  const bg = (Math.sin(sx*0.035+sy*0.022+t*0.7) + Math.sin(sx*0.028-sy*0.038+t*0.95))/2\n    + Math.sin((sx+sy)*0.11+t*2.2)*0.15 + Math.sin((sx-sy)*0.09-t*1.8)*0.15\n    + Math.sin(sx*0.06+t*1.5)*Math.cos(sy*0.05-t*1.2)*0.1;\n  const dist = Math.sqrt((sx-cx)**2+(sy-cy)**2), ang = Math.atan2(sy-cy, sx-cx);\n  const spiral = (Math.sin(dist*0.045-ang*3.5-t*1.3) + Math.sin(dist*0.038+ang*4.5-t*1.0)\n    + Math.sin(dist*0.06-ang*2-t*1.8)*0.5)*0.35;\n  const voronoi = (() => {\n    let md = 999, md2 = 999;\n    for (let i = 0; i < 7; i++) {\n      const vx = 80+i*60+Math.sin(t*0.3+i*1.7)*25, vy = 40+Math.cos(t*0.25+i*2.1)*20+((i*37)%120);\n      const d = Math.abs(sx-vx)+Math.abs(sy-vy);\n      if (d < md) { md2 = md; md = d; } else if (d < md2) md2 = d;\n    }\n    return Math.sin((md2-md)*0.3)*0.25;\n  })();\n  const rd1 = Math.tanh((act - inh*0.65 + bg*0.3 + spiral)*2.0);\n  const rd2 = Math.tanh((cross*0.5 + act*inh*0.3 - bg*0.2 + voronoi)*1.8);\n  const reaction = Math.tanh((rd1*0.7 + rd2*0.3)*1.5);\n  const cell = Math.tanh((Math.sin(sx*0.13)*Math.sin(sy*0.13) + Math.sin((sx+sy)*0.09+t)*0.4\n    + Math.sin(sx*0.07-t*0.8)*Math.cos(sy*0.11+t*0.6)*0.3)*1.4)*0.25;\n  const micro = Math.sin(sx*0.3+t*3)*Math.sin(sy*0.3-t*2.5)*0.08*Math.abs(reaction);\n  const blend = reaction*0.65 + cell*0.25 + micro, v = blend*0.5+0.5;\n  const pal = [[5,12,52],[12,55,95],[18,110,125],[35,165,145],[120,195,110],\n    [195,190,70],[220,150,65],[200,95,75],[160,50,85],[90,25,65],[35,15,55],[5,12,52]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const edge = Math.abs(reaction)>0.65 ? 1.12 : Math.abs(reaction)>0.4 ? 1.05 : 0.95;\n  const lum = 0.85 + Math.sin(reaction*Math.PI)*0.15 + Math.abs(rd2)*0.08;\n  const glow = Math.max(0, 1 - Math.abs(reaction)*1.5)*0.1;\n  return [\n    Math.min(255, ((pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*edge + glow*40)|0),\n    Math.min(255, ((pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*edge + glow*60)|0),\n    Math.min(255, ((pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*edge + glow*80)|0)\n  ];\n}",
  "score": {
    "coherence": 7,
    "harmony": 6,
    "complexity": 9,
    "density": 4,
    "total": 26,
    "reason": "The image has high complexity with intricate, flowing patterns but lacks coherence due to abstract, non-UI elements. Color harmony is moderate with vibrant but clashing tones. Density is low as it's purely decorative with no functional information, making it unsuitable for a minimal OS UI."
  },
  "timestamp": 1774531004467,
  "filename": "gen141_1774531004467"
}