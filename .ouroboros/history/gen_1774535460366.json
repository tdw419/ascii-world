{
  "code": "(x, y, t) => {\n  const wells = [\n    [160+Math.sin(t*0.4)*120, 100+Math.cos(t*0.3)*60, 90],\n    [320+Math.cos(t*0.35)*100, 150+Math.sin(t*0.5)*70, 110],\n    [240+Math.sin(t*0.25+2)*150, 75+Math.cos(t*0.45)*80, 75],\n    [100+Math.cos(t*0.55+1)*90, 190+Math.sin(t*0.4+2)*50, 65],\n    [380+Math.sin(t*0.3+3)*80, 55+Math.cos(t*0.6+1)*60, 95],\n    [240+Math.cos(t*0.3+4)*100, 210+Math.sin(t*0.35)*70, 80],\n    [60+Math.sin(t*0.45+5)*70, 130+Math.cos(t*0.38+3)*55, 70],\n    [420+Math.cos(t*0.28+2)*60, 180+Math.sin(t*0.52+4)*65, 85]\n  ];\n  let wx = 0, wy = 0;\n  for (const [wX, wY, mass] of wells) {\n    const dx = wX - x, dy = wY - y;\n    const dist2 = dx*dx + dy*dy + 800;\n    const pull = mass / dist2;\n    wx += dx * pull * 40;\n    wy += dy * pull * 40;\n  }\n  wx += Math.sin(x*0.013+t*0.35)*14 + Math.cos(y*0.018-t*0.22)*9 + Math.sin((x+y)*0.008+t*0.15)*6;\n  wy += Math.cos(y*0.013-t*0.28)*14 + Math.sin(x*0.016+t*0.38)*9 + Math.cos((x-y)*0.009-t*0.18)*6;\n  const sx = x + wx, sy = y + wy;\n  let act = 0, inh = 0, pot = 0, kin = 0;\n  for (let i = 0; i < wells.length; i++) {\n    const [sX, sY, mass] = wells[i];\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    const ph = t*(0.7+i*0.12) + i*1.9;\n    const w = mass/90;\n    act += (Math.sin(d*0.09-ph) + Math.sin(d*0.13-ph*1.3)*0.5) * Math.exp(-d*0.007) * w;\n    inh += (Math.sin(d*0.04+ph*0.5) + Math.sin(d*0.06+ph*0.7)*0.4) * Math.exp(-d*0.0025) * w;\n    pot += Math.sin(d*0.055+ph*0.6)*0.5*Math.exp(-d*0.004)*w;\n    kin += Math.cos(d*0.11-ph*1.4)*0.5*Math.exp(-d*0.006)*w;\n  }\n  act /= 8; inh /= 8; pot /= 8; kin /= 8;\n  const flow = (Math.sin(sx*0.028+sy*0.02+t*0.55)+Math.sin(sx*0.022-sy*0.032+t*0.85))/2;\n  const micro = Math.sin(sx*0.14+t*2.2)*Math.sin(sy*0.13-t*1.9)*0.25;\n  const energy = Math.sin((pot+kin)*3.5+t*0.4)*0.25;\n  const cx=240, cy=120, dist=Math.sqrt((sx-cx)**2+(sy-cy)**2), ang=Math.atan2(sy-cy,sx-cx);\n  const spiral = (Math.sin(dist*0.045-ang*5-t*1.8)+Math.sin(dist*0.035+ang*3-t*1.3))*0.2;\n  const rd = act - inh*0.75 + flow*0.2 + micro + energy + spiral;\n  const reaction = Math.tanh(rd*4.0);\n  const edgeRaw = 1.0 - Math.abs(reaction);\n  const edgeGlow = Math.pow(edgeRaw, 2.5) * 2.5;\n  const v = reaction*0.5+0.5;\n  const pal = [[1,1,12],[0,15,45],[0,60,120],[0,180,220],[20,255,200],[140,255,80],[255,220,30],[255,100,50],[255,40,90],[200,10,80],[80,0,50],[15,1,20],[1,1,12]];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  let r = pal[i][0]+(pal[i+1][0]-pal[i][0])*f;\n  let g = pal[i][1]+(pal[i+1][1]-pal[i][1])*f;\n  let b = pal[i][2]+(pal[i+1][2]-pal[i][2])*f;\n  const peak = Math.max(0, reaction);\n  const baseGlow = peak * peak * 0.6;\n  r += edgeGlow*40 + baseGlow*80;\n  g += edgeGlow*180 + baseGlow*120;\n  b += edgeGlow*255 + baseGlow*200;\n  const spH = Math.max(0, spiral)*0.8;\n  r += spH*80; g += spH*160; b += spH*255;\n  const vign = 1 - Math.min(Math.sqrt((x-240)**2+(y-120)**2)/320, 1)*0.45;\n  const lum = 0.85 + Math.sin(reaction*Math.PI)*0.15;\n  r = Math.min(255, r*lum*vign); g = Math.min(255, g*lum*vign); b = Math.min(255, b*lum*vign);\n  return [r|0, g|0, b|0];\n}",
  "score": {
    "coherence": 8,
    "harmony": 7,
    "complexity": 9,
    "density": 4,
    "total": 28,
    "reason": "The image has strong visual coherence with flowing, organic patterns and minimal artifacts. Its color harmony is effective with a cool blue/purple palette and glowing cyan accents, though the high contrast may reduce readability. Complexity is high due to intricate, fractal-like forms. However, density is low — it's mostly decorative with little functional UI information, making it unsuitable for a practical OS interface."
  },
  "timestamp": 1774535460366,
  "filename": "gen25_1774535460366"
}