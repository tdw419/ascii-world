{
  "code": "(x, y, t) => {\n  const w1 = Math.sin(x * 0.02 + t * 0.3) * Math.cos(y * 0.025 - t * 0.2) * 15;\n  const w2 = Math.cos(x * 0.018 - t * 0.25) * Math.sin(y * 0.022 + t * 0.18) * 12;\n  const wx = x + w1, wy = y + w2;\n  const s = [\n    [120+Math.sin(t*.5)*80, 60+Math.cos(t*.35)*40],\n    [360+Math.cos(t*.45)*80, 50+Math.sin(t*.6)*35],\n    [80+Math.sin(t*.3+2)*60, 180+Math.cos(t*.5+1)*50],\n    [400+Math.cos(t*.4+1)*70, 190+Math.sin(t*.55)*45],\n    [240+Math.sin(t*.25+3)*120, 120+Math.cos(t*.4+2)*80]\n  ];\n  const fr=[.08,.09,.07,.1,.065], sp=[2,1.8,2.3,1.6,2.5], ph=[0,1.2,2.4,3.6,4.8];\n  let act=0, inh=0;\n  for(let i=0;i<5;i++){const d=Math.sqrt((wx-s[i][0])**2+(wy-s[i][1])**2);const v=Math.sin(d*fr[i]-t*sp[i]+ph[i]);if(i<3)act+=v;else inh+=v;}\n  act/=3; inh/=2;\n  const s6x=240+Math.sin(t*.2)*180, s6y=120+Math.cos(t*.15)*100;\n  const d6=Math.sqrt((wx-s6x)**2+(wy-s6y)**2);\n  const slow=Math.sin(d6*.03-t*.8);\n  const a1=t*.1, a2=t*.07, a3=t*.05;\n  const pw1=Math.sin(wx*Math.cos(a1)*.04+wy*Math.sin(a1)*.04+t*.6);\n  const pw2=Math.sin(wx*Math.cos(a2)*.05-wy*Math.sin(a2)*.05+t*.9);\n  const pw3=Math.sin((wx+wy)*Math.cos(a3)*.03+t*.4);\n  const hf1=Math.sin((wx+wy)*.12+t*2.5)*.12;\n  const hf2=Math.sin((wx-wy)*.1-t*2)*.12;\n  const hf3=Math.sin(wx*.15+wy*.08+t*3)*.08;\n  const r1=Math.tanh((act-inh*.7+slow*.3+pw1*.2+hf1)*2.5);\n  const r2=Math.tanh((pw2*.5+slow*.4+act*.3+hf2)*2);\n  const r3=Math.tanh((pw3*.6+inh*.2+r1*.3+hf3)*1.8);\n  const bl=r1*.5+r2*.25+r3*.25;\n  const cx=240, cy=120, dist=Math.sqrt((x-cx)**2+(y-cy)**2);\n  const ang=Math.atan2(y-cy, x-cx);\n  const radMod=Math.sin(dist*.035-t*1.2)*Math.cos(ang*4+t*.3)*.15;\n  const spiral=Math.sin(ang*3-dist*.02+t*.5)*.1;\n  const vMod=Math.sin(dist*.02)*.08;\n  const v=Math.tanh((bl+radMod+spiral+vMod)*1.5)*.5+.5;\n  const pal=[[12,15,58],[20,72,115],[38,142,138],[55,178,155],[218,188,52],[208,92,62],[115,32,72],[55,18,85],[12,15,58]];\n  const idx=v*(pal.length-1), i=Math.min(idx|0,pal.length-2), f=idx-i;\n  const lum=.86+Math.sin(bl*Math.PI*1.5)*.14;\n  const sh=spiral*6;\n  return[\n    Math.min(255,Math.max(0,(pal[i][0]+(pal[i+1][0]-pal[i][0])*f+sh)*lum))|0,\n    Math.min(255,Math.max(0,(pal[i][1]+(pal[i+1][1]-pal[i][1])*f-sh*.4)*lum))|0,\n    Math.min(255,Math.max(0,(pal[i][2]+(pal[i+1][2]-pal[i][2])*f+sh*.3)*lum))|0\n  ];\n}",
  "score": {
    "coherence": 8,
    "harmony": 7,
    "complexity": 9,
    "density": 4,
    "total": 28,
    "reason": "The image has high coherence with smooth, flowing organic patterns and no visual artifacts. Its color harmony is strong with a warm-cool contrast, though the intensity may reduce readability. Complexity is high due to the fractal-like, evolving patterns. However, density is low — it's purely decorative with no functional UI elements, making it unsuitable for a practical OS interface."
  },
  "timestamp": 1774491158959,
  "filename": "gen8_1774491158959"
}