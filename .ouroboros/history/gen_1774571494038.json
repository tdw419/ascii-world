{
  "code": "(x, y, t) => {\n  const wx = Math.sin(x*0.02+t*0.3)*12+Math.cos(y*0.015-t*0.2)*8;\n  const wy = Math.cos(y*0.02-t*0.25)*12+Math.sin(x*0.018+t*0.35)*8;\n  const sx = x+wx, sy = y+wy;\n  const P = [2,3,5,7,11,13,17,19];\n  const src = [\n    [100+Math.sin(t*0.6)*70, 55+Math.cos(t*0.4)*25, P[0]*0.04, P[1]*0.7],\n    [380+Math.cos(t*0.5)*70, 55+Math.sin(t*0.7)*25, P[1]*0.03, P[2]*0.5],\n    [70+Math.sin(t*0.3+2)*50, 120+Math.cos(t*0.5+1)*35, P[2]*0.022, P[3]*0.4],\n    [410+Math.cos(t*0.8+1)*50, 120+Math.sin(t*0.6+3)*35, P[3]*0.025, P[4]*0.35],\n    [100+Math.sin(t*0.4+4)*60, 185+Math.cos(t*0.9+2)*40, P[4]*0.015, P[5]*0.3],\n    [380+Math.cos(t*0.35+5)*60, 185+Math.sin(t*0.55)*40, P[5]*0.018, P[6]*0.25],\n    [240+Math.sin(t*0.45)*90, 90+Math.cos(t*0.65)*50, P[6]*0.016, P[7]*0.2],\n    [240+Math.cos(t*0.5+3)*80, 150+Math.sin(t*0.75+1)*55, P[7]*0.013, P[0]*1.1]\n  ];\n  let aS=0, iS=0;\n  for(const[sX,sY,f,sp]of src){const d=Math.sqrt((sx-sX)**2+(sy-sY)**2);aS+=Math.sin(d*f-t*sp);iS+=Math.sin(d*f*0.7+t*sp*0.6+1.5);}\n  const act=aS/8, inh=iS/8;\n  const bg=(Math.sin(sx*P[0]*0.02+sy*P[1]*0.01+t*0.8)+Math.sin(sx*P[2]*0.008-sy*P[3]*0.01+t*1.1)+Math.sin(sx*P[4]*0.005+sy*P[5]*0.005+t*1.5))/3;\n  const dist=Math.sqrt((sx-240)**2+(sy-120)**2), ang=Math.atan2(sy-120,sx-240);\n  const spiral=(Math.sin(dist*P[1]*0.015-ang*P[2]-t*1.5)+Math.sin(dist*P[3]*0.01+ang*P[4]-t*1.2))*0.3;\n  const reaction=Math.tanh((act-inh*0.6+bg*0.25+spiral)*3.5);\n  const cell=Math.tanh((Math.sin(sx*0.2)*Math.sin(sy*0.2)+Math.sin((sx+sy)*0.12+t)*0.5)*2.0)*0.15;\n  const gx=sx%48, gy=sy%36;\n  const gridLine=gx<0.8||gy<0.8?0.12:0;\n  const p1=sx>4&&sx<156, p2=sx>164&&sx<316, p3=sx>324&&sx<476;\n  const q1=sy>4&&sy<76, q2=sy>84&&sy<156, q3=sy>164&&sy<236;\n  const inPanel=(p1||p2||p3)&&(q1||q2||q3);\n  const pEdge=inPanel?((sx%160<1.5||sx%160>158.5||sy%80<1.5||sy%80>78.5)?0.35:0):0;\n  const topBar=sy>1.5&&sy<5?0.25:0, botBar=sy>234&&sy<238?0.25:0;\n  let textL=0;\n  if(inPanel){const ly=sy%80;if(ly>12&&ly<15)textL=(Math.sin(sx*0.8+t*3)*0.5+0.5)*0.18;if(ly>22&&ly<25)textL=Math.max(textL,(Math.sin(sx*0.6+t*2.2+1)*0.5+0.5)*0.12);if(ly>32&&ly<35)textL=Math.max(textL,(Math.sin(sx*0.4+t*1.8+2)*0.5+0.5)*0.08);}\n  let dataV=0;\n  if(p3&&q2){for(let i=0;i<8;i++){const bx=338+i*17,bh=8+Math.sin(t*P[i%8]*0.25+i*1.1)*18+18;if(sx>bx&&sx<bx+10&&sy>154-bh&&sy<154)dataV=0.35;}}\n  let neuralLayer=0;\n  if(p1&&q2){for(let l=0;l<4;l++){const ly2=95+l*14;if(sy>ly2&&sy<ly2+2)neuralLayer=(Math.sin(sx*0.3+t*P[l]*0.4+l*2)*0.5+0.5)*0.2;}}\n  const blend=reaction*0.65+cell+gridLine+pEdge+topBar+botBar+textL+dataV+neuralLayer;\n  const v=Math.max(0,Math.min(1,blend*0.5+0.5));\n  const pal=[[4,4,22],[8,15,55],[0,60,100],[0,220,180],[120,255,40],[255,240,0],[255,100,10],[255,20,70],[160,0,240],[60,0,140],[4,4,22]];\n  const idx=v*(pal.length-1), i=Math.min(idx|0,pal.length-2), f=idx-i;\n  const glow=Math.pow(Math.max(0,reaction),4)*1.2;\n  const edgeGlow=Math.abs(reaction)>0.65?Math.pow(Math.abs(reaction),2)*0.6:0;\n  const lum=1.0+glow+edgeGlow;\n  const satBoost=inPanel?1.1:1.0;\n  return[\n    Math.min(255,(pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*satBoost)|0,\n    Math.min(255,(pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*satBoost)|0,\n    Math.min(255,(pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*satBoost)|0\n  ];\n}",
  "score": {
    "coherence": 5,
    "harmony": 4,
    "complexity": 9,
    "density": 3,
    "total": 21,
    "reason": "The image features high complexity with intricate, swirling patterns and strong visual contrast. However, its coherence is low due to chaotic, non-uniform shapes and visible artifacts. Harmony is weak because the neon colors clash aggressively. Density is very low — it's purely decorative with no functional UI elements — making it unsuitable for a practical OS interface."
  },
  "timestamp": 1774571494037,
  "filename": "gen897_1774571494037"
}