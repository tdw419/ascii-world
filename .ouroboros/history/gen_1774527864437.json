{
  "code": "(x, y, t) => {\n  const cx = 240, cy = 120;\n  let wx = 0, wy = 0, sx = x, sy = y;\n  for (let o = 0; o < 3; o++) {\n    const s = 1 << o;\n    wx += (Math.sin(sx*0.012*s + t*0.25*s) * 12 + Math.cos(sy*0.009*s - t*0.18*s) * 8) / s;\n    wy += (Math.cos(sy*0.013*s - t*0.22*s) * 12 + Math.sin(sx*0.01*s + t*0.3*s) * 8) / s;\n    sx = x + wx; sy = y + wy;\n  }\n  const src = [\n    [160+Math.sin(t*0.6)*100, 80+Math.cos(t*0.4)*50, 0.07, 2.0, 1.0],\n    [320+Math.cos(t*0.5)*100, 160+Math.sin(t*0.7)*50, 0.09, 1.7, 0.8],\n    [240+Math.sin(t*0.3+2)*140, 120+Math.cos(t*0.5+1)*90, 0.055, 2.3, 1.2],\n    [120+Math.cos(t*0.8+1)*80, 180+Math.sin(t*0.6+3)*60, 0.11, 1.5, 0.7],\n    [360+Math.sin(t*0.4+4)*90, 60+Math.cos(t*0.9+2)*70, 0.065, 2.6, 1.1],\n    [240+Math.cos(t*0.35+5)*120, 200+Math.sin(t*0.55)*80, 0.08, 1.9, 0.9],\n    [80+Math.sin(t*0.45+3)*70, 40+Math.cos(t*0.65+1)*50, 0.13, 2.1, 0.6],\n    [400+Math.cos(t*0.55+2)*60, 200+Math.sin(t*0.75+4)*45, 0.075, 1.8, 1.3]\n  ];\n  let aS = 0, iS = 0, cS = 0;\n  for (const [sX, sY, f, sp, w] of src) {\n    const d = Math.sqrt((sx-sX)**2 + (sy-sY)**2);\n    aS += Math.sin(d*f - t*sp) * w;\n    iS += Math.sin(d*f*0.7 + t*sp*0.6 + 1.5) * w;\n    cS += Math.sin(d*f*1.3 - t*sp*0.8 + 3.0) * w * 0.5;\n  }\n  const tw = aS + iS + cS;\n  aS /= tw; iS /= tw; cS /= tw;\n  const bg1 = Math.sin(sx*0.035+sy*0.022+t*0.7), bg2 = Math.sin(sx*0.028-sy*0.038+t*0.95);\n  const bg3 = Math.sin((sx+sy)*0.11+t*2.2)*0.3, bg4 = Math.sin((sx-sy)*0.09-t*1.8)*0.3;\n  const micro = Math.sin(sx*0.18+sy*0.14+t*3.5)*0.15;\n  const bg = (bg1+bg2)/2 + bg3 + bg4 + micro;\n  const dist = Math.sqrt((sx-cx)**2 + (sy-cy)**2), ang = Math.atan2(sy-cx, sx-cx);\n  const sp1 = Math.sin(dist*0.045-ang*3.5-t*1.4), sp2 = Math.sin(dist*0.035+ang*5-t*1.1);\n  const sp3 = Math.sin(dist*0.08-ang*2-t*2.5)*0.3;\n  const spiral = (sp1+sp2)*0.35 + sp3;\n  const react = Math.tanh((aS - iS*0.65 + cS*0.35 + bg*0.25 + spiral)*2.0);\n  const react2 = Math.tanh((iS - aS*0.5 + cS*0.4 + bg*0.35 - spiral*0.5)*1.8);\n  const cell = Math.tanh((Math.sin(sx*0.14)*Math.sin(sy*0.14) + Math.sin((sx+sy)*0.09+t)*0.4)*1.4)*0.25;\n  const vort = Math.sin(ang*6 + dist*0.03 - t*1.8)*Math.exp(-dist*0.005)*0.2;\n  const blend = react*0.5 + react2*0.15 + cell*0.15 + vort*0.1;\n  const v = blend*0.5+0.5;\n  const pal = [\n    [6,12,52],[12,42,88],[18,85,115],[28,135,130],[65,165,140],\n    [160,180,100],[210,175,65],[225,135,55],[200,85,65],[160,45,75],\n    [100,25,65],[50,15,55],[6,12,52]\n  ];\n  const idx = v*(pal.length-1), i = Math.min(idx|0, pal.length-2), f = idx-i;\n  const edge = Math.abs(react)>0.75 ? 1.2 : Math.abs(react2)>0.7 ? 1.1 : 1.0;\n  const glow = Math.max(0, 1.0 - Math.abs(react)*1.5)*0.15;\n  const lum = 0.85 + Math.sin(react*Math.PI)*0.15 + glow;\n  return [\n    Math.min(255, (pal[i][0]+(pal[i+1][0]-pal[i][0])*f)*lum*edge)|0,\n    Math.min(255, (pal[i][1]+(pal[i+1][1]-pal[i][1])*f)*lum*edge)|0,\n    Math.min(255, (pal[i][2]+(pal[i+1][2]-pal[i][2])*f)*lum*edge)|0\n  ];\n}",
  "score": {
    "coherence": 9,
    "harmony": 8,
    "complexity": 9,
    "density": 5,
    "total": 31,
    "reason": "The image has high coherence with smooth, flowing gradients and no visual artifacts. Its harmony is strong with a balanced warm-cool color scheme that is aesthetically pleasing. Complexity is high due to the intricate, organic patterns. However, density is low as it lacks functional UI elements or information, making it more decorative than practical for an OS interface."
  },
  "timestamp": 1774527864437,
  "filename": "gen47_1774527864437"
}